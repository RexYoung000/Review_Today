# Review Today 产品界面与体验方向

## 页面与控件收敛（2026-09-07，已授权实施）

今天页标题与主按钮固定在内容区顶部，下方统计和内容独立滚动，覆盖此前今天页整体居中的约定。Agent 起始页仍整体居中，名称和 slogan 各自居中。热力图保留 26 周，增加月份定位并收紧全零状态留白。

输入仅在明确点击、键盘导航或输入操作后获焦；初次进入和普通切换会话不抢焦点，外部点击、窗口失活及听写锁定取消活动焦点表现，后续用户操作优先于延迟回焦。所有控件以自己的轮廓统一背景、悬停、按下、选中、焦点及点击区域，不叠加不同圆角。

知识库筛选与「总数 N」同排，数量代表当前筛选结果，零项仍显示；窄窗主题另排。卡片用进入符号表示查看详情，标题最多两行、悬停可读全文；明确不完整的历史标题用现有学习目标作显示回退，不改原始数据。首次空态引导至 Agent，筛选和生命周期空态分别说明。

会话列表始终展开，忽略旧折叠偏好。无进行中会话且未搜索时显示新吉祥物与「开始学习」气泡，点击进入 Agent 并聚焦已有草稿，不自动创建消息。角色和气泡在会话工具栏与底部设置栏之间按可见整体垂直居中，适应窗口高度。气泡采用柔和的大圆角实底，无常态描边、无尖尾；略向右偏并轻微倾斜，以两颗递小圆泡连接角色。文字使用已选定的「荆南麦圆」原始字形，左对齐分两行：「开始」13 pt、「学习」22 pt，字距均为 1 pt，不合成加粗；完整可访问名称仍为「开始学习」。仅此气泡使用该字体，其他界面维持系统字体。字体随 App 内嵌并仅在进程内注册，同时包含原始 OFL 1.1 版权与许可。仅气泡加一层微弱向下软阴影，角色与连接小圆泡不增加阴影。角色等待 1 秒后，循环交替播放已有 8 秒阅读和 4 秒左右观察动作，段间留 0.8 秒轻呼吸；覆盖上一轮单次播放后静止的约定；搜索/归档空态不使用新建引导。知识库空态、复习页统一新角色；复习判断用思考，计入复习保留普通保存反馈。隐藏/后台暂停，减少动态静止，不改变既有语音与待处理随机动作。

本轮不增加模型、实时语音、播报或声纹能力，不改统计卡交互语义。原生验收与未通过项记录在 docs/m1-acceptance.md，最终体验验收保持开放。

## 待机动作修订：竖立阅读与夸张弹性（2026-09-07）

Rex 选择更夸张的卡通力度：伸展最高约静止高度的 140%，弹跳离地最高约静止身高的 35%，包含明显蓄力、腾空、落地挤压和回弹。保留 4 秒左右看看、3.6 秒伸展、3.2 秒弹跳；首版幅度需在 App 小尺寸、正常速度下验收。

阅读改为 8 秒：角色侧倾、看向右侧，合拢本子从身后绕侧拿到身前，竖直展开阅读、翻一页、合上并收回身后。保持无手造型；本子不透明，依靠真实遮挡和前后层级切换出现，不渐显或从零放大。纸页朝角色、外封面朝观众，书脊竖直、略带 V 形透视（两侧外缘高、中央书脊低，封面与纸页方向一致），上沿低于眼睛；封面复用 R 母版轮廓的单色简化标志，不镜像、不增加文字或玻璃材质。

待处理空态透明画布改为 150×170，保留静止角色实际大小，统一各动作取景，不以缩小角色或动态缩放镜头避免裁切。原身体网格、权重、黑白材质与眼部规则不变；前述随机等待、暂停恢复、减少动态以及仅待处理空态和 HTML 的范围继续生效。以原生运行及正常速度录制验证，离线视频单独标注。

## 随机待机与眼部规则（2026-09-07）

黑色和银白角色均使用白眼白、黑瞳孔；仅银白角色眼眶使用约 1 pt 深灰细线，随眼部形变，身体无外描边。统一用于待机、思考和语音，覆盖此前反转眼部规则。

待处理空态与 HTML 预览接入左右看看（4 秒）、轻轻弹跳（3.2 秒）、眨眼伸展（3.6 秒）、悬浮翻本子（6 秒）；不增加手臂，本子无文字。首段前等 1 秒，每段完整结束后等 1.5–3 秒，从其他三段等概率选择，禁止相邻重复；停顿轻微呼吸。动作首尾姿态一致，道具收起、阴影恢复。隐藏/后台暂停且恢复不追赶，销毁停止；减少动态效果立即静止并收起本子。只扩展原 Spine 生成源和共用调度器，保留原网格、权重、语音和思考收尾语义。

## 深浅材质与页面重心（2026-09-07）

深色主题使用银白吉祥物、深眼底和浅瞳孔；浅色主题继续黑色主体、白眼底和深瞳孔。思考、语音、待机共用主题材质，保留原明暗与几何，移除轮廓补偿描边；小球与波条用相应灰阶保持前后景辨识。此规则覆盖此前浅深主题统一黑色主体的约定，Logo 不改。

Agent 起始内容在可用区域内整体垂直居中并限制宽度；今天页采用相同的整体重心原则，空间不足时正常滚动。AirJelly 仅借鉴内容重心与留白，不引入其 Logo 或功能。移除日常输入区 Return/Shift Return 操作提示、今天标题下任务数量小字；保留 slogan、统计标签、错误与必要状态信息，快捷键行为不变。

## 听写忠实整理与反馈（2026-09-07，已授权实施）

结束录音后一次回填可编辑草稿，用户手动发送；保留北京地域、当前模型、录音生命周期与已有吉祥物。本轮不接声纹或实时转写，不承诺过滤旁人讲话。

整理只依据本次转写：去明确赘词和无意义重复，处理明确口误，保留有意强调、否定对比、条件、不确定性、数字单位、术语及知识答案。按语意分段，并列项用列表、步骤/明确编号用编号；不添加标题、总结、信息或调整内容顺序。明确针对本稿的口述排版指令转为换行等排版，引用、讨论或不确定的指令保留为文字。提问与任务请求仍为草稿，不代为执行。

录音成功才进入聆听，停止录音后立即进入真实转写/整理状态。保存成功立即恢复编辑和发送，不等待收尾动画；成功显示「已添加到草稿」，整理回退显示非阻断提示「已保留原始转写，可直接编辑」。保留单次撤销、取消、原会话绑定、失败恢复及减少动态效果。分阶段记录耗时，不显示虚构进度、不承诺未经测量的秒出。

Typeless 仅作为自我修正、自动排版与减少修改的产品参考（https://www.typeless.com/）；未确认其内部模型或延迟实现。当前 Fun-ASR-Flash 不支持说话人分离（https://help.aliyun.com/zh/model-studio/asr-model/）。验收记录见 docs/m1-acceptance.md，本节授权不代表已验证或最终验收通过。

## 主窗口与页面去重（2026-09-07）

主窗口移除横跨内容的标题栏与重复页标题，系统关闭/最小化/全屏按钮保留在侧栏顶部；侧栏使用更圆润的连续圆角。保留侧栏导航名、会话列表及会话操作，正文不重复显示当前页/会话名。Agent 起始页只显示 App 名称与 slogan，移除正文 Logo；侧栏展开与收起均保留已确认的品牌 Logo，展开最小宽度保证字标不被截断；知识库去掉大小两层标题，保留筛选与数量。听写按钮移至发送按钮左侧，复用中性悬停、按下和键盘焦点反馈；听写回填后仍由用户发送，不改接口。待处理空态复用黑色 Spine 角色，增加轻微骨架呼吸；减少动态效果静止，离页/后台暂停。参考 Typeless 仅限窗口按钮布局与侧栏圆角。

## 黑白材质接入日常 App（2026-09-07，已授权）

Rex 已确认将本轮黑白材质应用到 App。日常页面、复习、设置及菜单入口启用已确认的中性实底、黑白按钮/选中/焦点与无色阴影；保留布局、Logo 和错误/警告/删除语义色，不引入玻璃界面。思考、语音与小尺寸入口统一为黑色角色及相同眼部，仅环绕碎片小球灰白；波条按原活动量使用灰阶反馈。保留现有听写和会话逻辑，不改变 API、数据或模型配置。

原生/HTML 对照继续归档，不把样例内容接入日常数据。覆盖下方“等待接入”的历史边界；本次接入进展与验证见 `brand/refresh-2026-09/monochrome-ui/README.md`。完成开发签名构建与日常窗口检查后交付；正式发布仍单独处理。

## 云端听写接入（2026-09-07，已授权实施）

使用北京地域 `fun-asr-flash-2026-06-15`，结束录音后转写并保守整理，追加可撤销草稿，由用户发送。单次最多 5 分钟；录音/处理期间锁定输入及发送。真实音量驱动聆听，转写/整理驱动思考，结束后收敛；不使用回答动画，不增加语音对话。

录音与请求绑定原草稿，离页/切换会话中断并保留原草稿的待重试录音；成功保存草稿后删除本机音频，失败保留重试，取消删除/重录/删除会话清理。后台不持久保存音频，不自动重试可能计费的请求。识别、整理与主文字供应商配置独立，整理失败保留原始转写。

百炼官方隐私说明表示不用于训练，但会依法存储调用产生的数据；未承诺具体音频即时删除期限，因此不得将本机删除描述为云端删除。来源：https://help.aliyun.com/zh/model-studio/privacy-notice 。海外上线前重新核对地域、密钥、模型、数据处理条款与价格。


## 黑白材质与明暗反馈试作（2026-09-07，已授权）

制作独立、内存数据的原生「当前版／试作版」对照。Rex 看过试作后要求主体为黑色，并进一步取消不协调的青绿点睛。最新规则覆盖原计划的银白主体与青绿反馈：画布、侧栏、正文、标签、历史进度、选中、焦点、思考小球与语音波条全部使用中性黑白灰；黑色角色仅保留克制反光，活动通过明暗、动作、描边和文字区分。错误／警告／删除保留必要语义色。保持实底、布局、Logo 与交互，不重新引入玻璃 UI。共享样式默认仍为当前版，日常接入等待 Rex 查看小样。

角色保留骨架、网格、轮廓、形变、动作节奏和生命周期；材质以已有纹理明暗派生，几何继续来自原始数据，禁止从改色素材按绿色阈值重提轮廓。浅深主题使用同一黑色主体，通过中性边缘保证辨识。思考小球与语音波条的活动/停止反馈使用灰阶，不再带青绿。旧角色入口与加载占位只改配色，不改造型。首次绘制等待原生材质配置，避免小尺寸入口在加载瞬间闪回旧绿色。

试作提供浅深、紧凑尺寸、长正文、知识卡／标签／进度及思考／语音四态，保留减少动态效果。验证文字 4.5:1、必要边界与状态 3:1，复跑动效、原生生命周期与焦点检查，保存截图和短动效对照。无模型、麦克风和日常数据库访问；实际进度见 `brand/refresh-2026-09/monochrome-ui/README.md`。

## 默认品牌接入与原有 UI 保留（2026-09-07，已授权）

Rex 确认将已选黑色底座／银白渐变 R 直接设为默认 Logo，来源为 `brand/refresh-2026-09/monochrome-preview/ReviewToday-Confirmed-BlackWhite.icon`。App 图标、侧栏品牌入口与 Agent 起始页使用同版图标；菜单栏保留系统适配的单色 R 模板。绿色原版及历次小样继续归档。

标题与品牌文字使用中性正文色，不显示青绿点睛；品牌青绿用于框体、焦点／选中反馈及吉祥物，错误和警告保留语义色。移除本轮玻璃界面试作对日常共享视图的挂接，保留现有实底、布局与交互。图标材质不意味着启用界面玻璃；以后只有 Rex 明确提出需求才重新接入界面玻璃。下方玻璃界面接入计划由此决定覆盖，历史制作证据保留。

## 黑白、青绿与玻璃材质小样（2026-09-07，已授权第一阶段）

黑白确认：Rex 附图确认黑色底座、银白渐变 R 的效果，并在工程中去掉底座内侧玻璃边圈；以 `monochrome-preview/user-confirmed-appearance.png` 及对应确认工程快照为准，保留 R 的原生高光与厚度。此确认不代表原生界面或日常接入已验收。

黑白补充（已授权制作）：原样归档 Rex 再次提供的绿色原版截图，并保留已采用的高清 R 母版和现有图标资产。另交付两份无点睛色的独立小样：石墨底／银白 R 的原版质感（克制渐变、轻微厚度）及玻璃质感（沿用 v3 清透窄边）；不覆盖绿色版本，不视为正式图标接入。截图只作为原版材质参考，高清版本使用现有透明 R 轮廓制作。

深色清晰度修复（已授权）：Rex 指出 v2 黑色底座发灰、发糊，本轮制作 v3 小样，收窄玻璃边缘高光、减少中央灰雾与重复阴影；两层 R 的原始轮廓、位置与哑光金属渐变保持。保留 v2 做原生导出前后对照，并复查默认、深色、单色和小尺寸；不扩大到日常 App 接入。

材质修订（Rex 已确认）：图标内部的青绿 R 改为哑光金属观感，用柔和明暗渐变与细腻倒角体现厚度；外部为包住整个图标的透明玻璃外壳，增强边缘厚度、转角高光及轻微折射，中央保持清透，避免盖住内部斜切。参考图仅借鉴材质，继续采用中性浅深底＋青绿 R。先制作 v2 图标对照小样，现有 v1 保留；原生界面玻璃仍保持克制，日常接入继续等待实际小样确认。

Rex 已逐项确认：黑白灰为主、现有深青绿 `#246C63` 点睛；保留原始 A 的 R 轮廓，图标改为中性浅深底与绿色 R。图标玻璃层次明显，原生操作层克制；正文、知识卡、文字输入底、快捷开始卡片与侧栏保持实底，主按钮保持黑白关系。错误、警告与删除保留语义色。吉祥物沿用现有造型、材质及动效，本轮仅统一品牌配色规则。

第一阶段仅交付 Icon Composer 可编辑 `.icon`、默认/深色/单色及小尺寸预览，以及基于现有内存 fixture 的原生 Agent 起始页/会话页小样。玻璃用于会话标题右侧操作组、输入区底部操作组与对应浮层，不叠加到阅读内容；新样式只在专用预览入口启用。必须验证图标导入、保存、重新打开、导出，以及原生浅深主题、宽窄布局、焦点、菜单、减少透明度和减少动态效果。最低 macOS 26.5 保持；本机证据不替代 macOS 27 验收。Rex 查看并确认实际小样后，才替换日常 App 图标与启用正式样式。当前制作状态与验收证据见 [玻璃小样记录](brand/refresh-2026-09/glass-preview/README.md)。

## 已确认动效接入 App（2026-09-07，已授权）

Rex 已接受圆润接触版本，并确认本轮只接动画，听写下一轮处理。当前 Agent 的真实 running / adjusting 状态使用已确认的 recall 环绕角色替换转圈标识；排队、未启动、失败和完成不伪装成思考。结束后短促收拢，减少动态效果保持静止；切页、后台和视图释放停止计时。语音四态封装成原生可调用组件，并在 App 内开发验证入口检验，暂不增加主输入区录音或语音聊天按钮，不把文字流式输出当作语音回答。

沿用网页已验的骨架、材质与接触逻辑，通过随 App 打包的本地渲染资源接入 SwiftUI；不依赖 Node、localhost 或远程网页，不复制重做另一套形变。四态验证明确标记模拟，不访问麦克风、不自动发消息。旧语音 POC 保留作历史。已完成真实运行绑定及离线原生组件；19 项动效回归、隔离原生运行时检查通过，Debug 签名与 Release 构建通过。原生窗口实看了环绕、语音聆听/思考切换与紧凑占位；四态推进、减少动态、停止及隐藏暂停通过运行时检查。当前仍无听写或真实语音会话；不将组件验证解释为这两项能力上线。接入与证据见 M1 验收顶部增量记录。

## 语音圆润碰撞与回弹（2026-09-07，已授权实施）

本轮替代此前统一压平底部的方案：波条与绘制共用圆头胶囊形接触范围，身体根据实际接触点局部让位、保持圆润与面积，波条受压和身体回弹双向关联。聆听保持中央，以波条托起后的轻微离面、回落表现接收，首版目标离面 3–6 px；思考保留左右跳跃，回答保留句首一次下压、提前下瞟，并在实际落地后发出主声波。停止保留速度并收敛，静音不持续假弹动。颜色、宽窄波条数、无投影、独立 recall 与原生范围保持。

复用现有 Spine 网格和共享 120 Hz 计算，不引入 Blender、物理依赖或真实语音服务。数值验收以标准尺寸轮廓穿透 ≤ 0.5 px、面积偏差 ≤ 10%、三角形不翻转为门槛；还须正常速度、半速、宽窄与四态切换实看。当前已实现：19 项自动化检查通过，含独立轮廓穿透、面积保持、网格完整、接触共同下沉、四态切换/收敛和 30/60/120 fps 一致性；浏览器检查宽窄、浅深、正常/半速、静音、停止与减少动态效果。更新 14 秒离线连播及接触序列，证据见 [动效记录](brand/refresh-2026-09/motion-rig/README.md)。柔软程度与节奏待 Rex 验收，真实语音和原生仍未验证；下方旧版压平底部的方案不再作为当前接触规则。

## 语音受力形变强化（2026-09-07，已授权）

Rex 确认强化聆听的吸收收缩、思考底部压强，以及回答主动下压发声。回答采用推荐节奏：每句开头一次明显的低幅抬起/下落，句中轻微起伏，不逐个声波重复砸落。聆听两侧向内收、顶部轻凹；思考落地压平底部接触面，脚下两三根波条下陷、两侧略隆起，并保留短暂承重；回答下落前眼睛下瞟，声波在落地之后向外传开。上下部形变及面部有先后，保留无投影和既定宽窄占位。当前用模拟语句节奏，不宣称真实语音句首检测；范围仍为制作预览，未接原生 App 或烘焙为 Spine 时间轴。已实现并通过 16 项检查，覆盖轮廓收缩、底部弧度、短暂承重、句首单次冲击、提前下瞟、落地后声波及网格不翻折。预览增加半速观察并更新四态连播，已检查浏览器宽窄布局、浅深主题、停止与减少动态效果。真实语音与原生接入仍未验证。

## 语音四态整合（2026-09-07，已授权试作）

Rex 确认去掉语音跳跃投影，明确聆听、思考、回答与停止的区别，并适度延长音浪。四态共用同一角色比例与弹性波面：聆听波动从两侧向角色汇聚，回答波动从角色向两侧传出；思考保留舒缓跳跃，不把快慢解释为真实思考速度；停止取消新动作、落稳、身体回到自然轮廓、眼神归中并收住余波。宽预览左右各增加 2 条低幅边缘波条，窄窗/紧凑预览仍为 15 条，不拉开既有间距。当前继续使用模拟声量，保留独立思考环绕，不接真实语音或原生 App。本轮实时动作仍由程序驱动，未烘焙到旧版 Spine JSON；四态已整合；14 项检查通过，覆盖声波相反传播方向、无投影、宽窄条目数、不同阶段停止与稳定收势。已检查浏览器浅深主题、宽窄布局、快速切换、静音及减少动态效果；提供 14 秒离线四态连播。当前为可运行试作，真实语音、原生效果与 Spine 编辑器导入未验证。

## 语音弹性接触试作（2026-09-07，已授权）

Rex 指出上一版角色与波浪各动各的，提供跳跃小球压低波峰的代码参考，并确认先看「思考跳跃 → 落地 → 停止」。本轮只验证这一段：角色缩为约 2–3 根波条宽，底部对准实际波面；起跳预压、空中伸展、落地局部挤压及波浪下陷/回弹共用一个接触状态，眼神预判下一落点，阴影随离波面高度变化。停止不等待循环，在当前位置短促缓降，余波消散；减少动态效果保持静止。独立思考环绕保持，聆听/回答保留旧版供对照，不宣称四态整合已完成。不接真实语音或 App。已实现接触试作：13 项自动化检查通过，覆盖局部形变、波面稳定、不同跳跃阶段停止、余波消散、重新起跳和减少动态效果；浏览器已检查宽窄布局、停止落稳与深色/减少动态效果。提供 8 秒离线视频（4.8 秒触发停止）。本次接触动画由程序控制同一 Spine 骨架与加权网格，尚未烘焙进可导出时间轴，不宣称编辑器动画交付完成。

## 语音点的骨骼动效修订（2026-09-07，已授权实施）

Rex 暂时接受环绕修订，继续优化语音中波浪上方的点。复用同一小体积深青绿角色、独立眼神、局部形变及椭圆投影，角色与既有波浪共享模拟声量；聆听克制，回答时左右错拍起伏，思考沿用已确认的环绕。切换立即反馈并平滑过渡，停止或声量为零后收敛到静止，减少动态效果时保持静态与明确状态文字。新增语音制作预览及可编辑循环，保留已接受的思考小样；不接麦克风、真实语音或日常 App。已实现：新增两条 4 秒语音骨骼/FFD 循环，同一声量驱动波浪、形变与投影；11 项自动化检查通过。465 px / 1280 px 浏览器检查了四状态、快速切换、零声量、浅深主题和减少动态效果；已通过 MCP 导出 8 秒语音视频及 JSON。真实语音、原生 App 和 Spine 编辑器导入未验证。

## 环绕深度与投影修订（2026-09-07，已授权修复）

Rex 指出球只有沿身体边缘运动的感觉，确认修复前后遮挡、投影及主体比例。主体先缩小约 14%，保留深青绿色；球调整为较浅青绿，加入克制的体积明暗。轨迹改为倾斜的扁椭圆，前半圈经过身体中下部、后半圈被身体遮挡，近大远小。身体下方保留柔和椭圆投影，球有地面移动投影及经过身前时落在身体表面的投影；投影与动作一起写入导出数据。减少动态效果时保留静态身体与底部投影。当前只修订制作小样，不更改 Logo、日常 App 或静态造型定稿。修订已实现：14 骨骼，9 项自动化检查通过；身前/身后、浅深主题、减少动态效果和中断收拢已在浏览器检查，更新了视频与导出数据。

## 可编辑形变与动画 MCP 小样（2026-09-07，已授权实施）

Rex 在 GitHub 工具调研后确认开始实现：以当前 V3 临时母形制作「眼睛先瞟动 → 身体局部弯曲 → 碎片分离并重组」的短动画，并提供 AI 可调用的制作接口。交付需包含独立眼部控制、身体网格与权重、局部变形关键帧、可中断预览和可编辑的 Spine 4.2 JSON。先验证官方运行时能读取并播放；Spine 编辑器导入需单独实机验证，未验证不能称为正式 `.spine` 工程。

研究候选 spine-motion-mcp 的形变功能缺失，CLI 往返亦未实机验证；本轮参考其接口思路，独立实现范围受限的本地 MCP，未复制代码或修改全局 MCP 配置。动画数据、预览与 MCP 共享一套制作逻辑。AI 调整权重范围、眼神、局部形变幅度和时长，程序计算并校验数据；当前没有任意控制点/拓扑编辑。保留旧预览作对照，当前不接麦克风、真实语音或日常 App；不改变 R Logo，不视为 V3 静态造型定稿。下方 09-06 的「未制作 Spine 数据」说明保留为旧预览历史。

首版实现 [可编辑形变小样](brand/refresh-2026-09/motion-rig/README.md) 与 [7 个 MCP 工具](tools/mascot-motion/README.md)：11 骨骼、193 顶点、3 区归一化权重、独立眼神/眨眼、加权局部形变及分离重组。官方 4.2.120 运行时读取、MCP 实际调用和浏览器交互已验证；有 5.6 秒离线动画及导出数据。生成体纹理未稳定提供 Alpha，当前依赖网格边界裁剪显示，不能称为透明生产拆件。Spine 编辑器导入、原生性能和 Rex 动效验收尚未完成。

## 吉祥物融入现有动效（2026-09-06，方向确认 / 预览）

Rex 明确希望把角色融进现有转圈和波浪动效，已确认先看「主体分出小块绕行再重组」及「吉祥物接替波浪上方点、弹性跟随」两个概念。优先评估即时响应和小尺寸辨识，不增加独立角色页面或新的出场场景。V3 暂作预览母形，Spine 拆件与原生接入在运动概念评估后再决定；当前 Logo 和 App 保持。范围、提示词及验证见 [动效预览](brand/refresh-2026-09/motion/README.md)。

## 原始 A 接入与吉祥物修订（2026-09-06，图标已接入 / 吉祥物提案）

Rex 已要求替换选定图标，并基于它重新设计吉祥物。默认 App 图标为深青绿底/暖白原始 A，品牌入口使用同形 R，适配浅深主题。新的独立母版不使用 V8 人物裁切；默认导出入口同步切换，旧母版保留。首轮「回页伙伴」已被否定；按 Rex 最新要求，参考 Grok Bot 的简单几何轮廓与克制眼神，制作独立的「回望伙伴」静态方案。V2 直立切角轮廓也已被否定，最新 V3 为横向卵石与克制眼神，以深青绿呼应品牌；仍待造型选择。旧语音动画 POC 不混入未选定的新角色。详细范围、提示词与验证见 [接入记录](brand/refresh-2026-09/production.md)。下方此前「未授权替换」的状态已被本次明确替换请求覆盖，实际完成情况以本轮记录为准。

## Logo 与 App 图标重新设计（2026-09-06，原始 A 已选定）

Rex 指出现有品牌过于幼态，要求更简洁、更独特，并更贴合产品。本轮以当前「理解知识、建立关联、主动回忆」的个人学习教练定位重新设计静态 logo 与 macOS App 图标。Rex 最终选回原始 A：紧凑的 R、内部斜切、暖白/石墨/青绿的视觉关系。新符号不受 V8 角色母形限制。

选定依据为 Rex 再次附图并明确「还是这个吧」，[用户选定图](brand/refresh-2026-09/accepted-a-reference.png) 是后续生产资产制作的视觉依据。原始 A 图形与配色方向锁定，折返保留为含蓄寓意，不采用 A2 箭头或 A3 平切回弯。B、A2、A3 仅作探索历史，详见 [品牌记录](brand/refresh-2026-09/README.md)。当前 Review Today 字标组合保留，不据此扩大为正式命名决定。所选图片是方向展示板；后续已完成独立透明母版、16–1024 px AppIcon 与品牌入口接入。浅深色和侧栏收放已在真实签名 App 检查；原生 Dock/Finder 外观仍待验，角色动画仍保留已有资产。

## Agent 正文混排与流程图（2026-09-06，已实施，分项验收）

- 检查题在对应回答中出现一次；后续追问不让任务区重新重复旧题，输入框只使用简短作答提示，不复印完整题目。任务控制与保存确认继续保留。
- 采用 B 的开放正文混排，按需加入 C 的流程图。保留讲解深度、默认完整展开；短追问不凑模板。开头直接回应，每段一个意思，具体小标题组织长解释，仅少量关键短语加粗。
- 比较关系用简洁表格，过程/因果用线性流程图和必要说明；例子/引用用轻量侧线，阅读目的切换处使用留白或分隔线。重要限制紧跟结论，学习路线不重复已有学习安排。
- 排列项必须有标记：并列要点使用圆点列表，顺序/步骤使用 1、2、3 的有序列表；不以纯换行代替列表。折行与该项正文对齐，普通解释段落仍自然分段。
- 沿用 RunwayPalette、820 pt 阅读边界及系统字体；段内、段间、章节间留白递增。表格窄窗按行改为带列名的纵向内容，流程图横向放不下时竖排；不缩小文字或裁掉内容。
- 检查题靠近对应回答末尾，沿用真实 required action，只出现一次；不由展示层推断或新增题目。图中节点仅表达知识关系，不暗示学习进度或掌握。
- 原文和 message 协议保持；原生识别标题、段落、列表、表格、引用、分隔线、代码，以及受限 Mermaid 线性流程。普通文本即时显示，表格逐步成形，未完成流程先显示已到达节点，闭合后只更新当前块；不支持的格式保留原文。
- 正文可选取、链接可用，提供完整回答复制。流式重排不抢焦点；阅读上文时保持锚点。浅深主题、最小/默认/宽窗、中英混排及辅助阅读顺序均需验证。结构方向已选定，精确视觉和真实模型表现以本轮验收记录为准。

> 当前容量与摘要以 Harness §24、顶部与运行入口以 Harness §22 和下方紧凑顶部契约为准；§21、§19 与未被覆盖的 §18 保留。2026-09-06 已取得本轮受控与原生真实模型发送/流式/重启证据，详情及未覆盖项见 M1 验收 §0.15；不代表完整 M1 或 Rex 体验验收通过。DeepSeek 配置不变。品牌图标已切换原始 A，旧角色/语音 POC 保留；[OneWorks 两版作品](brand/oneworks/README.md) 为未采用的探索。
> 当前实施基线：[Agent Harness V2 主规格](docs/agent-harness-v2.md)。产品、交互与品牌角色方向已由 Rex 选择；原生界面已完成工程自检，但尚未按 Harness V2 获得用户验收。静态概念图和构建成功都不作为最终体验证据。

## 会话操作与上下文圆环（2026-09-06）

- 顶部归档/恢复仅显示图标，沿用标签按钮的热区、hover 与可访问名称；悬停仍可读到动作名称。
- 输入区容量改为小圆环，随最新实际请求估算更新；占比为输入 token / 有效活动输入上限（默认 256K，较小模型扣除回答预留后下调；Harness §24）。摘要/裁剪后占用可以下降，未发送草稿不计入。
- 鼠标进入圆环立即在上方显示黑底白字、圆角的「约已用 / 窗口上限」短浮层（参考 Rex 的 AirJelly 截图）；不依赖系统 tooltip 延迟。移出即隐藏，浮层不拦截鼠标；键盘聚焦同样可预览，点击仍查看详情，详情打开时隐藏短浮层；详情说明最近一次请求估算、活动输入上限、整理触发值、模型完整窗口及回答预留。无请求显示「尚无请求」，历史记录缺有效输入上限显示「上限未知」与中性圆环，不伪造比例。退出/切会话关闭预览，不抢输入焦点；Reduce Motion 不补间。
- 关闭跨会话记忆后，会话行及标题显示脑形斜杠标识，菜单变为「允许跨会话记忆」；重新开启后标识消失。复用既有持久化、引用版本失效及保存失败反馈，不删除知识卡。
- 移除更多菜单及右键菜单中的「选择此会话」。批量操作仍从会话区「选择多个会话」、⌘ 点击和 Shift 点击进入，与当前打开的会话区分。

## 紧凑顶部与真实运行入口（2026-09-06，本轮实施契约）

本节覆盖下方旧的重复 Agent/会话标题、常驻标签行和可发送界面 fixture。独立文档检查点 `b9e0f77` 推送后实施，当前代码和验证证据见 M1 验收 §0.15。

- 会话只显示一条紧凑标题栏：左侧当前标题，右侧标签入口、归档/恢复、更多，统一按钮热区/hover/焦点；无多余三角。起始页只显示 Agent，不显示空会话标题。长标题截断并可查看全文，窄窗不挤压操作。
- 只移除系统工具栏重复标题，不隐藏关闭/最小化/缩放按钮；标题仍作为窗口身份保留。顶部标签与更多弹层优先向内容区展开，输入框菜单继续向上展开。
- 标签按需展开查看/编辑，无标签不留空行；仅有学习计划时在标题下显示可展开的一行学习安排。标题、清单、正文和输入区遵循同一阅读边界与留白。
- 默认正常使用；开发预览和真实模型隔离验收有不同窗口标识。预览允许检查输入排版，但禁止发送/创建 outbox，明确内容仅在内存，不显示「已保存在本机」或服务 ready。
- 静态样例不持续计时。动效仅由显式、有限的预览驱动，结束/切页立即停止；正常使用窗口不出现样例或假运行记录。
- 正常投递按真实状态呈现保存、待连接、正在发送、服务已接收、执行与失败；协议缺少合法 Run 标识不能无限显示准备发送。重试沿用原消息 ID。
- 交付时切回正常使用构建，不能把预览窗口留作日常 App。先保留误输入预览的「你好」为正常起始页未发送草稿，不自动发送、不覆盖其他草稿，不搬迁样例、不删除真实数据库。

## Agent 交互精修（2026-09-05 对齐，2026-09-06 实施）

- 侧栏展开时：图标与单行 Review Today 组成品牌，删除「记忆教练」；主导航对齐品牌，设置与主题切换并列固定底部。会话区跨页常驻、独立滚动。
- 收起是窄图标栏，不是完全隐藏；保留四个主入口、会话、新建、设置、主题入口。点击会话展开原侧栏并恢复列表位置，不切换右侧、不弹临时列表。窄窗自动收起不覆盖人工选择/展开宽度，显式展开后仍可查看列表。
- 会话行仅标题、最多一个简短主题标签及必要的小型运行/异常提示；普通完成不显示勾、「本轮已回应」或模式。人工标签优先，其余标签不删除。悬停/键盘聚焦显示「⋯」，操作空间固定；归档/恢复、标签、记忆策略与多选等沿用原动作。标题栏仍提供直接归档/恢复。
- 学习方式与思考强度保持独立。输入区按钮只显示图标和当前值，无多余下拉三角；一次打开直接选择，每行图标/名称/说明/选中标记，不再 Menu 套 Picker 子菜单。支持方向键、Return/Space 和 Escape；关闭不丢光标/选区，也不抢用户主动转移的焦点。
- 快捷开始默认六项：弄懂概念、读懂资料、梳理知识、探索新主题、辨析易混点、攻克面试题。每项图标/标题/用途说明；本地库补充拆解 JD、补齐前置知识、举例理解、检查理解、关联已有知识、规划学习路径。标题右侧刷新换六项，无「更多」、无组内重复或连续相同组；刷新不改草稿/偏好、不调用模型。窄窗重排，不横向溢出。
- 预填：空输入填入；只有未编辑模板时允许替换；手写/粘贴/编辑后追加末尾，不能覆盖选区。同一模板连点不重复；快速切换以最后输入为准；原生撤销、中文组合、Session 隔离不回退。无法证明来源的恢复草稿一律保护。点击/刷新均不发消息、不创建 Session。
- 共用交互状态：hover 浅底、selected 明确、pressed 即时、focus 可见。图标按钮有统一留白/热区，菜单打开保持高亮；会话和导航文字不位移。卡片仅轻微视觉缩放/边框变化，不改变占位；非交互容器不假装可点。Reduce Motion 取消缩放但保留状态；动效不拖慢逻辑或真实流式。
- 焦点隔离：会话选中、批量勾选和键盘焦点独立表达。列表容器获得焦点不能给所有行画描边；自定义焦点提示仅由控件自身的明确焦点状态驱动，不读取可能来自祖先容器的焦点作为本行状态。普通点击不进入多选；键盘聚焦未激活的行不切换右侧会话。
- 原生验证范围见验收 §0.14。界面 fixture 只证明交互，不证明 Harness 成功；本轮不新增网页组件、语音、品牌资产或模型档位。

## 导航、输入与动效补充（既有基础，受上方精修规则覆盖）

- 首次使用默认浅色，不随系统深色初始化；用户可手动切深色，之后记住选择。升级不覆盖已有人工外观偏好，浅深主题都需真实检查。
- 主导航为今天、Agent、知识库、待处理。默认打开今天；学习会话区在各页面均存在于同一侧栏下方，可折叠、独立滚动。Agent 打开新对话起始页，点击会话直接打开对应历史，不自动选择最近会话。
- 会话区标题行采用「学习会话」及搜索、归档视图切换、新建三个 icon 按钮；有可访问名称、tooltip、键盘操作和明确选中态。搜索按需展开；归档图标在进行中/已归档列表间切换，标题随之变化，不常驻分段大按钮。
- 侧栏新建与输入框「+」不是同一功能：前者进入独立起始草稿，首次发送保存成功才建立 Session，后者只添加学习材料。本轮材料限定文字、公开链接和主动选择的已有知识卡，不显示不可用的 PDF/Word/图片入口；添加不代表保存或开始完整课程。
- 多选按需出现：⌘ 点击追加、Shift 点击范围选择，另有可发现的「选择多个会话」入口；选择后显示数量、全选、归档和退出。批量归档沿用逐会话生命周期守卫，不因一个失败丢掉其他结果；只对实际成功的归档提供撤销。归档视图中的单个/批量恢复使用同一版本守卫，只恢复编辑能力，不自动生成；原生可用性尚待验证。
- 标题栏保留目标/标签和直接归档/恢复；学习清单仍在标题下。模式选择移到输入框底部，与思考强度并列，不新增常驻权限开关或右栏。
- 「学习方式」五选项：Auto、知识整理、资料学习、主题探索、问题攻克；每会话记住，新会话默认 Auto。「思考强度」两选项：智能、深入思考；每会话持续保存，新会话继承最近主动选择，不在发送或故障恢复后重置。具体模型不是用户需要选择的档位。
- 输入框旁容量使用上方上下文圆环与按需详情，不用它表示学会程度或全部历史大小；未取得有效输入上限时明确上限未知，不展示伪百分比。可用预算、预留与估算口径以 Harness §19.4 / §18.3 为准。
- 旧知识关联轻量融入正文，提供原卡片/学习记录入口；区分「之前讲过」与有范围/时间的验证证据。归档不遗忘，独立「不用于跨会话记忆」可从会话操作访问；不新增常驻记忆侧栏，不将该操作呈现为删除知识卡。

### 动效语言与真实状态

总体为「局部灵动、整体稳定」。只借鉴用户提供组件的形状和节奏，原生实现使用现有 SwiftUI/AppKit 能力，不引入 React/Framer Motion 运行依赖。

| 位置 | 标准表现 | 中断与减少动态效果 |
|---|---|---|
| 单项选择 | 选中控件由小变大、轻微回弹，不让整行跳动 | 逻辑即时生效，反选随时打断；Reduce Motion 使用直接状态/短淡入 |
| 全选 | 选中状态一次生效，仅可见行做短促错峰呈现，总时长有上限 | 不等待每行播完才可归档；快速全选/取消以最后输入为准 |
| 折叠区/详情 | 整行可点，箭头转向与内容短展开关联 | 保持编辑焦点和阅读锚点，支持键盘和减少位移表达 |
| Agent 等待 | 参考 DotsRing：八个环点依次缩放/明暗呼吸，适配行内小尺寸，配真实阶段和累计秒数 | 等用户、停止、失败、完成不继续循环；Reduce Motion 用静态符号及文字/秒数 |
| 阶段与正文 | 新阶段摘要短暂逐字出现；回答按真实流式到达，内容成为主角 | 历史不重播，停止即冻结，无完整答案后模拟打字或动画积压 |
| 后续语音处理 | 参考 WavePhysicsLoader：圆角条、小球往返、落点压缩与弹性波浪 | 固定循环只代表处理中，不能冒充真实收音；Reduce Motion 用静态状态 |
| 后续聆听/说话 | 沿用波浪视觉，但根据真实输入/输出音频响应，明确麦克风状态 | 静音、结束、权限拒绝时停止收音表达；声音不是唯一反馈 |

参考来源是 Rex 在对话中提供的两段组件代码：WavePhysicsLoader 为 15 条/4 秒往返、小球落点形变；DotsRing 为 8 点/1.5 秒缩放和透明度错峰。参数只记录参考原貌，不是已验收的原生时长。独立浏览器预览已供 Rex 查看并确认方向，未连接麦克风/模型，不证明真实语音、服务成功或原生性能。正式实施须保留代表速度、连续点击/反向操作、停止及 Reduce Motion 录屏，精确节奏再由 Rex 验收。

### 故障反馈与后续语音边界

- 主备切换是内部规则，不显示「已切换备用模型」或要求用户维护模型。内部恢复成功继续同一回答；显著延迟只给「处理时间稍长」，恢复失败再说明影响、已保留内容和有效操作。
- 默认运行详情保留真实阶段、依据摘要、耗时和可执行恢复；供应商/模型、路由切换、参数、异常类和堆栈只进脱敏开发诊断。不隐藏已发生的内容纠正，也不虚构进度。
- 听写后续采用保守清理、可编辑、可撤销、手动发送的草稿；不设计标错入口、置信度下划线或强制复核。不能借清理修改否定、数字、专业含义或练习答案。
- 语音聊天后续沿用同一 Session 和学习清单，只把底部输入区切为静音/结束等语音控制；自然轮替与打断，不另开沉浸式人物页面，结束语音不取消学习目标或覆盖原草稿。
- 听写与语音聊天的模型未锁定为 GPT Realtime；二者不是同一功能。M1 仍不启用语音入口或录音；以下历史正式语音复习及角色动画方向保留，不据此接入当前学习页。

## 学习手册修订（基础规则，受以上补充覆盖）

- 保留单侧栏/开放居中回答/原生输入。标题下固定学习清单，默认一行目标与当前步骤，展开偏好按会话保存；长列表内部滚动，普通问答/寒暄不显示。
- 点击步骤定位已有讲解，未开始的步骤显示标题与状态但不执行教学。已讲解、跳过、自述、验证分别用文字呈现，不手动勾选，不用百分比暗示掌握。当前没有内容的步骤不可点击，补学安排通过对话提出。
- 回答以标题/正文/例子/旁注/来源组织，不强套大卡片；未闭合 Markdown 安全显示，最多一个主操作，自由输入一直可用。完成小结来自真实进度/评价：达成、待练习、入库状态；不额外模型总结或夸奖，不弹窗/强制保存。
- 连接提示只说明影响/保留内容/操作，区分重试连接与重试回复；异常类/模型清单/命令/HTML 只在开发详情。保存失败不得声称已保留。
- Today 优先复习/准确续学/成果，热力图辅助，三条收起/展开不变；收轻空态、重复说明和空标签。高级排队选项按运行场景出现。
- 窗口以当前工程最小 760×620 pt 为回归下限（取代下文旧 980×680），同时测试默认/宽窗。清单展开、计时和流式不得抢焦点/滚动；浅深主题、键盘、中文输入法和 Reduce Motion 原生验证。
- 用隔离 RAG 数据展示空态/讲解/练习/完成；只证明界面，不证明真实 Harness。新文案使用既有本地化机制。
- 用户明确授权 OneWorks 网页制作抽象学习伙伴候选（纸页/记忆）；下文 V8 位图-only/旧母形限制仅适用于 V8。奶白/石墨/青绿协调配色，不增加动态吉祥物；Rex 接受以前不替换正式资产。来源/参数/原始导出见 brand/README.md。

### Agent 起始页与会话切换（§19）

- 起始页：轻量 Agent 标题、Review Today 品牌、居中输入框、下方六个可刷新快捷开始。正式会话：会话标题/标签/归档、真实任务清单、唯一主滚动消息区、底部输入；两个状态复用同一 NSTextView。
- 快捷开始按上方模板保护规则预填并可撤销；不请求模型、不自动发送、不切换两个选择。
- Agent 与侧栏 + 复用独立起始草稿；切页、重复点击和重启不清空。首次发送原子保存 Session/消息/偏好/outbox，成功才进入列表；失败保持原文和焦点。
- 主导航选中与具体会话选中表达当前右侧位置；从今天/待处理打开对象使用准确 ID。切页不停止生成或丢弃各自草稿。
- 新起始草稿默认 Auto/继承最近主动思考强度，首发继承用户当前选择。个性化继续学习入口留在今天。
- 默认/最小/宽窗验证起始→发送→历史→切页→返回、草稿与选择恢复、可撤销插入及布局不跳失焦。截图只能证明静态结构，原生录屏和测试才证明行为。

## 1. 设计目标

Review Today 的主窗口同时承担两个清楚但不混杂的任务：Today 是学习与复习数据看板；Agent 工作区包含新对话起始页及具备完整上下文、即时反馈和恢复能力的学习 Session。

```text
在学习 Session 中提出目标
→ 立即知道 Agent 已接收并正在做什么
→ 通过整理、教学、探索或问题攻克完成当前目标
→ 用户确认后形成记忆
→ Today 查看并开始到期复习
```

用户每次打开 App 都应该立即理解：今天是否需要复习、最近学习进展如何、从哪里继续上一段学习。进入 Session 后，任何输入都必须立即得到可理解的反馈。

## 2. 证据与设计判断

| 类型 | 当前依据 | 支持的判断 | 不证明什么 |
|---|---|---|---|
| 用户确认 | Rex 选择“今日记忆跑道”与“安静的学习编辑台” | 首页重心和产品性格 | 最终画面已经好看或好用 |
| 项目事实 | 当前已有 SwiftUI 主窗口、学习工作区和 Harness V2，目标为 macOS 26.5 | 在现有组件与配色上修复窗口适配 | 构建成功代表界面已经好用 |
| 本机产品观察 | Typeless 的首页、历史与低摩擦语音入口 | 快速采集应尽量用完即走 | Typeless 的视觉适合直接复制 |
| 用户提供的本机截图 | AirJelly 的单侧栏、会话列表与开放式 Agent 内容区 | 主导航、会话导航和当前工作可以在一个稳定窗口结构中共存 | AirJelly 的品牌、人物、积分、升级、工具入口或具体交互已适合直接复制 |
| 设计判断 | 复习不是考试，用户有自己的回答节奏 | 避免硬截断、红分和连续打卡压力 | 五分钟体验已经经过真实用户验证 |
| 用户确认 | 角色采用稳定小人母形与发光 living-line 动态语言 | 可以进入角色、App 图标与 Logo 的同源视觉探索 | 第一版生成图已经达到最终品质或可直接用于 Spine |

Typeless 和 AirJelly 只作为体验证据。Review Today 不复制其品牌、图标、文案、页面或持续监听机制。AirJelly 本轮参考边界见 [`docs/design-references/airjelly-agent-structure.md`](docs/design-references/airjelly-agent-structure.md)。

## 3. 产品原则

- **主动但不催促**：系统负责提醒和选题，但允许稍后、跳过、暂停和自然完成当前回答。
- **让过程可见但不暴露思维链**：任务卡片说明当前阶段和下一步；展开层显示节点、路由依据摘要、耗时与恢复，隐藏模型思维链。
- **帮助回忆而不是考试**：不给回答设置硬性一分钟截止，不用分数制造压力。
- **建立信任而不是制造黑盒**：来源、核验、转写、判断和下一次日期都可以查看与纠正。
- **一次只做一件事**：一个 Learning Task 聚焦一个学习目标；正式复习一次只呈现一道题。
- **音频不是唯一通道**：问题、状态、提示和反馈始终有文字等价表达。
- **角色服务于复习关系**：角色让“温柔纠正、随时回应、主动提醒”可感知，但不能挤占题目、来源和反馈，也不能让用户承担维护虚拟关系的压力。

## 4. macOS 信息架构

主窗口使用 `NavigationSplitView`，侧边栏固定四个主入口及可折叠的学习会话区。设置使用系统 Settings 窗口。正式复习和“立即试一题”共用独立复习窗口。全局 Agent 运行记录仍是开发入口，不是正式侧边栏项。

```text
主窗口                         设置                 菜单栏（后续）
┌────────┬──────────────┐     一般 / 开发          最近状态
│ 今天   │ 数据看板      │
│ Agent  │ 起始 / 会话   │
│ 知识库 │ 列表 / 详情   │     试一题 或 今日复习（同一窗口）
│ 待处理 │ 队列          │
│ 会话区 │ Session / 对话│     复习窗口
└────────┴──────────────┘
```

### 4.1 主窗口侧边栏

Harness V2 固定四个主入口，具体会话也可从同一侧栏直接打开：

1. **今天**：今日复习、学习活动、待处理和最近成果的数据看板；不提供输入。
2. **Agent**：新对话起始页、独立草稿和快捷开始，不自动创建空会话。
3. **知识库**：按主题浏览、按掌握状态筛选、查看详情并纠错。结构见 4.6。
4. **待处理**：冲突、证据不足和校验失败的统一队列。结构见 4.7。待处理数量显示在侧边栏该项上。

学习会话区在各主页面均可访问；点击行准确打开历史和草稿，侧栏「+」进入起始页。首次发送才建立会话，Agent 入口不自动续接上次会话。

Agent 运行记录不作为正式产品入口。开发模式开启后，从 Settings 的开发区或窗口菜单打开。

### 4.1.1 学习工作区

学习工作区采用“单一主侧栏 + 开放式 Agent 工作区”。Session 是上下文容器，不是一次提交记录；学习页内部不再创建第二条 Session 侧栏或窄窗弹层。

```text
主侧栏                         Agent 工作区
─────────────────────────────────────────────────
Review Today                   标题 · 标签 · [归档]
今天                           用户与教练的完整对话
Agent                          起始页 / 新对话
知识库                         行内运行阶段 / 选择 / 错误
待处理                         输入文字或粘贴公开链接……
学习会话 [搜索] [归档视图] [+]   [+材料] [学习方式 ▾] [思考强度 ▾]
  RAG 入门
  面试准备
设置
```

规则：

- 模式选择仅有 Auto、知识整理、资料学习、主题探索、问题攻克；
- 选择模式作用于当前目标及后续任务；Auto 保持 Auto，内部同目标自动转接并说明。非 Auto 的未授权方式变化只建议；明确选择或指令不重复确认；
- 用户消息先独立写入并立即出现，不强制显示学习任务卡；服务尚未就绪时显示“正在启动学习服务”；
- 普通对话用轻量运行行显示已保存、识别中、已收到补充、正在调整、排队中、已停止及失败；展开查看真实阶段、依据摘要、耗时与恢复，模型/异常/内部切换只在开发诊断，不显示隐藏思维链。
- 默认发送及时补充当前执行；提供明确的排队发送与停止回复操作。停止保留目标与待办，不自动消费队列；恢复与取消目标分开。
- 知识整理结果先作为会话内草稿展示；理解条件与明确同意满足后再加入知识库和复习。理解检查可跳过，未验证状态可见。
- Task 卡片显示当前工作阶段、等待动作和下一步，可展开“运行详情”；
- “运行详情”包含节点、路由依据摘要、耗时、重试、错误和恢复，不展示隐藏思维链；
- Task 完成后 Session 仍可继续；归档可恢复，不删除对话与知识；
- Session 标题栏永久显示归档／恢复；会话行悬停/聚焦显示更多菜单，右键提供同一动作。归档不确认，运行中的归档先停止当前回复、冻结可见部分并保留历史，再显示可撤销反馈；
- Session 行仅显示标题、最多一个主题标签及必要运行/异常提示，不展示完成勾和模式。自动标签由意图结果建议，人工覆盖并可恢复自动；关联知识标签只读；
- 高置信无关主题只显示“建议新建 Session”，不得自动移动用户输入；
- 用户确认新建后只传递结构化交接摘要，并保留返回来源 Session 的入口；
- M1 输入只支持文字和公开链接，不显示不可用的语音入口。

#### 窗口与内容适配（2026-09-04，替代 2026-09-03 双栏方案）

本轮把旧的“主侧栏 + 学习页 Session 栏”压缩为一个标准主侧栏，并将右侧改为开放式 Agent 工作区；不改变 Harness、五模式、受控入库或品牌方向。旧的宽屏 240 pt Session 栏和窄屏 Session 弹层仅保留为历史实现证据，不再是当前设计依据。

- 主窗口最小内容尺寸为 760 × 620 pt，默认 1280 × 820 pt。根 `NavigationSplitView` 提供唯一侧栏；侧栏可以显示、隐藏和拖动调整宽度，状态跨重启保存。
- 侧栏顶部品牌和四个主入口固定；其下学习会话区跨页面保留，可折叠。搜索、新建和归档视图切换使用同一标题行的 icon 按钮，会话列表独立滚动；Settings 固定在底部。
- 窄窗口收为图标栏，点击会话/展开入口恢复原侧栏；缩放不切换 Session、不清空草稿，不使用临时 Session 弹层。
- 标题最多两行，完整标题可悬停查看；Session 操作有独立空间，模式在输入区。消息、任务、运行详情必须在当前视口内换行，不允许依靠横向裁切掩盖溢出。
- 对话与输入框共享居中的阅读边界，最大正文宽度 820 pt；两侧留白随可用空间缩减。用户消息靠右、教练消息靠左，不能使用不可压缩的空白挤占正文。
- Agent 回答直接位于内容画布，不使用包裹整段内容的大卡片或重复头像；用户消息使用轻量右对齐容器。运行状态、工具结果、确认动作与错误插入对应回答位置。
- 任务摘要与模式／恢复操作分层；主视图只显示可理解原因和恢复动作，底层命令与堆栈只在技术详情中出现。长状态、来源、错误码和中英混排可以纵向增长；重试、取消、模式、归档和发送始终可达。
- 输入框规则已由下节 2026-09-04 修订替代：文字区域随内容在 64–160 pt 内增长，再内部滚动；不再固定 72 pt。
- 侧栏与内容区沿用 `RunwayPalette`、现有字体和语义组件；不复制 AirJelly 的浅色视觉、人物、积分、升级、文件夹或工具入口。Reduce Motion 下侧栏和状态立即切换，信息保持完整。

原生验证门槛：最小、默认和扩大窗口；侧栏显示／隐藏／拖宽及会话区独立滚动；准确 Session 跳转；缩放后输入保持；长中文／英文链接；运行详情展开；输入框滚动；浅色／深色。精确尺寸是实施起点，最终舒适度仍由 Rex 验收；不把静态图或构建视为体验通过。

#### 输入、阅读与实时反馈（2026-09-04，当前有效）

- 学习输入封装原生 `NSTextView`，正文、占位提示与中文预输入使用同一字体、段落样式和文本起点；光标使用随主题变化的正文色，焦点描边清楚可见。
- Return 发送、Shift Return 换行，保留 ⌘ Return；输入法选词期间 Return 只确认预输入，空白不可发送。保留原生选择、粘贴、撤销。发送按钮在独立底部行，不覆盖文字。
- 成功保存后清空并保持焦点，保存失败保留输入。未发送草稿按 Session 本地保存，切换恢复各自草稿，新会话为空；草稿不自动发送给模型。
- 仅移除对话内重复的角色头像，正文统一左对齐；保留侧栏品牌、App 图标和既有配色。无新 UI 组件库。
- 新旧“运行详情”都使用整行可点击、可键盘操作的展开按钮。完成后的普通运行收敛为一行“已完成 · 用时”及详情入口，学习目标仍保留必要状态。
- 运行时采用上述环点动效、真实阶段和累计秒数；同次执行换阶段或内部恢复不重置计时，停止冻结，用户重试另计并保留历史。排队、服务等待和失败不冒充思考。
- “我想一下”“稍等”等暂缓表达使用一条自然确认收口，不出现无意义的 Terra 生成、失败卡或重试按钮；当前目标、待办和理解状态保持原样。
- 后台模型能力检查不属于当前对话时间线。服务及消息协议已经可用时不显示全宽“正在验证模型”横幅；流式或部分角色受限只显示紧凑能力提示，完整角色诊断留在开发入口。只有服务不可达、协议不兼容或全部模型能力不可用时使用阻塞性横幅。
- 新阶段摘要短暂逐字呈现，正文仅显示真实到达的增量，不等全文后模拟打字，额外显示缓冲不超过 100 ms。历史消息不重播。Reduce Motion 取消脉冲及打字动画，保留实时内容和计时。
- 用户在底部时跟随新内容；阅读上文时保持位置并提供“回到最新”。主动发送定位新用户消息。展开详情、计时和增量刷新不抢编辑焦点。
- 部分回答标记“生成中／未完成”，不是已验证知识或可入库版本；中断后保留可见部分，旧执行不得继续追加。
- 历史参考：[60fps Monogram 状态到回答过渡](https://60fps.design/shots/monogram-pulse-sweep-chat-response-reveal-interaction)，仅借鉴轻量过渡，不采用玻璃或浮动面板。旧「不采用波形」限制已由上方用户指定的语音波浪方向替代；不复制资产或把 Web 源码直接接入原生 App。

#### Today 活动与最近学习（2026-09-04）

Today 继续是纯数据看板。在状态卡下增加最近 26 周学习热力图，并保留可展开的“最近学习”：

- 活跃只统计已完成的知识回答、资料学习步骤和正式复习；问候、输入、打开页面、失败、预览练习与未完成生成不计入；
- 热力强度固定为 0、1、2、3–4、5 次及以上；顶部显示活跃天数、当前连续天数和最长连续天数，文案保持中性，不使用火焰、断签提醒或庆祝；
- 时间戳保存为绝对时间，显示和按日聚合使用用户当前系统日历与时区；同一 Run／ReviewAttempt 只计一次；
- 悬停格子显示日期、总数和类型分布；点击有活动日期展开当日明细，明细可进入准确 Session 或知识条目；
- “最近学习”默认显示最近三个进行中 Session；页面内展开后在固定高度区域滚动全部进行中 Session，收起恢复三条。点击行必须打开对应 Session，不得只切换到学习首页；
- 每个最近学习行显示标题、最多两个主题标签、最新真实状态和模式。Today 不提供输入、模式切换或知识提交。

### 4.2 菜单栏

菜单栏是 Harness V2 之后的低摩擦入口，不是当前 M1 的必验收表面。恢复实施时，它只承担快捷创建或继续 Session，而不是缩小版主窗口：

- 快速输入文字；
- 粘贴公开链接；
- 开始和结束语音随记；
- 显示最近一次任务的用户状态；没有最近任务时显示“还没有采集”，并提供打开主窗口；
- 打开主窗口查看完整结果。

菜单栏不展示知识库、完整运行轨迹或正式复习题目。首版不做全局快捷键。

### 4.3 独立复习窗口

从首页“现在开始”、通知或回执上的“立即试一题”进入独立专注窗口：

- 与主窗口共享本地数据，但拥有独立窗口生命周期；
- 主窗口保留用户原来的浏览位置，不被复习界面替换；
- 一次只显示一道题及其当前状态；
- 关闭窗口触发“暂停并可恢复”，不丢失已完成题目；
- 当前未完成题恢复后重新提问。

**立即试一题**与**正式复习**共用同一个单栏 `WindowGroup`，不各做一套界面。用窗口标题区分：

- 正式复习：`今日复习`；
- 预览：`试一题 · 不计入排期`。

预览会话标记为 `preview`：不显示 FSRS、下次日期或正式完成统计；交互状态机与正式复习相同。窗口没有侧边栏。

### 4.4 屏幕清单

以下表面属于 Demo 界面范围。导航结构以上方四入口及会话区为准，不把内容工作区都做成导航按钮或把设置塞进主窗口。

| 表面 | 何时必须有可点击骨架 | 本期写清的内容 |
|---|---|---|
| 主窗口 + 今天 | Harness V2 UI | 复习与学习数据看板，不含输入 |
| 学习工作区 | Harness V2 UI | Session 列表、完整对话、模式、任务反馈与恢复 |
| 知识库 | 切片 1 回执展开后要能打开一条知识 | 主题列表、知识列表、详情与编辑入口 |
| 待处理 | 切片 2 核验冲突 | 单一队列、优先级、处理动作 |
| 设置 | 已有入口并随 Harness 补诊断 | 独立 Settings scene，不是主导航页 |
| 菜单栏 | Harness V2 之后 | 快捷创建或继续 Session、无最近任务时的空状态 |
| 独立复习窗口 | 切片 4 preview / 切片 5 正式 | 共用窗口、标题区分、预览不显示排期 |
| Agent 运行记录 | 切片 1 开发模式 | 入口在设置 / 窗口菜单 |

当前实施顺序以 `docs/agent-harness-v2.md` 为准。文字、公开链接、Session 恢复和四模式是本轮必验收；语音、菜单栏采集、Realtime 和通知不属于本轮 M1。

### 4.5 窗口角色

主窗口尺寸与学习区重排遵循 4.1.1；其他窗口保留各自行为：

- **主窗口**：`NavigationSplitView`；侧边栏四个主入口及会话区；内容区随主入口或具体 Session 切换。
- **复习窗口**：独立 `WindowGroup`，单栏、无侧边栏；保持一道题可读。最小尺寸跟随系统默认，不在文档中写死。
- **设置**：系统 Settings 窗口；与主窗口并列，不替换今天页。
- **菜单栏**：后续 `MenuBarExtra` 弹层，只创建／继续 Session 和显示最近任务状态。
- **Agent 运行记录**：开发模式开启后的独立页面或窗口；普通产品导航中不可见。

### 4.6 知识库

知识库只服务于查看、纠错和建立信任，不是笔记库、文件夹或知识图谱。使用两栏：左侧主题，右侧该主题下的知识列表；选中一条后在右侧详情（或第三栏）阅读，不进入自由编辑画布。

```text
知识库
────────────────────────────────
主题            知识点
全部            学习目标标题
  认知科学        主题 · 掌握 · 下次复习
  系统架构        …
  …             

选中后的详情
学习目标
来源与证据
当前问题（主问题，最多两个变体）
掌握与下次复习
历史判断
[暂停]  [软删除]  [永久删除]
```

规则：

- 默认主题为“全部”；每条知识只有一个主要主题，不做自由标签。
- 可按掌握状态筛选：Again、Hard、Good、Easy、已暂停。筛选不能丢掉到期或待处理线索，只改变当前列表。
- 详情固定上述块，顺序不变。从今天的回执展开知识点时，打开知识库并选中对应条目。
- 修改措辞、主题或语言在详情内完成；改变核心含义时按产品规则生成新版本。
- 永久删除必须明确确认；暂停和软删除可从详情恢复。
- 不提供库内搜索聊天、图谱或与当前知识无关的推荐。

### 4.7 待处理

所有需要人工介入的结果进入同一队列。侧边栏“待处理”显示未处理数量。处理完的条目从列表消失，不另做完成页。

```text
待处理  3
────────────────────────────────
冲突 · 学习目标摘要
核验发现来源与公开资料不一致
[采用限定版本]  [改为来源观点]  [继续暂停]  [删除]

校验失败 · 学习目标摘要
整理结果无法通过检查
[查看原文]  [重新整理]  [删除]

转写异常 · 语音随记
转写不完整或不可靠
[确认转写]  [重新录制]  [删除]

缺少来源 · 想学 RAG
还没有外部学习材料；可以补充资料，或明确使用 Agent 生成讲义。
[贴上一段]  [给公开链接]  [帮我找来源]  [直接教我]
```

优先级自上而下：冲突或证据不足 → 缺少来源或意图过宽 → 结构 / 语义校验失败 → 转写异常。同一优先级内按进入队列时间排列。

动作与产品规则一致：

- 冲突项不能被标成“已确认事实”后直接进入复习；
- 继续暂停的知识留在待处理，直到用户选择其他动作或删除；
- 删除共享来源时，若仍被其他知识引用则只移除本条知识。

### 4.8 设置

使用 macOS 标准 Settings 窗口，不增加侧边栏第四项。

```text
设置
────────────────────────────────
一般
  每日提醒时间          21:00
  复习语言              用中文复习 / 用目标语言复习

开发
  开发模式              开
  将知识标为到期        仅开发模式
  Agent 运行记录        打开 →
  吉祥物动画 POC        打开 →（仅 Debug）
```

- 每日提醒时间驱动本地通知；无候选日仍不发通知。
- 复习语言是对提问语言的用户覆盖，不改变知识内容语言。
- 开发模式默认关闭。打开后，Settings 开发区和窗口菜单出现 Agent 运行记录入口，并允许把知识标为到期或跳过两小时等待。正式模式与 POC 口径忽略该覆盖。
- Debug 构建在同一开发区提供隔离的吉祥物动画 POC 入口，用于验证状态、配件、打断和 Reduce Motion；Release 构建不显示该入口。
- 学习服务与模型能力诊断不放在独立引导页；当前任务显示用户可操作状态，Settings 开发区提供详细诊断。麦克风和通知权限在对应长期功能恢复实施时再进入产品状态。

### 4.9 空状态与失败状态

实现时使用下表文案方向，不另发明互相矛盾的空白页。

| 表面 | 条件 | 用户看到什么 |
|---|---|---|
| 今天 | 当天没有正式复习候选 | “今天无需复习”，不是空白或失败 |
| 学习 | 尚无 Session | 说明四种学习方式并提供“新建学习”，不伪造示例对话 |
| 学习 | Python 服务正在启动 | 用户消息已保存；任务卡显示“正在启动学习服务” |
| 学习 | Python 服务启动失败 | 原始消息保留；显示可理解原因、重试和运行详情入口 |
| 学习 | 等待用户 | 明确显示需要选择、确认或回答的内容，不继续播放处理中动画 |
| 学习 | Task 可重试失败 | 保留当前上下文，提供“重试”与“取消任务”；不重复用户消息 |
| 学习 | 高置信新主题 | 显示“建议新建 Session”及原因，保留“继续当前 Session” |
| 今天 | 麦克风未允许 | 状态行提示可用文字采集和文字回答；不阻断打开 App |
| 今天 | 通知未允许 | 状态行提示仍可从 App 内开始复习 |
| 今天 | 尚无任何知识 | 学习动态显示尚无成果并提供“前往学习”；复习区使用“今天无需复习” |
| 知识库 | 还没有知识点 | “还没有知识点。从侧栏新建一次学习。” |
| 知识库 | 当前筛选无结果 | “没有符合当前筛选的知识”，保留筛选控件 |
| 待处理 | 队列为空 | “没有需要处理的内容。” |
| 菜单栏 | 没有最近任务 | “还没有学习记录”，以及打开学习工作区 |
| 复习窗口 | 预览会话 | 标题含“不计入排期”；不显示 FSRS 或下次日期 |
| 复习窗口 | 恢复失败或会话已失效 | 说明无法继续本题，返回主窗口；已完成题仍保留 |
| 复习窗口 | 评分或本地写入失败 | “本题尚未计入复习”，停留当前结果，不进入下一题 |

## 5. Today 学习与复习看板

Today 不承担输入和对话。建议的信息顺序如下，不在文字规范阶段定义未经渲染验证的精确像素：

```text
今天
────────────────────────
今晚 21:00 复习
7 个知识点 · 预计约 5 分钟
[现在开始]

学习动态
活跃 Session 2   正在进行 1   需要处理 1
[继续学习]

今天新增
“我理解为……”
来源 · 主题 · 生成 3 个知识点
[立即试一题]

今日复习结果
完成后展示掌握结果与下一次安排
```

首次启动不做独立引导页。Today 只展示数据；服务状态与当前 Run 错误放在学习工作区，完整能力探测放在 Settings 开发诊断中；OpenAI Key 只显示可用／不可用，永不显示真实值。不含真实学习内容的模型链路测试不进入普通对话时间线。

### 状态规则

- 没有复习候选时，复习块显示“今天无需复习”，而不是空白或失败。
- Python 服务不可用时，学习工作区先保存消息并显示启动、恢复或可操作错误；Today 只投影真实任务统计。
- 麦克风或通知未允许时，状态行用文字说明替代路径，不弹出无法关闭的向导。
- 工作过程使用用户语言：正在理解目标、正在检查资料、正在整理答案、等你确认来源、等你回答、正在形成记忆、需要处理。
- 每个 Task 的展开层可以显示节点、依据摘要、耗时和重试；跨任务技术记录仍只在开发页。
- 今天新增默认展示简短回执，详细知识点按需展开并跳到知识库对应条目。
- 回执上的“立即试一题”打开预览复习窗口，不更新 FSRS。
- 主题愿望确认来源后的回执写“根据你确认的来源……”，而不是把模型常识说成原文。
- Today 的复习块只问今天复习什么；新的学习目标从“学习”工作区创建。

## 6. 正式复习交互契约

### 6.1 入口和完成

- 正式入口：首页按钮、本地通知或当天仍有候选时的手动开始；窗口标题为“今日复习”。
- 预览入口：整理回执上的“立即试一题”；窗口标题为“试一题 · 不计入排期”。预览不受两小时等待限制，不写入 FSRS 或正式完成统计。
- 两种入口共用第 6.2 节状态机和同一窗口；预览不展示下次复习日期。
- 完成：五分钟窗口结束且当前题自然收尾，或候选用尽后显示会后总结。预览没有五分钟队列，答完当前题后结束。
- 暂停：关闭窗口或选择暂停，已完成题立即保存，未完成题不产生评分。

### 6.2 一题状态

```text
准备问题
→ Agent 提问
→ 聆听用户
→ 用户确认答完
→ 展示转写
→ 评分或免费重录
→ 简短反馈
→ 用户可改判
→ 本地保存
→ 下一题
```

- 使用 VAD 自动识别说话边界，同时保留“我答完了”按钮和口令。
- 用户可以打断 Agent 播报。
- 回答时不滚动展示全文转写，以免干扰回忆；答完后再显示完整转写。
- “转写不对”是技术纠错，不是知识答错，可免费重录且不占一次提示。
- 单题约一分钟时只播放一次轻提示音并显示文字说明，用户可以继续、请求提示或跳过。
- 提示音不能是唯一反馈；静音、听力受限或未听见时仍能理解状态。
- Agent 最多给一次学习提示；提示后重答即使正确，最高自动等级为 Hard。

### 6.3 判断与纠正

- 先显示 Again、Hard 或 Good 和一句可理解依据。
- Easy 只在用户认为明显过于简单时主动选择。
- 用户可以改为任意等级；界面应同时保留“Agent 原判断”和“最终采用”。
- 会后总结允许进一步修改知识、问题或评分规格，避免同类误判持续发生。

### 6.4 故障与降级

- Realtime 中断时提示正在切换，先用 macOS 系统语音朗读，再提供纯文字路径。
- 评分失败时明确说明“本题尚未计入复习”，允许重试，不能显示虚假掌握结果。
- 本地写入失败时停留在当前题结果页，不进入下一题。
- 网络恢复、App 重启和窗口恢复后，最终状态必须与用户最后一次有效操作一致。

## 7. Agent 运行记录

开发模式中的独立页面用于理解和排查 Agent 循环，而不是展示模型思维链。入口在 Settings 开发区或窗口菜单；关闭开发模式后入口隐藏，不出现在侧边栏。

每次任务展示：

- 触发来源和任务 ID；
- 实际经过的节点；
- 触发联网核验或待处理的结构化原因；
- 每步耗时、重试和最终状态；
- 经过脱敏和裁剪的输入输出摘要；
- 失败位置与恢复动作。

不展示：

- 模型隐藏思维链；
- OpenAI Key、Authorization 头或短时凭证；
- 与排查无关的完整知识正文或音频；
- 可以绕过产品规则的自由工具调用入口。

## 8. 视觉性格：安静的学习编辑台

### 8.1 共同特征

- 首用默认浅色，手动选择后持久化；浅深两套外观均需完整原生验证，不把深色当作附属主题。
- 依靠清晰排版、稳定留白和状态层级建立秩序。
- 使用克制的蓝绿色语义角色表达进行中与已掌握，但任何状态都同时配文字、形状或图标。
- 允许一个克制的品牌角色参与语音状态与关键反馈；不引入火焰连续打卡、排名、考试红分或夸张庆祝。
- 不用大面积玻璃、动态渐变或复杂粒子掩盖任务状态。
- Agent 是具备老师能力的女性 AI 搭档，而不是真人教师或需要用户维护关系的虚拟宠物。

### 8.2 语义角色

最终值需要在渲染稿中验证，当前只定义用途：

- `contentPrimary`：问题、知识点和主要结论；
- `contentSecondary`：来源、说明、时间和辅助信息；
- `contentCritical`：冲突、失败和永久删除；
- `surfaceBase`：主要阅读与复习背景；
- `surfaceRaised`：回执、任务和反馈卡片；
- `surfaceSelection`：当前主题、当前题或选中知识；
- `actionPrimary`：现在开始、提交回答和保存；
- `focusRing`：键盘焦点；
- `motionFeedback`：轻量状态确认；
- `motionReduced`：减少空间移动后的等价状态反馈。

### 8.3 品牌角色母形

- 角色关系：贴心的复习老师。她理解用户提供的内容，随时回应，温柔提醒今日问题；答错时纠正但不指责。
- 外形以中性抽象角色为主，通过女声、措辞、停顿和细腻动作表达女性人格，并使用短偏分刘海与一根小呆毛提供轻微女性特征；不依赖长发、睫毛、裙子或蝴蝶结。
- 静止母形采用稳定、略带手绘感的小人：大而简洁但不完全对称的头、小型悬浮身体、无明确双腿、点状眼睛。
- 头和身体轻微错位，整体形成柔和 S 形或逗号形重心；通过前倾、收腹、弯曲和歪头产生俏皮感，不再正圆、直立和左右对称。
- 默认气质是“平时呆萌、回应时机灵”：倾听时略带困惑但保持醒着，理解、鼓励或纠正时通过姿态与眼嘴变化突然显出反应，不长期挂标准微笑。
- 嘴是短小、不规则的手绘表情。V12.1 的闭口与句末微笑使用完整封闭、两端自然变尖的石墨月牙，避免开放线段端点像被裁断；张嘴状态必须是完整封闭且上下均有弧度的自然小口，不使用统一平顶、贴边裁切或横向切线；不发光、不设计成胶囊、屏幕、硬件灯或固定语音标志。
- 语音与 AI 感由轮廓脉动、身体伸缩、重心变化和沿线游走的光表达，不再由嘴单独承担。
- 静态辨识度必须来自角色本身的固定头身轮廓、五官关系或姿态，不依赖动作残影、外部光环、悬浮道具或临时特效。
- 角色在语音会话中使用正常、轻量的双耳头戴耳机：纤细完整头梁、左右两个小型耳罩，不添加麦克风杆、游戏耳、灯带或夸张硬件结构。耳机不是待机时的常驻制服。
- 头部固定保留两三笔短偏分刘海和一根偏离头顶中心的逗号形小呆毛。刘海不覆盖眼睛，呆毛不表现成长卷、弹簧或天线；耳机头梁从刘海与呆毛后方经过，佩戴时仍能识别两者。
- 初轮先用单色动画草图验证母形和动作；蓝绿色、深色外观与材质版本在角色被接受后再验证，不能机械反色。
- 不复制参考角色的具体轮廓、五官排布或任意变形方式。

### 8.4 动态与动画运行时边界

- 角色采用固定拓扑：头、悬浮身体、两条线性手臂、眼睛和声音口可以稳定拆分，避免依赖每帧重画的任意变形。
- 空闲时轻微呼吸与悬浮；聆听时前倾和收腹；思考时轮廓或内部线条缓慢游走；说话时以轮廓、身体和重心的弹性响应为主，嘴只做自然表情变化。
- “回声线”只在说话、思考或理解发生时短暂离开主体轮廓，作为声音和记忆延迟的动作表现；静止角色不常驻第二轮廓，避免被误解为头发、光环或草稿残线。
- 待机时角色完全不显示耳机；用户明确发起语音会话后，正常双耳耳机直接出现并戴好，聆听、思考和回应期间保持佩戴，会话结束或取消后消失。耳机状态表达“进入语音会话”，不能暗示 App 在待机时持续监听。
- 耳机不得成为录音或隐私状态的唯一反馈。界面仍需用文字、系统权限状态与可感知的录音控制明确说明“正在聆听”或“未在聆听”。
- Reduce Motion 下直接切换待机与佩戴状态，不播放耳机飞入、展开或收纳动画，同时保留文字和控制状态。
- 小呆毛只承担非常轻微的状态反馈：待机自然微弯，进入会话稍微立起，思考时轻偏一侧，回应时小幅回弹。它不能持续大幅摆动或成为唯一状态反馈；Reduce Motion 下保持固定。
- 纠正答案时使用轻微歪头与开放手势，不摇头、叉腰、敲黑板或制造惩罚感；答对时也不做大幅庆祝。
- 动作必须服从内容节奏，不能延迟开始答题、下一题或重复操作。
- Reduce Motion 下取消持续悬浮和大范围伸展，保留静态姿态、声音口亮度与文字状态作为等价反馈。
- 正式购买 Rive 或 Spine 前，先用 GPT Image 生成的完整透明角色状态图和 Apple SpriteKit 制作隔离的 macOS 原生动画 POC。代码驱动完整角色位图与 GPT Image 生成的局部嘴型／闭眼覆盖层，不使用 SVG、Bezier 或 Canvas 重画角色；不得拆分会改变连接关系的头、身体、手臂或耳机。
- V10 独立生成拆件已被否决：头部自带领口、身体变成葫芦形、双臂悬空，静止拼装无法还原 V8 的连续 S／逗号身体。不得继续通过调坐标、缩放或遮挡掩盖拓扑变化。
- V11 完整状态图只通过“角色不分离”，没有通过说话表现：单张开口图与整体伸缩不能代表嘴型随语音运动。V12.1 以本机真实 TTS 播放音量驱动闭口、小开口、中开口和大开口，并加入独立眨眼、低幅呆毛／重心响应和句末温柔微笑；手动滑杆只作调试回退。正式拆件前必须执行静止重组检查：全部部件归位时与已接受完整母图保持同一轮廓、比例、五官位置和连接关系。
- V12 前两轮嘴型分别因统一平顶和开放线段／牙齿横线仍显截断而被否决。Rex 接受的 V12.1 改为：闭口与句末状态使用完整尖尾月牙，小开口使用近圆 O，中／大开口保持完整有机椭圆，句末不再使用板牙。只有完成覆盖停顿、句末、打断与 Reduce Motion 的代表速度录屏并由 Rex 视觉验收后，才能称为动态体验通过。
- Spine V1 的语音核心 Rig 不把头、身体或胸前静置双臂拆成独立纹理，而是使用一张连续的 `core_body_with_resting_arms_mesh` 承担低幅权重变形；耳机、五官、刘海、呆毛与嘴型作为独立槽位。递卡、看书等需要大幅手臂移动的动作使用后续动作包专用遮挡纹理，不能让静置手臂变成悬空豆形部件。
- POC 通过后再根据动作数量、动画师协作和长期维护需要，决定保留完整帧／原生 SpriteKit，或迁移到 Rive／Spine；状态名、动作节奏与验收记录可以复用，V10 错误拆件不能复用。

### 8.5 吉祥物、App 图标与 Logo

- 三者来自同一个角色，而不是三套互不相关的图形系统。
- 吉祥物使用完整悬浮身体，用于复习窗口、语音状态和未来 Spine 表演。
- App 图标最终应从被接受的角色母形提炼，在小尺寸保留最有生命力的轮廓与表情；是否使用头部近景不在角色确认前锁死。
- 已接受草图同时锁定 App 图标取景：角色轮廓约占图标宽度的 72%，呆毛只保留约 2%–4% 上边距，头部成为第一视觉层级；身体、双臂与知识卡在下边缘自然裁切，不为展示完整角色而缩小。完整悬浮身体只用于吉祥物立绘，不用于 AppIcon。
- 极简 Logo 最终从同一角色提炼，用于菜单栏、状态图标、水印和小尺寸品牌位置；不预设必须由头部线圈或嘴构成。
- 品牌的核心静态动作采用“复习老师带着今日复习卡来找用户”：优先探索双手抱着两张轻微错位的复习卡、向用户递卡邀请，以及翻卡提问三种构图。空手抱臂容易显得审视或封闭；吹哨与胸前常驻哨子偏向体育教练、裁判和催促，不进入常驻吉祥物、App 图标或 Logo。
- 复习卡负责表达“带着内容帮助用户重温”，角色表情负责表达温柔老师人格，耳机和原生会话状态负责表达语音互动。App 图标不强行同时塞入复习、老师、语音与 AI 的全部符号，也不使用问号、对勾、文字、麦克风或通用聊天气泡替代角色辨识。
- 复习卡不放真实英文、产品 Logo 或可阅读题目；使用小型陶土色标题条、两三条长短不一的石墨内容线和轻微错位的第二张卡建立知识资料语义。完整吉祥物中的卡片可有极浅的哑光纸纹和轻微纤维感，App 图标与小 Logo 必须简化为平面结构，不保留缩小后会变脏的材质细节。
- App 图标中的默认表情采用居中、闭口的温柔微笑，不使用偏向一侧的不对称方牙笑。居中两颗小板牙保留为吉祥物说话或鼓励状态的候选表情，不作为当前常驻 Logo 表情；无牙开口版本不用于静态图标。
- V8 正式品牌资产以 V7 左侧为唯一静态方向，通过 GPT Image 生成工作流分别产出完整抱卡吉祥物、彩色 App 图标与单色标志位图母版；不再用 SVG、Bezier 路径或代码重新描画角色。代码仅允许按已接受草图的取样中位色移动现有像素，以及对选定母图做无内容创作的裁切、缩放、圆角蒙版与格式导出。
- V8 当前角色颜色直接从已接受草图取样：产品底 `#FEF9F2`、头部 `#FFF7E8`、下半身 `#FCEFD6`、知识卡 `#FEF8ED`、图标陶土底 `#E58E6D`、石墨 `#3B3A38`、卡片强调 `#E97C4D`。角色不是统一纯白：头部接近纸白，下半身保留更深的奶油层次。不得把角色推成明显黄色、金色或桃色，也不得把图标底推成高饱和橙红、中心发光或暗角渐变。产品正式名称尚未确认，因此本轮不制作或锁定 Wordmark。
- Rex 已锁定 V8.3 AppIcon 人物显示范围。V8.4 透明资源只能从同一模型母图移除背景并保留真实 Alpha，不得借透明提取重新设计角色；暖白底版本继续作为安全回退。
- 模型曾在完整吉祥物去底时把棋盘格直接画入 RGB 图片，因此失败结果不进入资产包；透明吉祥物改为从已接受模型母图确定性移除连通背景，透明单色标志使用模型成功输出的 RGBA 结果。两项均已通过 Alpha 与暖白／陶土叠底检查，但仍需 Rex 视觉验收；深色叠底只用于暴露浅色角色边缘风险，不代表深色版本已完成，石墨标志也暂不作为深色反白标志或菜单栏模板图标。App 图标母图可以通过打包脚本生成 macOS 16–1024 px 资源，但构建成功不代表 Dock／Finder 视觉已经验收。Spine 拆件与动态表情也不由静态位图自动证明。
- 可见证据：第一轮未通过稿为 `mascot-concept-v1.png` 与 `app-icon-logo-concept-v1.png`；第二轮性格与动作方向稿为 `mascot-character-study-v2.png`；正常双耳耳机会话状态稿为 `headphones-session-storyboard-v3.png`；`mascot-bangs-ahoge-study-v4.png` 已由 Rex 接受为当前角色母形方向；`mascot-logo-color-study-v5.png` 是第一张角色—耳机—App 图标—Logo 配色关系稿；`mascot-review-card-logo-study-v6.png` 比较抱卡、递卡与翻卡三种复习语义构图；`mascot-card-expression-study-v7.png` 左侧已被 Rex 接受为当前静态品牌方向；`brand/raster/` 承载 V8 选定的模型位图母版，生成约束与失败边界见 `brand/GENERATION.md`。提示词、取舍与证据边界见同目录 `BRIEF.md`。

## 9. 键盘、辅助功能和本地化

### 9.1 键盘与焦点

- 今天、知识库、待处理、设置、快速采集和复习核心操作都必须可仅用键盘完成。
- 焦点顺序与视觉阅读顺序一致，异步更新、弹窗和恢复后保持可预测位置。
- Escape 关闭临时菜单或请求暂停，不能直接丢弃当前答案。
- 自定义录音或语音状态控件必须暴露名称、角色、值、状态和可执行操作。

### 9.2 非单一感官表达

- 颜色不是进行中、冲突、掌握或失败的唯一载体。
- 提示音、完成音和语音播报均有文字或可见状态。
- 问题和反馈支持屏幕阅读顺序，装饰图形从辅助功能树隐藏。
- 重要异步变化按需宣布，避免每个转写片段都抢占注意力。

### 9.3 Reduce Motion 与 Reduce Transparency

- 标准动效应克制、迅速且不阻塞重复操作。
- Reduce Motion 下减少大范围位移和缩放，以淡入或即时状态替代，同时保留完成、错误和因果关系。
- Reduce Transparency 下使用不透明表面保持文字对比与分组，不能简单移除所有层级。

### 9.4 语言

- 首版界面以中文为主，但用户学习内容可包含中英混排和目标语言。
- 用户可见字符串进入本地化系统，不手工拼接中文句子。
- 日期、时间、时长、数量和复数使用系统区域格式。
- 布局必须验证长知识点、长来源名、混合语言、较大文字和窗口缩放。

## 10. 验收路径

2026-09-03 本轮只适配既有学习界面的运行语义：五项模式、无 Task 的轻量反馈、排队/停止/恢复、绑定版本操作、理解状态，以及最新消息可滚动看到。已实际查看菜单、发送回显、停止/恢复、重试后的回答与重启历史；无完整五模式用户验收，不提升为 `user accepted`。启动时无服务输出的诊断提示系统文件夹授权和本地环境，不能把权限等待显示成意图或模型错误。截图与前置恢复步骤见 `docs/m1-acceptance.md` §0.8。

实现后 Rex 需要在真实 Mac App 中完成：

1. 新建自动模式 Session，输入“我想了解 RAG”，确认消息立即出现，并进入逐步收窄目标。
2. 停止本机服务后再次输入，确认消息不丢失、启动／恢复状态可理解，同一 Task 能在服务恢复后继续。
3. 分别走通 Auto、知识整理、资料学习、主题探索和问题攻克；检查每种模式的等待用户与完成条件。
4. 粘贴 JD，确认先出现能力地图和优先问题；选择一题后进入先答再校准、独立作答与追问。
5. 展开 Task 运行详情，确认节点、路由依据摘要、耗时、重试和恢复可审计，但没有隐藏思维链。
6. 归档与恢复 Session；对无关主题选择新建 Session，确认只传递结构化交接摘要。
7. 在浅色、深色、Reduce Motion、Reduce Transparency、键盘、指针和窗口缩放路径下完成学习核心任务。
8. 以中文长内容、中英混排、长来源名和长问题检查阅读、输入、焦点与布局。
9. 重启 App，确认 Session、消息、Task、Event、资料和知识关联恢复且不重复。
10. 从形成记忆回执打开知识库详情；处理一条冲突后确认队列与知识状态一致。

语音复习、通知、菜单栏采集和吉祥物动画保留其既有验收定义，但不是 Harness V2 本轮 M1 的通过条件。

只有原生实现运行并由 Rex 检查后，才能把状态从 direction aligned 提升为 code complete 或 user accepted。编译成功、文字文档或静态图都不能单独证明体验完成。

## 11. 尚未确认

以下不影响导航结构，留到原生实现或渲染验证：

- 产品正式名称、最终 Logo／App 图标／正式 Rive 或 Spine 资产及其小尺寸与明暗模式版本；
- 代表性页面的渲染稿与具体视觉令牌值；
- 菜单栏弹层与独立复习窗口的实际尺寸、窗口行为和动效节奏；
- 原生辅助功能语义、VoiceOver 和真实用户体验结果。

第 78 项已由 Harness V2 决策修订：首次启动仍不做独立引导页，服务状态进入学习任务与 Settings 诊断，Today 保持数据看板。


## 本轮教学与归档删除交互（2026-09-06）

- 教学正文直接讲知识、适量给例子并引用已读来源；不展示“至少两个互补来源”或英文证据状态。搜索失败说明“网页核验暂未完成，先讲基础内容”；冲突或时效部分明确待核实。新用户无需先建卡或积累个人记忆。
- 已归档会话菜单及多选工具栏增加红色“永久删除”。路径：归档列表 → 永久删除 → 确认 sheet → 删除或取消。显示名称/数量、不可恢复后果、可删除与保留卡片数及练习数；默认仅删除会话。独有卡片选项有候选才显示，默认不勾选。
- 共享/仅引用/归属不明卡片保留。范围变化更新 sheet 并要求再次确认；保存失败保留 sheet 和原内容，提供重试。确认取消无数据变化。
- 本地删除成功后返回列表/起始页；若服务未清理，显示“会话已从本机删除，后台清理待完成”，不提前声称全部完成。重启可续清理，旧数据不复活。保留知识原会话入口显示“原会话已删除”。
- 复用现有原生 sheet、菜单、语义色与焦点；Escape 取消，永久删除不用默认 Return 触发。验证单个、批量、取消、离线、失败、键盘和窄窗；最终视觉体验由 Rex 验收。
