# Review Today 产品界面与体验方向

> 完成阶段：direction aligned
> 证据边界：产品与交互方向已由 Rex 选择；尚无渲染稿、原生实现、交互录屏、辅助技术验证或用户体验验收

## 1. 设计目标

Review Today 的主界面不是笔记库、聊天窗口或学习数据仪表盘，而是一条清楚的“今日记忆跑道”：

```text
记住点什么
→ 系统正在形成记忆
→ 今天需要复习什么
→ 完成回答并得到反馈
→ 知识进入下一次复习
```

用户每次打开 App 都应该立即理解两件事：今天是否有内容需要复习，以及刚刚记录的内容是否已经被可靠整理。

## 2. 证据与设计判断

| 类型 | 当前依据 | 支持的判断 | 不证明什么 |
|---|---|---|---|
| 用户确认 | Rex 选择“今日记忆跑道”与“安静的学习编辑台” | 首页重心和产品性格 | 最终画面已经好看或好用 |
| 项目事实 | 当前 SwiftUI 仅为默认空壳，目标为 macOS 26.5 | 可以从零定义原生 Mac 结构 | 任何界面已经实现 |
| 本机产品观察 | Typeless 的首页、历史与低摩擦语音入口 | 快速采集应尽量用完即走 | Typeless 的视觉适合直接复制 |
| 本机产品观察 | AirJelly 的首页、主动帮助、任务和记忆管理 | 主动状态和可管理记忆可以建立信任 | 通用工作台适合 Review Today |
| 设计判断 | 复习不是考试，用户有自己的回答节奏 | 避免硬截断、红分和连续打卡压力 | 五分钟体验已经经过真实用户验证 |

Typeless 和 AirJelly 只作为体验证据。Review Today 不复制其品牌、图标、文案、页面或持续监听机制。

## 3. 产品原则

- **主动但不催促**：系统负责提醒和选题，但允许稍后、跳过、暂停和自然完成当前回答。
- **让过程可见但不技术化**：普通界面说明当前在读取、核验还是等待处理；技术节点进入开发模式。
- **帮助回忆而不是考试**：不给回答设置硬性一分钟截止，不用分数制造压力。
- **建立信任而不是制造黑盒**：来源、核验、转写、判断和下一次日期都可以查看与纠正。
- **一次只做一件事**：采集可快速离开，正式复习一次只呈现一道题。
- **音频不是唯一通道**：问题、状态、提示和反馈始终有文字等价表达。

## 4. macOS 信息架构

### 4.1 主窗口侧边栏

首版固定三个正式入口：

1. **今天**：今日记忆跑道、快速采集和今日复习结果。
2. **知识库**：按主题和掌握状态查看、纠错和管理知识。
3. **待处理**：集中处理冲突、证据不足、转写异常和校验失败。

Agent 运行记录不作为第四个正式产品入口。它只在开发模式开启后出现在设置或开发区域。

### 4.2 菜单栏

菜单栏承担低摩擦采集，而不是缩小版主窗口：

- 快速输入文字；
- 粘贴公开链接；
- 开始和结束语音随记；
- 显示最近一次任务的用户状态；
- 打开主窗口查看完整结果。

菜单栏不展示知识库、完整运行轨迹或正式复习题目。首版不做全局快捷键。

### 4.3 独立复习窗口

从首页“现在开始”或通知进入独立专注窗口：

- 与主窗口共享本地数据，但拥有独立窗口生命周期；
- 主窗口保留用户原来的浏览位置，不被复习界面替换；
- 一次只显示一道题及其当前状态；
- 关闭窗口触发“暂停并可恢复”，不丢失已完成题目；
- 当前未完成题恢复后重新提问。

## 5. 今日记忆跑道

建议的信息顺序如下，不在文字规范阶段定义未经渲染验证的精确像素：

```text
今天
────────────────────────
今晚 21:00 复习
7 个知识点 · 预计约 5 分钟
[现在开始]

记住点什么
[输入文字或粘贴链接……] [语音]

正在形成记忆
读取网页 1   正在核验 1   需要处理 1

今天新增
“我理解为……”
来源 · 主题 · 生成 3 个知识点

今日复习结果
完成后展示掌握结果与下一次安排
```

### 状态规则

- 没有复习候选时，第一块显示“今天无需复习”，而不是空白或失败。
- Python 服务不可用时，采集内容仍先保存本地；页面说明“已保存，服务恢复后处理”。
- “正在形成记忆”使用用户语言：读取网页、正在核验、整理完成、需要处理。
- 技术名词、节点耗时和重试次数只在开发记录页展开。
- 今天新增默认展示简短回执，详细知识点按需展开。

## 6. 正式复习交互契约

### 6.1 入口和完成

- 入口：首页按钮、本地通知或当天仍有候选时的手动开始。
- 完成：五分钟窗口结束且当前题自然收尾，或候选用尽后显示会后总结。
- 暂停：关闭窗口或选择暂停，已完成题立即保存，未完成题不产生评分。

### 6.2 一题状态

```text
准备问题
→ Agent 提问
→ 聆听用户
→ 用户确认答完
→ 展示转写
→ 评分或免费重录
→ 简短反馈
→ 用户可改判
→ 本地保存
→ 下一题
```

- 使用 VAD 自动识别说话边界，同时保留“我答完了”按钮和口令。
- 用户可以打断 Agent 播报。
- 回答时不滚动展示全文转写，以免干扰回忆；答完后再显示完整转写。
- “转写不对”是技术纠错，不是知识答错，可免费重录且不占一次提示。
- 单题约一分钟时只播放一次轻提示音并显示文字说明，用户可以继续、请求提示或跳过。
- 提示音不能是唯一反馈；静音、听力受限或未听见时仍能理解状态。
- Agent 最多给一次学习提示；提示后重答即使正确，最高自动等级为 Hard。

### 6.3 判断与纠正

- 先显示 Again、Hard 或 Good 和一句可理解依据。
- Easy 只在用户认为明显过于简单时主动选择。
- 用户可以改为任意等级；界面应同时保留“Agent 原判断”和“最终采用”。
- 会后总结允许进一步修改知识、问题或评分规格，避免同类误判持续发生。

### 6.4 故障与降级

- Realtime 中断时提示正在切换，先用 macOS 系统语音朗读，再提供纯文字路径。
- 评分失败时明确说明“本题尚未计入复习”，允许重试，不能显示虚假掌握结果。
- 本地写入失败时停留在当前题结果页，不进入下一题。
- 网络恢复、App 重启和窗口恢复后，最终状态必须与用户最后一次有效操作一致。

## 7. Agent 运行记录

开发模式中的独立页面用于理解和排查 Agent 循环，而不是展示模型思维链。

每次任务展示：

- 触发来源和任务 ID；
- 实际经过的节点；
- 触发联网核验或待处理的结构化原因；
- 每步耗时、重试和最终状态；
- 经过脱敏和裁剪的输入输出摘要；
- 失败位置与恢复动作。

不展示：

- 模型隐藏思维链；
- OpenAI Key、Authorization 头或短时凭证；
- 与排查无关的完整知识正文或音频；
- 可以绕过产品规则的自由工具调用入口。

## 8. 视觉性格：安静的学习编辑台

### 8.1 共同特征

- 原生支持浅色和深色外观，不把其中一种当作附属主题。
- 依靠清晰排版、稳定留白和状态层级建立秩序。
- 使用克制的蓝绿色语义角色表达进行中与已掌握，但任何状态都同时配文字、形状或图标。
- 不引入吉祥物、火焰连续打卡、排名、考试红分或夸张庆祝。
- 不用大面积玻璃、动态渐变或复杂粒子掩盖任务状态。
- 允许轻微温度与鼓励语气，但不把 Agent 拟人成需要维护关系的角色。

### 8.2 语义角色

最终值需要在渲染稿中验证，当前只定义用途：

- `contentPrimary`：问题、知识点和主要结论；
- `contentSecondary`：来源、说明、时间和辅助信息；
- `contentCritical`：冲突、失败和永久删除；
- `surfaceBase`：主要阅读与复习背景；
- `surfaceRaised`：回执、任务和反馈卡片；
- `surfaceSelection`：当前主题、当前题或选中知识；
- `actionPrimary`：现在开始、提交回答和保存；
- `focusRing`：键盘焦点；
- `motionFeedback`：轻量状态确认；
- `motionReduced`：减少空间移动后的等价状态反馈。

## 9. 键盘、辅助功能和本地化

### 9.1 键盘与焦点

- 今天、知识库、待处理、快速采集和复习核心操作都必须可仅用键盘完成。
- 焦点顺序与视觉阅读顺序一致，异步更新、弹窗和恢复后保持可预测位置。
- Escape 关闭临时菜单或请求暂停，不能直接丢弃当前答案。
- 自定义录音或语音状态控件必须暴露名称、角色、值、状态和可执行操作。

### 9.2 非单一感官表达

- 颜色不是进行中、冲突、掌握或失败的唯一载体。
- 提示音、完成音和语音播报均有文字或可见状态。
- 问题和反馈支持屏幕阅读顺序，装饰图形从辅助功能树隐藏。
- 重要异步变化按需宣布，避免每个转写片段都抢占注意力。

### 9.3 Reduce Motion 与 Reduce Transparency

- 标准动效应克制、迅速且不阻塞重复操作。
- Reduce Motion 下减少大范围位移和缩放，以淡入或即时状态替代，同时保留完成、错误和因果关系。
- Reduce Transparency 下使用不透明表面保持文字对比与分组，不能简单移除所有层级。

### 9.4 语言

- 首版界面以中文为主，但用户学习内容可包含中英混排和目标语言。
- 用户可见字符串进入本地化系统，不手工拼接中文句子。
- 日期、时间、时长、数量和复数使用系统区域格式。
- 布局必须验证长知识点、长来源名、混合语言、较大文字和窗口缩放。

## 10. 验收路径

实现后 Rex 需要在真实 Mac App 中完成：

1. 从主窗口和菜单栏分别完成三类采集。
2. 观察处理中、成功、失败、离线和待处理状态是否无需技术解释。
3. 从首页和通知分别进入独立复习窗口。
4. 完成正常回答、超过一分钟继续回答、提示重答、转写重录、改判、暂停恢复和故障降级。
5. 在浅色、深色、Reduce Motion、Reduce Transparency 和键盘路径下检查核心任务。
6. 以中文长内容、中英混排和目标语言问题检查阅读与布局。
7. 查看开发模式运行记录，确认它解释 Agent 机制但不破坏普通产品体验。

只有原生实现运行并由 Rex 检查后，才能把状态从 direction aligned 提升为 code complete 或 user accepted。编译成功、文字文档或静态图都不能单独证明体验完成。

## 11. 尚未确认

- 产品正式名称、Logo 和最终品牌资产；
- 代表性页面的渲染稿与具体视觉令牌值；
- 第 78 项首次启动预检的页面形态；
- 菜单栏弹层与独立复习窗口的实际尺寸、窗口行为和动效节奏；
- 原生辅助功能语义、VoiceOver 和真实用户体验结果。
