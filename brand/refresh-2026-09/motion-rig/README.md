# 可编辑形变小样 · 2026-09-07

Rex 已确认开始实现 AI 辅助制作工具，先看「眼睛先瞟动 → 身体局部弯曲 → 碎片分离并重组」。当前 V3 仍为临时母形，Logo 不变，旧位图动效预览保留。

交付为可操作网页、Spine 4.2 JSON/atlas/图片、参数源和可调用 MCP；不宣称完成 Spine 编辑器工程或原生集成。

启动入口及工具说明见 [制作 MCP](../../../tools/mascot-motion/README.md)。从仓库根目录执行：

```sh
npm ci --ignore-scripts --prefix tools/mascot-motion
npm run build --prefix tools/mascot-motion
npm start --prefix tools/mascot-motion
```

打开 <http://127.0.0.1:8769/>。手动静态服务器也能查看基线，但不能在线调整参数；须先构建准备 `vendor/`。

## 本次环绕修订

Rex 已确认修复「只有沿身体边缘转圈」的问题。主体等比缩至 86%，深青色纹理不变；球改为浅青绿并带上左侧高光，阴影采用接近黑色的青灰（深色背景仍有暗部对比）。

- 球沿倾斜的扁椭圆运行，近半圈覆盖身体，远半圈被遮挡，近大远小；Spine `drawOrder` 记录前后切换。
- 身体地面椭圆投影、球地面投影与球在身体上的投影，均作为 Spine 附件及时间轴输出。身体表面投影使用同一组权重/形变驱动的裁剪轮廓，不泄漏到身体外。
- 共 14 骨骼；身体仍为 193 顶点、336 三角形，裁剪轮廓另有 48 顶点。停止时保留正在淡出的前后顺序，减少动态效果保留静态身体和地面投影。
- 固定姿态对照：2.48 s 身前（`evidence/front.png`）；3.17 s 身后（`evidence/back.png`）；`front-dark.png` 为深色离线渲染。视频已更新。
- 自动化新增前后顺序、近远比例、身体缩放和裁剪轮廓随身体一致变化的回归检查；共 9 项通过。

## 验收路径

1. 「播放小样」：瞳孔先左瞟并短暂眨眼，身体随后发生局部弯曲，右侧分出小块绕行，最后重组回弹。
2. 「半速观察」或拖动时间轴，在 0.45 s、1.25 s、2.48 s、3.17 s、4.6 s 比较眼神、身体轮廓和碎片位置。
3. 勾选「显示网格与骨骼」「显示身体权重分布」，看到局部顶点变化与 3 区权重。
4. 在分离中「立即收拢」：文字立即响应，当前姿态向静止姿态混合约 0.22 s；可重新播放，不等待一圈结束。
5. 开启「减少动态效果」：停止循环、收拢为静止，时间轴禁用，状态解释仍存在；手动开关与系统媒体查询关联。未执行原生系统设置切换或 VoiceOver 端到端验收。
6. 改身体/眼神幅度后「生成并预览」：保存新版本并更新下载。原版仍在 `data/mascot.json`；MCP 修改也会同步到可见网页。

## 文件

- `recipe.json`：当前已提交的制作参数；非用户已验收的最终动效数值。
- `data/mascot.json`：可编辑骨架、加权网格与 4.2 形变时间轴。
- `data/images/`：atlas 和图像源，身体 RGB 图由网格轮廓裁剪显示，非透明 PNG 拆件。
- `assets/`：去眼生成素材、眼白/瞳孔/碎片的可编辑 SVG 及保守身体轮廓。
- `evidence/recall.mp4`：官方运行时骨骼计算 + Canvas 光栅适配器生成的 5.6 秒视频，非原生录屏。
- `evidence/bend.png`：从 MCP 图片反馈取得的关键帧（最终光栅修订后重新渲染）。

已验证浏览器宽窄布局、控制交互、MCP 修改与官方运行时读取；测试详见制作工具 README。Spine 编辑器导入、正式角色素材、原生小尺寸效果与性能、真实语音驱动均尚未验证。
