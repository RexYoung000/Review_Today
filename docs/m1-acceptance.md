# M1 Agent Harness V2 验收契约

## 会话组织、知识批量管理与连续定位（2026-09-08，已实现／体验验收开放）

本节替代下方同日会话二级工具栏方案。已接入一级搜索浮层、唯一可见草稿与新建过渡、单层会话文件夹、设置内归档管理、分主题知识多选与回收站语义，以及连续竖向定位条。保留单栏知识卡、层叠、吉祥物与既有删除保护；只扩展文件夹本地模型，服务接口、模型配置及知识分类/自动标题规则不变。

新增会话组织和知识管理状态契约，覆盖真实磁盘重开、旧 schema 迁移、首次发送/移动/批量保存失败、可见选择范围、共享来源及相关复习记录保护；复用翻卡、会话选择/删除、焦点、学习输入/记忆、UI 与运行模式回归。Debug/Release、严格签名、原生预览构建通过；最后的窗口复用修正后会话组织和 UI 契约再次通过。

原生实看标准/最小窗口、浅深主题、搜索输入/关闭回焦、重复新建与草稿保留、文件夹移动、归档搜索全选后改关键词清空勾选、恢复原文件夹、查看只读及复用主窗口、知识分组全选/暂停/回收/撤销、永久删除影响与隔离样例清除、连续定位/键盘/长正文滚动及减少动态。修正了搜索焦点、删除说明截断、回收站仍可试题及归档查看重复开窗。定位排序中的重复标题解析已优化：25 张卡本地 DEBUG 输入到绘制中位数由 188.9 ms 降至 73.0 ms，100 张修复后 99.8 ms；这不是物理屏幕延迟或稳定帧率保证。

8 段原速原生录像、截图、测量口径、验证输出及复现方式见 [本轮验证记录](evidence/2026-09-08-library-sessions/management/README.md)。日常开发 App 已更新并实际打开，原有知识正常载入；“界面预览 · 仅内存数据，不连接模型”确认只来自隔离预览，日常窗口无此提示。删除测试未使用日常数据。

最终视觉和实体触控板手感待 Rex 验收；完整 VoiceOver、系统减少动态开关、非中文大字号组合及真实模型服务闭环本轮未重验。没有关闭 M1 验收或执行发布。

## 知识卡单栏区块与层叠修订（2026-09-08，已实现／体验验收开放）

已按 Rex 选择的 B 方向接入单栏阅读，用主问题与判断区两块中性底色分层，取消内部分隔线，误区默认折叠。底部每层露出 24 pt，以短阴影与明暗差增强三层卡面；保留固定标题／操作、单一正文滚动和双向翻阅。同时修复旧排版器自动给普通段落编号及截断长正文的问题，原文与知识数据保持。

定向翻卡／原生输入／完整正文契约、Debug／Release 构建及严格签名通过；实际检查浅深主题、宽窄窗口、长正文末尾、展开与来源选文、标题拖动、连续键盘、短拖、单卡和关闭。截图与正常速度录屏见 [本轮单栏验证记录](evidence/2026-09-08-library-sessions/README.md)。日常开发 App 已更新，最终视觉及实体触控板手感、完整 VoiceOver 和非中文排版仍待 Rex 验收，不关闭 M1 或执行发布。

滑动边界补修：移动卡片与阴影裁切在右侧内容区，滑出逐渐淡隐、返回反向显现，短拖回弹恢复。标准浅色及窄窗深色越界长拖、反向返回、短拖、键盘与现有原生输入回归通过；Debug／Release／签名通过。新增原生过渡录像与采样范围见上述验证记录；实体触控板最终手感仍待验收。

## 会话二级操作与空态收敛（2026-09-08，已实现／体验验收开放）

搜索、多选改为 12 pt 轻量文字操作；空的进行中列表隐藏第二行和“进行中”，空归档仅保留范围提示。以搜索前当前范围的数据判断空态，搜索无结果和已有搜索/多选保留恢复/退出入口。会话范围、批量保护与草稿规则保持。

独立 Debug/Release 构建、严格签名、会话选择/空态状态回归与共用焦点回归通过；原生检查有会话、空进行中、空归档、搜索无结果、搜索关闭与多选恢复，覆盖浅深主题、标准/最小窗口和 250 pt 侧栏。修复验收脚本字体资源目录与 Xcode 不一致导致的预览空态退出；日常 App 字体打包原本正常。截图、正常速度录屏及具体边界见 [增量验证记录](evidence/2026-09-08-library-sessions/session-tools/README.md)。完整 VoiceOver 与最终视觉体验仍待验收。

## 知识卡与会话层级调整（2026-09-08，已实现／体验验收开放）

知识详情改为宽卡、向下层叠、标题区左右拖动翻阅和右侧竖向定位；正文单独滚动，来源可展开并选择文本，底部操作固定。已暂停/已删除空态与待处理复用 150×170 Spine 随机待机。会话工具栏分两层，搜索和多选属于当前列表；切换范围和新建重置搜索与选择，批量操作只使用当前可见的选中项。分类、自动标题、数据模型、接口和模型配置未变。

Debug 开发签名构建、严格签名和 Release 编译通过。新增翻卡状态与原生输入边界、会话选择测试通过；复用焦点、UI 契约、删除保护、31 项动效及原生 WK 生命周期回归。已在隔离内存窗口检查宽窄、浅深、长标题/正文、连续及反向翻阅、正文滚动/来源文本操作、竖向定位、键盘、归档搜索/多选/恢复/新建和删除确认；失败恢复由隔离契约测试覆盖。

正常速度原生录屏、截图、具体覆盖与复现入口见 [本轮验证记录](evidence/2026-09-08-library-sessions/README.md)。减少动态原生录屏使用同一布尔环境覆盖；本轮未再操作系统设置，实体触控板手感和完整 VoiceOver 仍未验收。日常开发 App 已更新并打开，最终体验验收与发布保持开放。

## 气泡字体选定（2026-09-07，已接入）

会话气泡使用 Rex 已选的荆南麦圆，随 App 内嵌原始字体与 OFL 1.1 许可，仅进程内注册；保留两行文字和完整可访问名称，其他页面字体不变。Debug/Release、签名、资源打包及原生浅深色字形和点击回焦已检查。最小窗口的新证据未完成，见 [验证记录](evidence/2026-09-07-ui-polish/README.md#荆南麦圆接入)。最终体验验收保持开放。

## 会话空态居中与柔和气泡（2026-09-07，已接入）

按已确认方案，在会话区域按可见整体垂直居中；按追加反馈撤掉硬描边和尖尾，使用柔软圆角实底、两颗递小圆泡与左对齐错落文字；仅气泡加微弱向下软阴影。维持「开始学习」及已有双动作循环，不改其他空态或输入行为。已在原生默认窗口及 760 pt 小窗口展开侧栏检查浅深主题：气泡与角色整体居中，未遮挡工具栏和设置；点击气泡进入 Agent 并聚焦草稿，无自动发送。Debug/Release 构建及 Debug 严格签名通过，日常 App 已更新。本次未改动画实现，未重跑上一节动效回归；完整 VoiceOver 等已有未覆盖项仍保留。最终视觉体验待 Rex 验收。

## 会话气泡与双动作循环（2026-09-07，已接入）

此前按反馈将文案改为「开始学习」，当时采用连续尾巴（轮廓现已由上节柔和气泡覆盖）；侧栏空态从单次阅读改为看书/左右观察交替循环，首次等待 1 秒，段间 0.8 秒。保留隐藏/后台暂停、恢复不追赶、减少动态静止；不改待处理随机待机和语音/思考动作。31 项动效回归、原生真实时钟「阅读 → 观察 → 阅读」检查、减少动态/隐藏暂停回归和 UI 契约通过；Debug/Release 构建及 Debug 严格签名通过。日常 App 已更新；完整循环录像与说明见 [UI 验证记录](evidence/2026-09-07-ui-polish/README.md#会话气泡与循环补验)。最终体验验收保持开放。

## 页面布局、焦点与新角色统一（2026-09-07，已实现／体验验收开放）

已确认今天页固定顶部、Agent 品牌居中、同轮廓控件反馈、真实编辑焦点、知识库筛选计数与完整标题、准确空态引导、始终展开的会话列表，以及侧栏一次阅读和知识库/复习页新角色接入。具体行为见 DESIGN.md 顶部；不改原始知识数据、后台接口或模型能力。

已实现并通过相关输入/听写/焦点契约、30 项动效回归、原生 WK 动效、Debug/Release 构建和 Debug 严格签名检查。实际检查默认及 760 pt 最小窗口、浅深主题、侧栏收放、固定顶部滚动、筛选计数/长标题/空态、Tab 进出编辑器、快捷填入与一次撤销、新角色和系统减少动态。

原生单次书本录像已保存；整窗滚动/焦点录像因系统录屏工具启动超时未完成，完整 VoiceOver、真实中文候选窗和本轮真实麦克风未通过完整验收。详细范围与复现见 [UI 验证记录](evidence/2026-09-07-ui-polish/README.md)。日常开发入口继续使用 `output/default-brand-build/Build/Products/Debug/Review_Today.app`。不发布，不关闭最终体验验收。

## 竖立阅读与夸张弹性修订（2026-09-07）

后续修正：本子透视统一为两侧外缘高、中央书脊低的 V 形，封面、纸页与翻页共享深度投影；补充 V 形方向回归，资源与证据同步更新。

待处理空态的阅读改为 8 秒，从身后绕侧拿出、竖立展开、阅读翻页、合上收回；封面复用现有 R，取消渐显。伸展最高约 140%，跳高约身高 35%，增加蓄力、局部滞后、落地挤压与回弹；画布 150×170，角色静止大小不变。眼部、左右看看、思考和语音轨道保持。

30 项回归通过，含逐帧裁切、透明度、书本朝向、换层遮挡、瞳孔与幅度检查；原生暂停/恢复/减少动态及思考语音检查通过，Debug 构建与签名通过。已查看实际 App 浅深空态及拿出本子的动作。原生 WKWebView 正常时钟录制与离线视频单独标注，见 `brand/refresh-2026-09/idle-motion/README.md`。HTML 控件浏览器交互仍受此前安全策略限制，未宣称已验收；视觉验收与发布未关闭。

## 四段随机待机与眼部修正（2026-09-07）

待处理空态接入左右观察、轻跳、眨眼伸展、悬浮翻本子四段 Spine 动作；首次等 1 秒，段间等 1.5–3 秒且不相邻重复。保留无手造型，浅深角色均为白眼白黑瞳孔，仅银白眼眶有细线。原有网格、权重和动画轨道未改变。

27 项动效/调度/眼部边界回归和原生动效检查通过，含本子打开、减少动态收起及隐藏暂停；Debug 构建与签名通过，实际 App 查看浅深小尺寸。HTML 已生成，但内置浏览器策略阻止本地文件打开，控件交互未验收；未绕过限制。四段与随机短视频、规则和入口见 `brand/refresh-2026-09/idle-motion/README.md`。视觉验收与正式发布未关闭。


## 忠实听写整理与反馈（2026-09-07）

提示词独立定义自然分段/列点、明确口误、口述排版及原意保护；单次模型调用返回编辑依据，后台核对唯一片段、不重叠、内容顺序与关键数字。修正列表编号误判，移除固定字数比例/否定计数作为唯一守卫。新增可选回退原因，原生区分「已添加到草稿」与「已保留原始转写，可直接编辑」，保存后立即解锁并保留一次撤销、收尾动画及四阶段计时。保留北京、现有模型、录音生命周期；不接声纹或实时识别。

97 项后台相关测试、2 个子测试，三项原生契约、22 项动效及原生动效回归，Debug 开发签名/严格签名与 Release 构建通过。18 组真实文字前后对照逐项核对通过；6 组约 11/32/69 秒合成音频经过两阶段真实 HTTP 调用，ASR 均成功，整理 5 成功、1 次因无效模型结构回填原文。等待中位数 3.872 秒、最大 7.686 秒，不是实时识别延迟或稳定性能承诺。

隔离原生窗口验证了草稿保护、锁定/恢复、取消、离页保留、回填与一次撤销及窄窗。麦克风没有清楚采集播放的测试句，仅回填「是。」；真实麦克风识别质量未通过，音量联动强弱、完整 VoiceOver 和系统减少动态体验仍待验收。没有向日常 Agent 发送测试消息。详细通过项、失败样本与已知限制见 [验证记录](evidence/2026-09-07-faithful-dictation/README.md)。本轮不关闭 M1/#17 或 Rex 最终体验验收，不发布。

新版原生入口：`/tmp/review-faithful-build/Build/Products/Debug/Review_Today.app` → Agent → 输入框右侧麦克风。另从本轮暂存内容独立导出 Debug 编译通过，未将并行待机资源加入听写提交。

## 深浅材质与页面重心（2026-09-07）

深色下待机、思考与语音角色统一银白材质和反向眼部，浅色保持黑色；移除网格轮廓描边，小球/波条使用主题灰阶，旧角色及加载占位同步适配。原图和骨架不变。减少动态效果配置现在立即取消计时并绘制静态姿态，修复原生检查发现的待下一帧停止问题。

Agent 起始页和今天内容整体按可用高度居中，限制过宽排版；移除日常输入操作提示和今天标题下数量小字。已在实际 App 查看深色待机银白角色、浅色 Agent 和今天页的新重心；侧栏 Logo 保留。23 项动效测试、原生运行/停止/减少动态/隐藏检查、Debug 构建与仅本轮改动的独立签名构建通过。未做真实录音、全页面回归和 macOS 27 验证；视觉细节待 Rex 查看。


## 主窗口与页面去重（2026-09-07）

去掉主窗口跨页标题、Agent 重复页名/会话标题和正文大 Logo、知识库两层标题。侧栏展开与收起继续保留已确认 Logo，使用连续圆角及 250–340 宽度范围；保留收放与宽度记忆，增加可访问的宽度调整操作。原生 AppKit 关闭/最小化/全屏按钮放在侧栏顶部，空白区域支持拖动窗口。听写位于发送左侧，沿用共享中性悬停/按下反馈，业务语义不变。待处理空态使用现有 Spine 黑色角色，独立 ambient 开关仅增加轻微骨架呼吸，不改变原思考/语音停止语义。

已验证 Debug 构建、仅本轮暂存内容的独立 Debug 构建与严格签名、22 项网页动效回归、原生动效含待机推进/减少动态/隐藏暂停，以及焦点与听写契约。实际窗口检查今天、Agent、知识库、待处理，浅深主题、侧栏收放、全屏进入退出；标题去重和待处理角色可见。完整真实录音、VoiceOver 流程及 macOS 27 未测试。窗口与侧栏样式的最终体验仍待 Rex 查看；本轮没有发布。


## 黑白材质日常接入（2026-09-07）

Rex 授权后启用中性黑白 UI、黑色统一角色与灰白环绕小球，保留既有 Logo、布局、语义色和听写逻辑。日常窗口不注入小样数据，也不引入玻璃 UI。

Debug 开发签名构建、严格签名检查、Release 编译（未签名）通过；22 项动效回归、原生动效运行及停止/零声量/后台/减少动态效果、配色对比度与焦点、听写状态及回填撤销契约通过。已实际打开日常 App 检查 Agent 浅深主题、输入区、学习方式菜单和历史数据入口。没有新增模型请求或录音；macOS 27、真实麦克风及全页面回归不在本次已验证范围。

日常入口：`output/default-brand-build/Build/Products/Debug/Review_Today.app`。细节与历史小样证据见 `brand/refresh-2026-09/monochrome-ui/README.md`。正式发布另行处理。



## 听写凭证与真实接口补验（2026-09-07）

用户提供控制台 CSV 后，从文件读取唯一原始 Key（无聊天转义字符），保存于 Git 忽略且权限 600 的 ASR `.env`；不记录密钥或 CSV 内容。北京 Fun-ASR 实际返回 `output.text`，修正原先仅解析 choices 包装的实现，新增两种响应格式回归，12 项听写测试通过。

真实云端及本地 `/v2/dictation/transcribe` 均返回 200 和 `Please keep the.`，本地服务已启动。样本是 0.854 秒非敏感合成语音，单次请求约 0.4 秒，仅证明鉴权、模型调用和文本解析打通，不代表完整中文/专业词识别质量或通常延迟。此前原始密钥缺失阻塞已解除；真实麦克风、中文长句、整理回填与原生完整体验仍待验收。


## 云端听写接入（2026-09-07，代码完成／真实识别待凭证）

- 主输入区接入开始、结束、取消、重试和删除录音；录音/处理期间锁定编辑及发送，识别结果以单次原生撤销追加到草稿，保存成功才清理录音。真实音量接到已有聆听动效，处理阶段用思考，停止保留短暂收势；不使用回答状态。
- 新增独立转写和保守整理接口，北京 `fun-asr-flash-2026-06-15`，不改变文字模型配置、主消息协议或知识库。转写结果先保存在本机，整理失败保留原文，重试已有文字不重新识别录音。
- 已验证：42 项后台相关测试、2 个子测试通过（兼容接口测试显式使用隔离的 openai_compatible 测试配置）；原生听写契约、现有输入契约及会话删除契约通过，覆盖权限拒绝、取消、文件保留/清理、保存失败重试、过期响应和单次撤销。当前工作区 22 项动效回归通过，其中包含并行材质工作的回归。Debug 签名构建、严格签名校验和 Release 构建通过；仅导出本轮暂存内容的干净 Debug 构建通过，不依赖未提交材质代码。
- 原生实看：独立 NativeQA 数据目录中 Agent 起始页的听写入口、浅深主题、按钮可访问性名称和提示语；没有开启麦克风、没有发送消息、不写入日常数据库。忙碌状态及回填由隔离契约检查覆盖，不冒充真实录音体验验收。
- 外部阻塞：本机尚无 ASR 原始 Key 配置，聊天凭证包含疑似转义字符，未猜测修改或发起付费调用。因此 Key/Workspace/权限、真实返回格式、识别质量、耗时和费用均未实测；真实麦克风与动效联动、紧凑忙碌布局、VoiceOver 实际操作仍待验证，不能宣布听写已可用或关闭验收。
- 后续入口：在 `agent-service/providers/asr/.env` 配置原始 Key，按该目录 README 重启服务；先用非敏感短 WAV 跑真实 smoke，再在 Agent 输入框点击麦克风验收。源码与配置样例已准备，密钥不进入提交。海外上线前重新核对服务地域、凭证、模型和数据条款。

## 已确认角色动效接入（2026-09-07）

Rex 授权把已确认动画接入 App，并明确“先完成动画接入，听写下一轮做”。当前不扩大麦克风、转写服务或语音聊天。

- **实际接入：**`RunPhaseLine` 根据真实 AgentRun 的 running / adjusting 和 startedAt 驱动 recall；完成、失败、取消收拢，不改服务协议和状态文案，不把文字输出当作语音回答。语音四态封装为原生可调用组件，开发窗口中的状态与音量均为明确标识的模拟。
- **离线资源：**随包 HTML 由同一骨架、纹理、共享碰撞模块和官方 Spine 运行时生成；不依赖 Node、本地 HTTP 服务或远程资源。背景暂停、视图移除清理，减少动态静止；装载失败保留静态标识和原生状态文字。
- **验证：**19 项 JS 动效/MCP 回归通过；隔离 WebKit 原生运行时检查通过，覆盖状态映射、资源真实载入、环绕推进/收拢、减少动态、隐藏暂停、三种活动语音状态、面积保持和停止计时。`node tools/mascot-motion/src/build-native.mjs --check` 校验源/包一致。Debug Apple Development 签名构建和严格签名验证通过，Release 构建通过；既有语音弃用等警告不属于本轮新增。
- **原生体验证据：**通过独立原生窗口实看已确认的大小环绕、语音聆听/思考切换与紧凑组件。最终验证窗口与日常共用 RunPhaseLine，模型仅存在内存；其余状态由原生运行时检查覆盖。新版签名日常 App 已打开，8 张知识卡、4 个学习任务和 RAG 历史仍在，没有发送日常测试消息；文件菜单已列出新验证窗口。完整真实模型往返未在本轮重跑，不冒充新取得的供应商或端到端证据。
- **验收入口：**Debug App「文件 → 新建 → 新已确认动效 · 原生验证窗口」；或 `bash tests/mac/run-mascot-native.sh --interactive`。依次切换四态、半速和减少动态；日常会话下一次实际运行时检查小环绕及结束收拢。语音转文字、真实音量/播放/断句绑定、VoiceOver 实际操作与最终美感验收仍待后续。M1 / #17 不因此关闭。

## Agent 输出可读性（2026-09-06，已实施，分项验证）

采用 B 正文混排＋按需 C 线性流程图。Rex 补充的列表要求已落实：并列项显示圆点，步骤显示序号，折行与正文对齐。正文完整展开，保留原文和协议；不强制每个回答都出现表格/流程图，不新增模型调用。

- **代码与受控验证：**最终 Debug / Release 构建通过；提交前再次运行 215 项 Python 服务回归通过（46.497 秒）。Mac 全部契约及真实本地 SSE 通过，涵盖输入/草稿、稳定消息 ID、停止/归档/重启重放及状态守卫。新增块解析契约覆盖相邻标题、序号/圆点/嵌套列表、代码、转义竖线、未完成行、中文与 emoji、逐字符流式、流程引用/分支/环/未知语法回退和检查题去重；真实生成的表格/流程成功解析，未落入源码回退。长文本约 30 ms 的单独解析样本不等同于端到端性能保证。
- **原生排版：**同一真实 SwiftUI 渲染组件在 340/580/780 pt 正文宽度与浅深主题下生成六张图，均核对无裁切；窄表格转为带列名的纵向内容，流程转为竖排。实际样例窗口核对标题/列名/链接/节点顺序、圆点列表、滚动和生成期间输入焦点；通过“复制完整回答 → 原生输入框粘贴”核对全文、表格、流程和题目保留。这里的流式按钮是明确标识的有限固定样例，不冒充模型流式。
- **真实模型内容：**讲解、对比、流程、整理、短追问与评价分别取得有效生成样本；真实对比表和流程图已送入客户端同一个解析器检查。这是生成组件验证，不能替代路由、搜索和完整 App 验收。首轮 Harness 三轮中出现多余澄清及 RT.MODEL.SCHEMA；生成组件也发生格式错误和有限重试，评价另行复测成功。全部尝试与失败保留在证据中，不宣称一次通过或供应商稳定。
- **真实 App 多轮：**独立 NativeQA / 18842 / 合成数据经原生输入发送两轮，分别 completed 69.137 / 46.650 秒。第一轮呈现圆点、对比表、可访问来源和检查题，但模型把请求的流程图推迟到下一轮；第二轮明确要求完整流程后，显示五节点流程、1–5 编号步骤和圆点注意事项。第二轮生成时滚到上文，完成后仍停在上文且输入框保持焦点。由此发现并修复后续追问让旧检查题在任务区重新出现、输入框重复完整题目的问题；检查题仍保留在历史原文中。
- **修复后重启：**同一隔离库恢复后仍为 4 条消息、原有 2 个完成 Run，表格/流程直接从历史显示；原生复查任务区不再重复旧题，输入框仅显示“输入回答或继续提问”。未重新生成历史内容。
- **开发构建交付：**Apple Development 签名 Debug 构建与严格签名验证通过，已打开 `/tmp/review-today-readability-signed-build/Build/Products/Debug/Review_Today.app` 正常窗口，8742 health 为 ok / deepseek。原“今天”页面的 8 张知识卡、4 个学习中任务及已有 RAG 会话保留，未发送日常测试消息。隔离真实模型 App 已退出，固定样例窗口已收起；这不是正式分发或发布。
- **剩余边界：**未完成 VoiceOver/Voice Control 实际操作、完整主窗口尺寸矩阵以及真实长回答期间所有滚动/切会话/恢复组合。模型仍可能偏长或选择不合适的教学节奏；本次渲染不会擅自截短、重写其知识内容。#17、M1 及 Rex 最终体验验收保持开放。

证据：[原生宽窗浅色](evidence/2026-09-06-answer-readability/rag-780-light.png)、[窄窗浅色](evidence/2026-09-06-answer-readability/rag-340-light.png)、[中宽深色](evidence/2026-09-06-answer-readability/rag-580-dark.png)、[真实模型分项记录](evidence/2026-09-06-answer-readability/real-model-results.json)、[原生真实两轮](evidence/2026-09-06-answer-readability/native-model-results.json)、[真实输出解析结果](evidence/2026-09-06-answer-readability/parser-results.txt)。

复跑：`bash tests/mac/run-answer-preview.sh --interactive` 打开独立原生样例，可切换六类内容、宽度、主题并预览有限流式；不会读取日常数据或连接模型。省略参数导出六张图，或传入导出目录。解析/重放回归沿用 `bash tests/mac/run-contracts.sh`；显式真实生成在 agent-service 内运行 `.venv/bin/python -m tests.real_answer_readability`，可用 `--case evaluation` 等只复测一项，会产生真实 API 用量。

> 主线合入与本次回归见 §0.19；启动修复见 §0.18，容量/悬停见 §0.17；顶部/运行入口及先前原生真实模型发送、流式、重启证据见 §0.15；DeepSeek 接入证据见 §0.13。旧章节保留历史；前批 HTTP 403 属于旧供应商，不是当前 DeepSeek 状态。分开记录代码、受控、真实模型与 Rex 验收。
> 对应 Issue：[#9 M1.1：冻结最小闭环验收契约与固定样本](https://github.com/RexYoung000/Review_Today/issues/9)
> 2026-09-04：单侧栏、开放式 Agent 工作区、Session 标签和 Today 活动反馈已完成代码实施、构建与受控检查；§0.9 仍是 Rex 的真实体验验收门槛，旧 §0.6 仅保留历史证据。
> 机器可读样本：`agent-service/tests/fixtures/m1_acceptance.json`
> 主规格：[Agent Harness V2](agent-harness-v2.md)

## 0.19 合入 main 与保留体验验收（2026-09-06）

Rex 明确授权“你先帮我合入 #17 吧”后，PR [#25](https://github.com/RexYoung000/Review_Today/pull/25) 已通过普通 merge commit `903b620` 合入 main。其内容与本次复核候选 `5794114` 完全一致；`m1/issue-17` 本地及远端分支已删除，当前仅 main、1 个工作树。分支和历史保留证据见 [M1 分支核对](m1-branch-audit.md)。本授权覆盖下方历史批次“暂不合并 PR”的限制；Issue #17、父 Issue #1 仍 OPEN，M1 未最终验收，M2 不启动，不发布。

- 本次无密钥服务回归 **194 项通过**，完整 Mac 契约通过（运行身份、焦点/输入、启动恢复、学习记忆、回放与 loopback SSE），旧磁盘 schema 迁移通过，macOS Debug / Release 构建通过。日志分别为 `/tmp/review-today-pr25-service.log`、`/tmp/review-today-pr25-contracts.log`、`/tmp/review-today-pr25-migration.log`、`/tmp/review-today-pr25-debug.log`、`/tmp/review-today-pr25-release.log`。
- 上述构建仅关闭签名检查编译，没有替换日常签名 App；既有真实模型与原生证据复用源码未变的 §0.13–0.18，本次没有另发真实模型请求或重做用户界面验收。
- GitHub Actions 候选 run `34022021651` 的任务因账户付款/消费额度限制未启动，不能报告远端 CI 通过。未改账单/保护规则，也未使用管理员绕过合并检查。
- #17 后续重点：完整五模式与多轮教学、独立作答、确认/拒绝入库；长流程交接/记忆/中断组合；Rex 原生视觉与交互验收。VoiceOver、系统 Reduce Motion、长期模型质量及两份历史数据的安全合并仍保留原有未验证/未实施状态。PR 合入只说明工程变更进入主线。

### 续记：验证在本机执行（2026-09-06）

Rex 选择当前不使用云端自动测试。停用 GitHub 的 `M1 controlled regression` 工作流并移除 `.github/workflows/m1-controlled.yml`；本地服务回归、Mac 契约、迁移脚本与测试样例全部保留。GitHub 的历史账单失败记录仍是真实历史，后续不再要求云端执行作为当前开发阶段的验证方式。

本次仅调整工作流与文档，应用、服务和测试代码无变化，沿用上方刚完成的 194 项服务测试、Mac 契约、迁移与 Debug / Release 构建结果，不重复运行整套回归。后续仍按实际改动范围完成本地验证和必要的原生交互检查。

## 0.18 开发服务启动修复（2026-09-06）

Rex 授权修复后，先修订 Harness §7.1 / §8 对应启动契约再实施。本批保持既有项目 `.venv`、模型配置和数据目录，不另起常驻代理、不扩大系统权限。

- 旧构建本次首次启动约 30 秒内恢复，采样已在导入组件；这证明故障存在间歇性，不证明前次权限读取阻塞不存在。前次 `getpath_readlines → open` 栈和 TCC AllFiles 预检不足以断言具体文件被拒绝。
- Debug 工程使用 Apple Development 与固定 Team，替代随代码变化的 ad-hoc cdhash 身份；CI 可显式关闭签名验证编译。冷启动预算改 90 秒；已就绪服务连续 10 秒不可达才恢复。解释器 / 组件 / 服务监听阶段分别记录，故障不直接展示原始进程输出；旧进程回调不能覆盖新启动。validation 同样呈现有限重试失败。
- 编译和完整 Mac 契约初轮通过：`/tmp/review-today-startup-unsigned-build.log`、`/tmp/review-today-startup-fix-contracts.log`。覆盖冷启动超过 30 秒不被杀、90 秒有界、健康短暂抖动与恢复重置、阶段不倒退、单次终止及既有输入/记忆/回放/SSE。最终签名构建与原生连续重启验证待完成。
- 首次证书签名被系统钥匙串阻塞：`errSecInternalComponent`，securityd 明确记录 codesign 的私钥使用未获允许 / `CSSMERR_CSP_USER_CANCELED`，证据 `/tmp/review-today-startup-signing.log`。已请 Rex 本机完成授权；不修改钥匙串 ACL、不获取密码、不用临时签名替代日常交付。
- 最后补齐 validation 失败投影后，全部 Swift 源码重新编译并通过定向启动契约：`/tmp/review-today-startup-final-contract.log`。从当前 Swift 提取实际 bootstrap 命令，使用空凭证、18749 与临时检查点启动 Python，0.95 秒取得 health ok / key_configured=false，两个阶段标记顺序正确，退出清理自己启动的进程；目录 `/var/folders/1f/qrpq14zs5l137bq24ls4qmbr0000gn/T/review-today-startup-smoke-0wycmb49`。这仅证明启动命令与服务导入，不代替 App 权限身份下的验证。
- 当前保留旧 `/tmp/review-today-256k-build/Build/Products/Debug/Review_Today.app`，原生检查「你好」问答及 1.3 秒完成记录完整、无服务错误横幅，8742 DeepSeek 三角色及流式 ready；未向日常会话发送测试输入。新版未替换日常 App，最终签名与真实收发/连续重启仍待授权后完成。

### 续验：签名授权完成与日常启动恢复（2026-09-06）

本段覆盖上方“新版待签名/未交付”的历史状态。Rex 在 Xcode 选择自己的开发团队（`7NDKLL3UJK`），随后完成本机钥匙串授权；保留该工程修改，不回退之前自动选取的团队。

- Debug 正常与独立 NativeQA 均使用工程里的 Apple Development 签名构建成功；`codesign --verify --deep --strict` 通过，designated requirement 为 bundle identifier + Apple 证书身份，不再绑定单次代码 cdhash。日志 `/tmp/review-today-startup-fix-build.log`、`/tmp/review-today-startup-qa-build.log`。只作开发构建，不是正式分发或发布。
- 退出旧 ad-hoc App 后，确认旧服务一起结束，新版自行启动 Python 子进程。随后正常 App 三次完整退出/重新启动，每次先确认 8742 无监听，health ok 用时 **1.92 / 1.91 / 2.11 秒**，每次为新的 App / Python 父子进程；结果 `/tmp/review-today-startup-restarts.jsonl`，复跑脚本 `/tmp/review-today-check-restart.py`。这三次未复现前次初始化阻塞，不代表覆盖所有系统权限/首次安装环境。
- 原生真实收发使用独立 bundle `com.rexyoung.ReviewToday.NativeQA`、18746、`/tmp/review-today-startup-native-validation`。通过原生输入区发送主动回忆解释问题，**3.2 秒**完成；App 展示实际 1,891 / 256,000、220,000 整理阈值。测试库不写入日常会话。
- 测试服务空闲时暂停 3 秒再继续，原 PID 不变、连接恢复；主动结束该测试子进程后，App **3.01 秒**自动启动新 PID 并恢复 health。2 条消息 / 1 个 Run 数量保持，未重生成。证据 `recovery.json` 与 `/tmp/review-today-startup-recovery.py`。恢复后继续原会话提问，**3.9 秒**完成，最终 **4 条消息 / 2 个 Run**，`result.json`；圆环显示 2,215 / 256,000。
- 已退出 QA 及其托管服务，保留 `/tmp/review-today-startup-fix-build/Build/Products/Debug/Review_Today.app` 日常构建与 8742 服务。原「你好」问答、1.3 秒记录及当前未发送草稿保留，发送按钮可用；未向日常会话投递测试输入。旧记录仍如实显示当时的 24K 上限，新请求才取得 256K。不重新迁移历史库、不关闭 #17/M1。
- 本轮复用上方源码无变化的 Mac 契约/无密钥冒烟结果，新增签名、原生启动/短暂中断/退出恢复/真实收发证据。代理未修改 TCC、钥匙串 ACL 或完整磁盘权限；本机钥匙串授权由 Rex 完成。

## 0.17 256K 活动上下文与即时悬停预览（2026-09-06）

Rex 确认 256K / 约 220K 整理 / 整理后 128–160K 方向后，先修订 Harness §24 与 DESIGN，再实施。随后 Rex 反馈系统 tooltip 没出现 AirJelly 式预览，改为原生 `onHover` 驱动的即时黑底白字短浮层；保留键盘预览及点击详情，不再依赖系统 tooltip。

- 服务默认活动输入上限 256,000、整理触发 220,000、目标 144,000；较小模型扣除 4,096 回答预留并同比缩放。计量继续明确为估算。API 输出预留参数贯穿 Responses、流式及兼容 fallback；模型替换和结构修复重新核算。
- 服务不再先取最近 16 条或截取每条 3,000 字符。较早已完成问答整组整理，最近完整一组与未完成输入保留；摘要覆盖的消息 ID、版本和引用依赖原子保存，ACK 不删尚未整理的原文。当前输入/任务/确认等保护字段不因达到整理目标而强行裁剪。传入长材料及 Mac 恢复上下文不再受原先 3,000 字符截断；单条请求文本结构限额提高为 512,000 字符，实际模型预算仍单独校验。
- **194 项无密钥服务测试通过**（19.950 秒，`/tmp/review-today-256k-tests.log`）：覆盖长消息末尾、超过 16 条历史、按 token 触发、整组处理、下一轮不重复整理、摘要失败/空/过长、并发补充/归档/停止取消、记忆失效、重启与 checkpoint/ACK、旧摘要兼容、小窗口分批、硬上限不静默删历史及输出参数。
- **Mac 构建与完整契约通过**：`/tmp/review-today-256k-build.log`、`/tmp/review-today-256k-mac.log`。包含真实 SSE/稳定重放/输入/记忆/容量口径；新增 hover 后定向契约再次通过：`/tmp/review-today-256k-hover-test.log`。当前 Xcode 构建 `/tmp/review-today-256k-build/Build/Products/Debug/Review_Today.app`。
- **真实模型合成长历史探针通过**：隔离 `/tmp/review-today-256k-live/checkpoint.sqlite3`，48 条合成历史触发一次摘要；随后问最早一节的纠正代号，正确答 MAPLE-7284（排除旧 PINE-1111），耗时 9.63 秒；回问最新一节答 EXAMPLE-23，耗时 5.84 秒。实际请求估算分别 135,831 / 136,040，均以 256,000 为活动上限；摘要版本一直为 1，原文加两轮问答后 52 条。完整探针与结果在 `/tmp/review-today-256k-live.py`、`/tmp/review-today-256k-live/result.json`。这是合成材料的链路与有限回问验证，不是长教材质量基准或最优窗口证明。
- **原生真实发送通过**：独立 `com.rexyoung.ReviewToday.NativeQA`、端口 18744、目录 `/tmp/review-today-256k-native`。CUA 发送「你好」完成 1.3 秒，收到 4,721 / 256,000 及 220,000 整理阈值；再从菜单选深入思考并发送合成解释问题，11.0 秒完成。独立结果 `result.json`，4 条消息；原生重启保留回答及容量，未重复生成。
- **短浮层原生可见**：重建后检查浅深色、指针进入后「约 2.1K / 256K」黑底白字圆角浮层、移开后消失、点击打开详情及 Escape 关闭。CUA 用坐标点击/右键使指针进入控件并截图确认（未以右键绑定浮层）；工具没有独立 mousemove，普通纯悬停手感仍待 Rex。未覆盖本轮 VoiceOver、系统 Reduce Motion 开关及所有窗口边界。
- **日常构建当前受启动环境阻塞**：已退出 QA 和旧日常 App，打开上述新 normal 构建，恢复「你好」及旧问答；没有向日常会话发送测试输入。旧请求仍如实显示其历史 24K 活动预算，新请求才使用 256K。日常后台 Python 再次卡在 `Py_InitializeFromConfig → getpath_readlines → fopen → open`，尚未进入业务代码；30 秒看门狗重试后仍不可用，采样 `/tmp/review-today-256k-normal-startup.sample`。没有修改系统权限、重置 TCC 或启动另一权限身份的常驻服务绕过；询问 Rex 是否看到系统访问提示。在线备份服务检查点 `/tmp/review-today-before-256k.sqlite3`。不能把这次日常重启报告为已恢复。

功能代码、受控与上述真实模型/界面检查已完成；正常服务恢复及 Rex 悬停/长资料体验验收仍未完成，不关闭 #17、不合并 PR、不放行 M1/M2。

## 0.16 会话图标、记忆状态与上下文圆环（2026-09-06）

按 Rex 本轮截图修订 DESIGN 与 Harness §23 后实施：顶部归档/恢复改共用图标按钮；输入区显示容量圆环及原生悬停帮助/可点击详情；会话列表和顶部增加记忆关闭标识；移除行菜单/右键菜单重复多选入口。保留既有多选栏、记忆策略版本、归档守卫、数据 schema 及本地 24K 输入限额。DeepSeek 官方 Flash/Pro 已补 1M 窗口元数据，旧供应商的窗口环境变量不串入 DeepSeek。

- Debug 构建通过：`/tmp/review-today-capacity-build.log`。
- 服务无密钥回归 180 项通过：`/tmp/review-today-capacity-service.log`。新增 provider/model 窗口隔离、未知型号与本地预算不扩大的断言。
- 完整 Mac 契约通过：`/tmp/review-today-capacity-contracts.log`，含容量分母/空态/未知/非法数值、既有记忆关闭/重新开启/引用失效、原生输入和 SSE 回归。最终悬停实现采用系统 help，定向 UI 契约复核通过（`/tmp/review-today-capacity-final-ui-test.log`）。
- 原生隔离预览 bundle `com.rexyoung.ReviewToday.CapacityQA`：观察归档图标无常驻文字，点击上下文详情显示约 5.5K / 1.0M 及 24K 输入预算；浅深色、窗口缩放均检查。顶部关闭记忆后列表和标题立即出现斜杠脑形，右键菜单变为允许跨会话记忆；重新开启后两个标识消失，右键菜单仅保留归档、标签、记忆操作。
- 预览所有数据仅在内存，发送禁用；这些圆环值为明确样例，不是真实模型用量。真实服务容量来源通过服务回归验证，本轮没有付费模型发送。旧会话缺 model_window 时显示上限未知；下次正常请求收到新元数据后更新，不回填推测值。
- 已关闭隔离预览，打开 `/tmp/review-today-capacity-build/Build/Products/Debug/Review_Today.app` 正常构建并恢复「你好」会话；原有问答与 1.3 秒完成状态保留，没有发送新消息。当前历史记录显示约 4.7K / 上限未知，符合旧记录降级契约。随后正常服务启动遇到下述外部阻塞，不能报告日常运行交付完成。
- 新开发构建正常启动服务失败：health 8742 未响应；Python 采样卡在初始化读取环境路径文件的 open，尚未进入服务业务代码，`/tmp/review-today-service-startup.sample`。系统 TCC 日志同期出现该构建的文件访问预检（AllFiles preflight=0），不足以断言具体哪一项权限必须扩大；未修改系统权限或另起有更高权限的服务绕过。已停止失败的新构建并恢复原构建 `/tmp/review-today-runtime-product-build/Build/Products/Debug/Review_Today.app`；旧构建同样达到启动重试上限、health 仍未响应。只读检查系统设置发现 Review_Today 的“桌面”文件夹开关已开启，不能把此次阻塞简化为未授权；没有切换权限、重置 TCC 或扩大磁盘权限。最终留在旧构建的「你好」会话，问答保留，但服务未恢复，新 UI 尚未完成日常构建交付。
- 当前不声称完整体验验收：系统悬停 tooltip 的指针停留、键盘全路径、系统 Reduce Motion/VoiceOver、精确最小窗口尺寸与长会话压力未完成本轮端到端验证；现有证据为连续 AX/截图而非录屏。最终视觉与交互待 Rex。
- 分支核对与分类见 [M1 分支清单](m1-branch-audit.md)：1 个工作树、9 条本地分支，8 个 issue PR 均未合并，本次无可按完成条件直接删除的分支；不关闭 #17/M1。

### 续验：正常运行恢复与真实容量（2026-09-06）

本记录覆盖上方“日常构建交付未完成”的历史状态，保留原排查过程。本次未改服务启动代码、系统权限或模型配置。

- 12:34 左右重新启动 `/tmp/review-today-capacity-build/Build/Products/Debug/Review_Today.app` 正常构建后，App 自行托管 8742 服务；health 为 ok / deepseek，router/coach/risk 的 smart/deep 均 ready，coach 流式 ready。正常会话「你好」原问答与 1.3 秒耗时保留，无预览标识、无测试消息。前台现为新版，不再回退旧构建。
- 在本轮代理开始前后，系统日志新增 12:32:36 的 Review_Today DesktopFolder 授权记录，随后读取项目及服务启动恢复。只能确认环境授权状态发生变化、重启后恢复，不能把上一轮 AllFiles 预检为 0 当作必须开放完全磁盘权限的证据；代理未切换权限、重置 TCC 或将服务移到更高权限进程。
- 新建独立原生验证目录 `/tmp/review-today-capacity-native-validation`，bundle `com.rexyoung.ReviewToday.NativeQA`、服务 18743，沿用 App 的 validation 模式。通过 CUA 在原生编辑器发送合成输入「你好」，实际回答完成 1.2 秒。收到并落盘 input_tokens=4721、model_window=1000000、input_budget=24000、output_reserve=4096；圆环按 0.4721% 绘制，不把服务的输入预算 ratio≈19.67% 当作模型窗口占比。
- 原生关闭跨会话记忆后，列表和标题显示斜杠脑形；退出并重新启动同一隔离库，标识及 1.2 秒完成状态仍在。右键菜单只含归档、编辑标签、允许跨会话记忆；重新允许后持久化为 allowed=1、policy_revision=2。全程保持 2 条消息、1 个 Run，重启未重复生成。独立结果见 `/tmp/review-today-capacity-native-validation/result.json`。
- 已退出隔离测试窗口与其托管服务，保留正常构建和正常 8742 服务；正常旧记录继续显示“上限未知”，新请求已证实能取得 1.0M 上限，不对历史记录猜测回填。
- 本次复用无源码变化的构建与上一轮 180 项受控/Mac 契约结果，新增上述真实模型、磁盘重启和日常运行证据。系统鼠标悬停 tooltip 已提供 `.help`，点击详情已原生验证；指针停留的视觉结果单独等待 Rex 确认。VoiceOver/系统 Reduce Motion 与完整 M1 验收仍未覆盖。

## 0.15 紧凑顶部与真实运行入口（2026-09-06）

状态：独立文档检查点 `b9e0f77` 已先提交并推送，随后实施紧凑顶部、运行身份与投递反馈修复。前一版 FocusFixQA 为内存 fixture，关闭消息同步却仍可发送；「你好」只进入内存 outbox，假 running 样例持续计时。这不是 DeepSeek 403；本次未清空真实数据库。

计划阶段隔离 Harness 实测：现有 Flash 寒暄完成 1.717 s；RAG 首公开内容 2.066 s、完成 5.464 s、38 个正文片段，无错误。仅验证合成输入的 Harness，不是原生 App 发送/SSE/重启证据，也不表示本轮已经修复。

本轮必须验证：

1. normal/preview/validation 的存储、网络和窗口身份一致；preview Return/发送不能创建 Session/消息/outbox，不报告持久保存；固定耗时及有限动效可停止。
2. 正常发送、保存失败、错误响应缺少/无效 Run ID、稳定 ID 重试、重放/停止/归档和上一轮单会话焦点隔离回归；不增加数据库 schema。
3. 原生真实模型隔离库从空会话发送你好/RAG：本地回显→服务接收→真实回答/流式→完成；重启同一隔离库后历史和消费位置恢复，不重复调用或计时。
4. 顶部无重复标题/空标签行；无标签、多标签、长标题、清单展开、浅深色和 760×620/默认/宽窗均可操作；保留焦点/滚动。
5. 交付前正常 App 的已有会话/知识/复习数据保留，无界面样例或演示横幅。旧测试输入「你好」仅保留为未发送草稿，先保存再退出旧预览；不自动提交模型。

### 本轮实施与验证记录

- 先取得红灯：新增 `AppRuntimeContractTests` 在旧实现断言「预览不得报告 ready」失败，`/tmp/review-today-runtime-red.log`。修复后另捕获投递保存失败时观察对象 Run ID 残留，补充回滚后的对象状态恢复。无效/缺失 Run ID 不再静默丢弃响应，也不自动重复提交。
- 本轮源码入口：`AppRuntime`、`MessageDeliveryPresentation`、`ConversationProcessor.recordAcceptance`、`LearningWorkspace.workspaceHeader`。没有新增 SwiftData 字段、生产数据迁移、业务 API、供应商或模型档位。
- 全量无密钥服务回归 179 项通过，`bash agent-service/run-controlled.sh`；日志 `/tmp/review-today-runtime-service.log`。Mac 契约覆盖预览发送拦截、真实隔离磁盘、首发/投递保存失败、稳定 ID 与重复响应、归档/held 不可重试、投递文案、静态样例以及原有焦点/输入/草稿/事件/记忆回归；入口 `bash tests/mac/run-contracts.sh`，日志 `/tmp/review-today-runtime-contracts.log`。中间重编曾因正在编辑源码而退出，不计为测试通过，以最终完整运行记录为准。
- `bash tests/mac/run-migration.sh` 通过（`/tmp/review-today-runtime-migration.log`），是既有磁盘 schema 的隔离回归，并非把用户两份历史数据库合并。Debug 正常/NativeQA 和 Release 构建通过，日志分别为 `/tmp/review-today-runtime-product-build.log`、`/tmp/review-today-runtime-build.log`、`/tmp/review-today-runtime-release.log`。初轮 Release 因调用仅 Debug 存在的方法编译失败，已补条件编译后重建；Release 不启动预览，不代表签名分发或发布。

### 原生真实模型链路（不是脚本替代）

在 `com.rexyoung.ReviewToday.NativeQA` 中，通过 Computer Use 点击 Agent、原生输入框粘贴并按 Return。使用 `/tmp/review-today-native-LjL9wq/app.store` 和同目录 `checkpoint.sqlite3`、本机 18742 服务；窗口底部明确显示「真实模型隔离验收」，无样例种子。App 自动托管现有 DeepSeek，health 显示结构化/正文流式可用。

| 实际输入 | Run 结果与模型计时 | App 持久化/呈现观测 |
|---|---|---|
| 你好 | completed，1.325 s；不创建学习目标 | 本地回显记录 52 ms；回答首段从收到到显示约 58.5 ms |
| RAG 是什么？解释检索与生成、公司报销例子和两个易混点 | completed；首正文 2.483 s，总计 9.496 s | 本地回显记录 50 ms；保存 1 ms；首段从收到到显示约 43.2 ms；77 个真实正文片段 |

连续 AX 观测到「等待发送→正在理解本轮意图→正在准备回答」、未闭合答案的「正在输出 · 尚未完成校验」，随后同一条回答收敛为「已完成 · 9.5 秒」。编辑器保持焦点，没有等待全文后模拟打字。以上 App 计时是已有埋点，不是外部摄影测得的显示器端到端 SLA；复杂回答与计划阶段短提示不能直接作性能前后对照。

退出再启动同一隔离库，通过侧栏重新打开会话，两个 Run ID 保持 `9f08ffef-5995-4866-a2db-4031ee206b20`、`f5ef5f07-8f39-4ceb-b018-78f3309323fa`；仍为 4 条消息、2 个完成 Run，耗时不重计。Mac 消费位置 95，服务 ACK 95；正常重启会重新检查模型能力，但没有重新生成历史答案。该隔离库中 Knowledge/ReviewAttempt 均为 0。

### 原生界面与预览

- 确认单条会话标题、无空标签行，无标签弹层与 4 个标签编辑/重开。顶部只移除工具栏标题，系统关闭/最小化/缩放仍在；初轮发现隐藏整条 toolbar 会移除窗口控件，已修正并复验。
- 实际缩放宽窗/约 1100 宽默认窗/760×620 最小内容区（含系统标题栏的截图约 760×650），长标题截断并有全文帮助，按钮保留；侧栏窄窗变图标栏。浅深色均检查。顶部标签/更多改向下展开，输入区选择仍向上；方向键与 Escape 不误执行新会话/标签/记忆操作。
- `REVIEW_TODAY_M1_UI_FIXTURE=learning` 新预览窗口：输入「预览输入，不应创建消息」，Return/⌘Return 均未新建消息或 Session，文本保留，发送按钮禁用；列表仍只有四个内存样例。预览运行不启动本地服务。最后构建切到知识库，底部仍显示「界面预览 · 仅内存数据，不连接模型」，不因离开 Agent 页面丢失测试身份。
- 讲解样例固定 4.6 秒；显式启动 8 秒动效后自动恢复「预览动效（8 秒）」按钮，计时不增长。学习安排展开/收起、步骤文字和固定输入区可见；这些样例只证明布局，不证明学习闭环。

### 正常窗口交付与数据注意事项

正常 bundle `Rex.Review-Today` 从 `/tmp/review-today-runtime-product-build/Build/Products/Debug/Review_Today.app` 启动，未设置任何测试环境变量；health 8742 返回 DeepSeek ready/流式 ready。起始页无预览横幅/样例，新草稿原本为空，已把 Rex 的「你好」保存进去，**没有发送**。只在确认正常 `AppSettings` 草稿落盘后退出旧 FocusFixQA，让内存样例自然释放；没有执行数据删除。

最终正常构建再次退出/启动，默认回到今天；点击 Agent 仍恢复「你好」草稿。数据库保持 9 个 Session/35 条消息，未因草稿恢复新增会话或回答；样例 Session 数为 0。最终完整 Mac 契约退出码 0，受控 SSE 最大接收延迟约 26 ms；正常 App 留在前台供 Rex 验收。

只读核对发现一个**既有历史数据分流**：当前无沙盒开发构建使用 `~/Library/Application Support/default.store`（9 个历史 Session、35 条消息，无样例）；旧沙盒库 `~/Library/Containers/Rex.Review-Today/Data/Library/Application Support/default.store` 仍有 8 张知识卡、3 条 ReviewAttempt。两份库均未删除/合并，本轮未改默认存储路径。旧卡在当前开发版并不可见；不能将「文件保留」写成「历史卡已迁移展示」。需要单独确认去重/目标库与备份恢复方案后迁移，未擅自实施。

未覆盖：系统级 Reduce Motion/VoiceOver、全部输入法真实候选选词、每一种网络断点及完整五模式人工验收。本轮受控输入测试包含组合文本保护，但不冒充实际中文候选操作；截图/AX 连续观测不是完整录屏。模型答案措辞质量也不由一次链路成功担保。当前可报告本轮代码、受控回归和上述原生链路通过，最终视觉/动效舒适度仍待 Rex；不关闭 #17/M1，不推进 M2。

## 0.14 Agent 交互精修（2026-09-05）

### 2026-09-06 会话焦点描边回归

Rex 截图与隔离 App 原生读取均复现：四行都有青绿色描边，但 AX 只标记一个会话 selected，未出现多选数量/操作栏。共享按钮样式读取了可从列表容器继承的 `isFocused`，导致视觉焦点与真实选择不一致。文档检查点 `549205c` 已先独立提交并推送；随后将自定义描边改由会话行、菜单入口与菜单选项自己的显式焦点驱动，不改多选入口、归档权限、模型或用户数据。

本次门槛：普通点击仅当前会话有选中底色；Tab/方向键焦点至多落到一个控件，未激活不导航；输入框获焦后会话焦点描边消失；显式多选才显示勾选/数量，退出恢复单会话导航。菜单入口/选项共用同一隔离规则，其他图标按钮不继承祖先焦点作为自定义描边。

本次实际验证（不是前批结果复用）：

- 失败先行：新增 `InteractionFocusTests` 在旧代码上因祖先焦点检查失败，日志 `/tmp/review-today-focus-red.log`。修后通过架构守卫及 SwiftUI 实际渲染取样，覆盖浅/深色、选中与焦点落在不同行、无焦点、禁用状态；不以选择 ID 正确代替视觉检查。
- 完整 Mac 契约入口 `bash tests/mac/run-contracts.sh` 通过，涵盖新增焦点、服务监控、原生输入/预填/撤销、首发原子保存、草稿/偏好、事件回放、停止/归档版本、学习记忆/评分隔离及 SSE。日志 `/tmp/review-today-focus-contracts.log`；受控 SSE 最大接收延迟 25 ms，仅为消费路径计时。
- Debug 构建通过，`/tmp/review-today-focus-build.log`。隔离 bundle `com.rexyoung.ReviewToday.FocusFixQA`，构建目录 `/tmp/review-today-focus-fix-build`，以 `REVIEW_TODAY_M1_UI_FIXTURE=learning` 启动；内存数据、禁用模型服务。
- 原生浅/深色：点击讲解/练习会话时仅当前行有选中底色，AX 只报告一个 selected，右侧准确切换；不再出现四行青色边框。编辑器获焦时列表无焦点描边。
- 原生键盘：列表 Tab 到「空状态」时只有该行描边，已打开的「讲解中」仍保留选中底色，右侧未切换；学习方式菜单上方向键只移动焦点，不改变已选模式，Escape 关闭并保留「问题攻克」。
- 原生多选：工具栏主动进入后才出现勾选/数量；选择一行显示 1，⌘A 显示 4，Escape 退出后恢复原会话单选，无残留批量状态。本次未执行归档/删除。
- 证据为本轮 Computer Use 的连续操作、AX 与截图，非完整录屏。本轮未复测系统 Reduce Motion、实际中文选词、全部窗口尺寸或真实模型；没有修改模型配置、SwiftData schema 或用户知识库。以上只确认本次焦点回归修复，不放行 M1/#17/M2。

前批基线：文档检查点 `d908711` 已先独立提交并推送，随后完成 UI 增量代码及受控验证；当时原生初轮取得部分证据后锁屏。本次仅补验上方列出的焦点/多选/菜单路径，**不报告整体验收通过**。实施入口为 Harness §21；§0.12 的四项快捷开始、旧侧栏及历史截图只代表前批。

| 场景 | 必须观察到的结果 | 证据 |
|---|---|---|
| 单层菜单 | 模式/强度一次打开直达、图标说明/选中态、方向键/Return/Escape，偏好保持；关闭不抢焦点或丢选区 | 原生 + 契约 |
| 刷新/预填 | 默认六项、组内无重复/不连续同组；刷新不改草稿/偏好/建会话；未编辑模板切换，编辑后末尾追加，选区保护、连点去重、撤销及组合输入安全 | 契约 + 原生 |
| 侧栏 | 展开/图标栏、会话入口恢复原列表位置，准确页面/会话选中，窄窗不覆盖人工偏好，重启宽度/草稿恢复 | 契约 + 原生 |
| 会话行 | 至多一个人工优先标签，无普通完成勾/Auto/状态句；hover/聚焦更多菜单不挤标题；单个/多选归档及恢复继续守版本 | 契约 + 原生 |
| 共用反馈 | 导航/会话/工具栏/详情的 hover、按下、选中、焦点清楚；卡片放大不改布局，非交互容器无误导动效 | 原生录屏 |
| 回归 | 输入/首发失败/草稿、停止/归档、SSE、正式评分及五模式不回退 | 现有受控入口；真实模型另记 |

原生需最小 760×620、默认及宽窗，浅深色、Reduce Motion、中文选词/Return/Shift Return、长标题和回答、上翻阅读与菜单关闭。仅设置环境值不等于系统设置或 VoiceOver 已验证；记录实际输入路径和未覆盖项。固定内存 fixture 禁用服务，不作为真实 Harness 成功；真实模型用隔离数据，不对用户知识库做破坏测试。

### 本批实现与证据

- 本地实现：`InteractionChrome.swift` 统一交互反馈、单层原生弹出选择和侧栏适配规则；`ContentView.swift` 增加图标栏及会话更多菜单；`AgentComposerStore.swift` 保存本地场景与预填规则；`LearningTextInput.swift` 经原生插入/撤销处理模板，等待组合文本结束后执行，读取最新待执行选择。
- 模板来源仅存在于当前编辑器；切页/重新载入无法证明来源时按用户草稿保护。没有修改 SwiftData 模型、服务接口、供应商、密钥或正式品牌。非交互统计使用静态视图，不再用禁用按钮使数据变灰。
- 先取得失败测试：十二项场景断言在原四项代码上失败（`/tmp/review-today-ui-refine-red.log`），随后补齐实现及回归。

| 检查 | 本次实际结果 | 可复跑入口/本机记录 |
|---|---|---|
| 无密钥服务回归 | 179 项通过，涵盖现有学习、权限、归档/停止、来源、恢复和 provider 契约 | `bash agent-service/run-controlled.sh`；`/tmp/review-today-ui-refine-service.log` |
| Mac 契约 | 输入、首发原子失败/重试、模式/草稿、标签、归档/旧版本隔离、记忆/评分隔离及 SSE 通过；新增十二项/六张刷新、模板替换/选区保护/连点、原生撤销、组合输入延迟、旧 Session 取消待插入、侧栏人工偏好及迟到焦点恢复守卫 | `bash tests/mac/run-contracts.sh`；`/tmp/review-today-ui-refine-contracts.log` |
| 旧数据库迁移 | 通过；旧会话、来源、知识、复习及草稿未回退。本轮未加字段，作为迁移回归而非新迁移功能 | `bash tests/mac/run-migration.sh`；`/tmp/review-today-ui-refine-migration.log` |
| 编译 | Debug 与 Release 构建通过；Release 仅是编译，不是签名分发或发布 | `/tmp/review-today-ui-refine-product-build.log`、`/tmp/review-today-ui-refine-release.log` |
| 原生初轮 | 独立 bundle `com.rexyoung.ReviewToday.UIRefineQA`，`REVIEW_TODAY_M1_UI_FIXTURE=learning`，内存数据且禁用服务；看到单行品牌、简化会话、底部主题、六张带说明卡；鼠标一次打开五模式、选择资料学习成功；概念卡填入、资料卡替换未编辑模板成功，没有新增 Session，编辑器重新聚焦 | 本轮 Computer Use 的 AX 状态及起始页/菜单截图；不是完整录屏或真实 Harness 证据 |
| 原生发现/未通过（前批记录） | 初轮方向键未进入弹出选项；已补显式焦点与延后聚焦，当时构建/契约通过但修后因锁屏未能复验。2026-09-06 已补上方向键移动/Escape/模式不变的原生证据；其他菜单路径仍待下方清单 | 当前范围见本节焦点描边回归记录 |
| 真实供应商 | 本轮没有重新调用真实模型；保留 §0.13 的 DeepSeek 历史样本，不重复登记为本轮通过 | 配置未改变 |

受控计时只证明 AppKit/存储和 SSE 消费路径，不是屏幕端到端测量；具体值保留在本批契约日志。不能据此宣布本地回显/所有真实生成都达成 SLA。

整体精修尚待补齐：菜单 Return 确认、选区与外点焦点；图标栏收展/滚动位置/宽度与跨启动恢复；最小/宽窗口；会话更多菜单/归档撤销；实际中文输入法选词、编辑后追加/撤销/刷新不改草稿；系统 Reduce Motion；长回答上翻及连续 hover。上方已补验的焦点、多选/全选/退出及浅深色不再列为未做，但不代表上述全部路径通过。动效舒适度、最终视觉和学习闭环由 Rex 验收；#17、M2 不放行。

## 0.13 DeepSeek 官方接入（2026-09-05）

- 已确认：Rex 明确要求接入。官方 `/models` 使用其提供的凭证返回 HTTP 200，可见 `deepseek-v4-flash`、`deepseek-v4-pro` 和视觉实验模型；本轮只用前两种文字模型，不启用视觉。
- 文档检查点 `e77569b` 已独立提交推送，随后实施。目标映射和兼容规则见主规格 §20。
- 已实现：独立 provider/凭证命名空间，DeepSeek 固定官方域名、Flash 路由/教学与 Pro 风险判断；system 角色、智能 none/深入 high、等价 schema 引用展开、明确 JSON 输出约束、最终本地校验及推理文本过滤。旧配置未删除，备用名单仍为空。
- 受控验证：179 项服务测试通过（`/tmp/review-today-deepseek-controlled-final.log`），新增覆盖凭证/域名隔离、旧配置兼容、未知 provider 拒绝、两种强度、嵌套可空 schema、真实增量、最终结构拒绝、非搜索回答不得充当证据，以及未授权音频调用拒绝。Mac 契约入口通过（`/tmp/review-today-deepseek-mac-contracts.log`），包括新 health 扩展所兼容的服务状态、输入/草稿、重放/停止/归档/恢复与 SSE；没有 SwiftData schema 修改，不新报一次迁移验证。
- 真实启动：关闭先前不调用模型的界面 fixture，打开正常开发 App；手动服务退出后 App 自动托管新 Python 进程（本次 PID 48125，父进程为 App 48014）。`/healthz` 显示 provider=deepseek，三个角色及 smart/deep 结构化 ready，coach 两种强度 streaming ready。原生 Agent 起始页已无演示标记、403 或连接失败横幅。没有在真实用户会话中提交测试消息。

修复后隔离 Harness 真实单次样本（输入均为固定合成材料，不写入真实知识库）：

| 场景 | 首段可见 | 总耗时 | 正文片段 | 结果 |
|---|---:|---:|---:|---|
| 智能寒暄 | 2.025 s | 2.031 s | 0（路由轻量回应） | 本轮完成，无学习目标 |
| 智能 RAG 解释 | 2.475 s | 5.996 s | 34 | 本轮完成，无训练目标/保存动作 |
| 资料学习第一步 | 2.706 s | 8.290 s | 48 | 本轮完成，目标等待用户继续教学 |
| 问题攻克首轮 | 3.912 s | 15.669 s | 20 | 本轮完成，目标停在校准，未认定掌握 |
| 深入 RAG 解释 | 29.865 s | 31.523 s | 18 | 本轮完成，high 未降档 |

样本记录：`/tmp/review-today-deepseek-real-final.jsonl`、`/tmp/review-today-deepseek-deep-rag-fixed.jsonl`。首段表示服务公开事件产生时间，不是 Mac 屏幕渲染计时。公网查证单独通过：Pro 有真实 completed 搜索调用及非空论文出处，最终适配调用耗时 4.760 s。

实测发现及边界：初次复杂 schema 出现 HTTP 400（anyOf 内引用缺 type），随后还出现代码围栏/纯文本导致 SCHEMA 失败；已保留回归并修复，不剥围栏伪装成功。强制每轮仅选择 web_search 会只产工具输出，已改为允许最终回答、但强制校验实际搜索完成记录。Pro 深入流式探测曾约 90 s 超时、首次搜索 30 s 超时；后续结构化/coach 流式和搜索已通过，不抹去供应商偶发慢请求，也不宣称长期稳定。深入 RAG 样本取于 JSON 提示补充前已通过的构建，最后启动探测使用最终适配代码。
- 保留边界：不自动重试历史失败、不改复习数据、不默认启用旧供应商备用；接入成功不代表五模式完整学习验收或 Rex 视觉验收通过。

## 0. 当前阻塞门槛

### 0.12 全量收敛与 Agent 起始页（当前新增门槛）

承接 §0.10 剩余项及 §0.11 全部待实施项。文档检查点后才改功能代码；旧 132 项测试不证明新功能。本次初始状态均为 proposed。

文档检查点 `2a69931` 已独立推送后开始功能修改。全局 AGENTS 已在本机修订并保留原件/差异，不在项目 Git 中；本轮未修改系统 Skill、供应商配置或正式品牌。

可靠性/记忆代码提交为 `68edd71`；Agent 界面与本节证据回写在后续独立 `feat` 提交，不把这两批算成 Rex 已验收。全局 AGENTS 原件与差异在 `/Users/rexyoung/.codex/backups/AGENTS-before-m1-1788606327455.md` 及同名 `.patch`，不随项目推送。

#### 2026-09-05 本轮实现与受控证据

- **代码**：独立 Agent 起始草稿、首次发送原子建会话、四项仅编辑快捷开始、跨页会话区、双控制/卡片选择、默认浅色、容量估算、原生 DotsRing 与选择动效已实现。新增版本化学习证据/本地索引、排除与传递依赖门禁、恢复增量、稳定步骤 ID、来源刷新历史/混合来源、有限预算与结构修复。仍复用既有五模式、归档、知识 claim/ACK、清单、小结与 Today。
- **服务自动测试**：`bash agent-service/run-controlled.sh` 的 163 项通过（8.154 秒，隔离目录 `/tmp/review-today-python-tests.3E9LUV`）。新增坏例覆盖首次计划自拟 ID、来源刷新/重试快照、混合资料、公开搜索最小化、记忆排除后上下文/摘要不回流、后续引用继承依赖、卡片版本失效、未知引用不授权、思考强度/结构修复/有限备用、共享时限和入库模型故障分类；重复失效通知不能取消后续新决定。不是五模式真实模型质量证明。
- **Mac 契约**：四个测试入口通过，涵盖服务状态、中文 marked-text/Return/撤销等输入契约、首发失败回滚与同 ID 重试、空会话隔离/偏好、消息重放/停止/归档/重复/乱序、连续恢复，以及本地记忆索引/排除/旧引用拦截/评分隔离。最终 `/tmp/review-today-contracts.8nPIHl` 中停止本地保存 0.77 ms；loopback SSE 三段 Unicode 的最大接收延迟约 30 ms。另覆盖迟到停止回执不得清除后续新决定。只证明受控保存与接收，不证明完整 UI 100/200 ms。
- **迁移**：`bash tests/mac/run-migration.sh` 从 `e89a85c` 构造旧磁盘库，再用当前模型读入，保留会话/草稿/来源/知识/正式复习 ACK 与评分；新思考/记忆字段安全默认。最终模型结构复测目录 `/tmp/review-today-migration.9unMaX`，未操作用户库。
- **构建**：当前 Debug 无签名 `xcodebuild` 成功，构建产物 `/tmp/review-today-m1-convergence-build/Build/Products/Debug/Review_Today.app`。旧 NSSpeechSynthesizer 弃用警告保留，未因此扩展语音范围。
- **原生阻塞**：本轮隔离 `REVIEW_TODAY_M1_UI_FIXTURE=learning` 使用内存库并禁用服务。工具报告 Mac 锁屏，已请求 Rex 解锁；不能复用 §0.10 旧截图证明新导航、真 IME、多选、窄窗、滚动、焦点和动效通过。

#### 本轮真实供应商记录（合成输入、临时库、禁止知识提交）

未改 Luna/Terra/Sol 或供应商。下表是单次样本，不是统一完成时限或稳定 SLA。

| 场景 | 首个公开事件 | 总耗时 | 结果 |
|---|---:|---:|---|
| 纯寒暄 | 5.54 秒 | 5.55 秒 | 完成，只有 Luna |
| 普通 RAG 问答 | 64.66 秒 | 72.51 秒 | 完成，68 个真实增量；首段仍慢 |
| RAG 资料第一段 | 35.29 秒 | 46.53 秒 | 完成，73 个真实增量；不是完整资料闭环 |
| 问题攻克初始样本 | 55.73 秒 | 91.51 秒 | 87 段预览后，首次计划自拟 ID 被旧校验拦截；已受控修复 |
| 修复后问题攻克复验 | 无 | 5.26 秒 / 0.61 秒 | 两次分别连接错误、上游拒绝；未取得修复后的成功样本 |
| 深入思考寒暄能力复测 | 无 | 0.49 秒 | HTTP 403；强度字段未降档，真实能力尚未验证 |

原始复验日志位于 `/tmp/review-today-real-problem-convergence.jsonl`、`/tmp/review-today-real-problem-retry.jsonl`、`/tmp/review-today-real-deep-capability.jsonl`（临时诊断，不是永久材料源）。可复用 `python -m tests.stream_real_smoke --case … --follow-up … --deep`；它不在无密钥自动发现测试中，不拿用户知识库做验证。

#### 尚不能放行

1. 新 Agent 页面、实际中文输入法、原生多选/布局/滚动/动效/精确旧记录跳转需解锁后真实检查，再由 Rex 验收。
2. 完整五模式、主题探索的真实互补来源、JD 选题、独立作答/迁移评价及拒绝保存闭环仍缺本轮真实成功证据；供应商恢复后优先复测问题攻克计划修复。深入思考不能仅凭参数测试报告能力通过。
3. 本地记忆关联已受控验证，语义相关性、教学措辞与真实引用质量待模型及原生联合验证；弱词面匹配用少量近期候选补充，不宣称具备完整语义向量召回。
4. 内部备用引擎只验证受控分支，实际合格名单为空；没有获准且验证完成的备用时诚实失败，不用换供应商绕过当前 403。
5. 端到端 UI 性能、真实断网/重启/归档与长回答交互仍待原生检查；OneWorks 正式采用等 Rex 选择，语音不启用，#17/M2/发布不放行。

| 组 | 预期与禁止行为 | 验证 |
|---|---|---|
| 协作规则 | 只审查不改；明确修复连续推进；显式文档 push 不跳过；危险操作仍批准；阻塞分范围且不虚报 | 原件/差异及规则场景审阅 |
| 四入口 | 今天默认，Agent 起始，库/待处理准确切页，常驻 Session；不能进入 Agent 就自动打开历史 | 导航契约 + 原生 |
| 起始草稿 | 反复 +/切页/重启保存草稿，无空会话；首发原子保存、失败保留、同 ID 不重复 | SwiftData 故障/迁移 + 原生 |
| 快捷开始 | **由 §0.14 修订数量/预填**；仍保留用户编辑、可撤销，不调用模型/发送/改变偏好 | 原生输入契约 + UI |
| 偏好 | 新草稿 Auto/继承思考强度，首发继承所选，历史各自恢复，故障不降档 | DTO/迁移/真实能力 |
| 恢复补缺 | 游标领先快照仍可合成连续恢复；缺口/冲突明确拒绝，不模型执行/入库；稳定步骤改名和刷新来源 | 服务/Mac 并发与恢复回归 |
| 已有能力 | 输入/IME、草稿、SSE、授权、归档、学习/来源、清单、小结、热力图、正式评分不回退 | 原回归 + 新 badcase |

原生路径：启动今天 → Agent 输入未发送内容 → 切其他页返回 → 插入快捷内容并撤销 → 首发得到准确新会话 → 再进 Agent 与历史分别恢复各自草稿/偏好。另测运行中切页不停止、显式停止/归档冻结、撤销不续跑及所有 §0.11 场景。最小 760×620、默认/宽窗、浅深、Reduce Motion 与键盘/真实中文输入法都必须留证。

最终分别记录文档、代码、受控、真实模型、原生及 Rex 验收。全局 AGENTS 本机修订不冒充随项目 push；系统 Skill 未改。OneWorks 候选不重做，正式接入等选择；听写/语音只固化后续方向。#17/M2/正式发布不自动放行。

### 0.11 学习记忆、双控制与内部可靠性（有效验收要求）

本节来源为本轮 Rex 逐项确认的产品/设计决定及 Harness §18；不替代 §0.10 未完成项，也不关闭 #17、启动 M2 或接入正式图标。语音只是后续方向，不计入当前文字/公开链接 POC 完成率。

#### 文档冻结时基线（历史，不是当前状态）

- 67e8760 为本轮文档更新前代码基线。§0.10 的 132 项服务回归、原生契约/迁移与部分界面检查是此前记录，本次未重跑；它们不能证明下表新增能力。
- `ConversationProcessor` 现把字词匹配后的最多五条知识标题/目标传给服务，`Models` 有卡片/步骤/ReviewAttempt；没有证据证明类型化跨 Session 学习记忆、排除策略和掌握状态关联已实现。
- 当前仍有独立学习导航和标题栏模式选择；思考强度/容量提示/按需多选及 DotsRing 原生反馈待实施。Rex 只确认了两种动效的方向；浏览器预览未接音频或模型，不是原生验收。
- 本回合未确定或启用任何备用模型/语音供应商。历史原供应商 403 不是当前探测结果；是否恢复、候选模型是否支持所需结构/流式/强度必须另测，不能用模型清单或少量探测替代五模式真实闭环。

#### 验收要求（逐项证据以 §0.12 为准，表格本身不是通过记录）

| 验收组 | 必须验证的正向与拒绝行为 | 证据类型 |
|---|---|---|
| 首用与导航 | 无设置时浅色/今天；已有人工深色不被覆盖；四个主入口、跨页会话区、独立滚动及准确会话/草稿定位 | 自动状态测试 + 原生 |
| 会话选择 | 搜索/归档视图/+ 同行且键盘可达；⌘/Shift/可发现多选入口、全选/反选/批量归档；运行中归档、部分失败、撤销与迟到隔离 | 自动 + 原生 |
| 双控制 | 新会话 Auto；强度继承最近选择；旧 Session 各自保持；发送/重启/内部切换不降低深入思考；两按钮不充当权限 | 契约/迁移 + 真实模型 + 原生 |
| 材料与容量 | 文字/公开链接/主动选卡引用不自动保存或开课；预算与实际构建上下文一致，估计注明；临界/超限/未知容量不伪造百分比 | 固定预算与受控模型 + 原生 |
| 压缩与恢复 | 旧摘要不得覆盖新目标/纠正/授权；完整历史仍可回查；成功回答不因摘要失败转失败，压缩不改强度或草稿 | 故障注入/版本并发 |
| 记忆证据 | 分别构造只讲过、自述、独立通过、用提示、正式复习答错/通过的同概念记录；引用措辞和教学方式符合证据范围 | 单元/检索 + 受控与真实评价 |
| 跨会话使用 | 未入库学习可用于新 Session 对比；引用能打开准确原会话/卡片；不混入无关对话、草稿、旧 pending 或旧授权 | 集成 + 原生 |
| 归档与排除 | 归档仍可检索；排除后原会话及派生摘要/缓存不可跨会话使用，重启/迟到回放不复活；已有独立知识卡保留 | 策略版本/迁移/并发 + 原生 |
| 知识关联 | RAG 场景按实际 fixture 关联旧索引知识并说明区别；无记录不说「你学过」；过期/冲突/已删除引用不用于证明掌握 | 检索负例 + 真实对话 |
| 复习隔离 | 到期不是遗忘；相关回忆仅可选；讲解/关联/随堂作答不写正式 ReviewAttempt/FSRS；拒绝保存目标仍完成 | 数据副作用断言 |
| 内部主备 | 限定名单/能力/强度、总次数和总时限；超时/断线恢复不提示切模型；无合格备用诚实失败；401/403不绕过；终态/停止不自动复活 | 故障注入 + 已配置模型的真实测试 |
| 流式一致性 | 同一可见回答/累计耗时；断线回放不重答；无法安全续接保留未完成，不拼接两个答案；旧 revision 禁止追加/提交 | SSE/快照/重试并发 + 原生 |
| 质量与工具 | 普通问答不固定二次全文审核；硬错误有限修复、风险先查证、已显示错误明确更正；资料指令不授权，搜索不传整段私有内容 | 合成 badcase + 工具调用记录审查 |
| 动效与阅读 | 八点等待/阶段秒数；停止/等待用户/失败停止循环；选择轻弹、全选仅可见行短错峰；最后输入获胜，不抢焦点/滚动 | 原生代表速度录屏 + Reduce Motion |

验收样例由合成或脱敏数据构造，明确记录初始 Session/Run/知识版本与复习状态、输入、预期动作及禁止副作用、真实错误和修复证据。失败先复现为测试，再改实现；修复不能只靠更换提示词后单次成功。原始真实聊天、凭证与隐藏思维链不得进入公开 fixture 或 CI 日志。

#### Rex 的原生验收路径（实施完成后再执行）

1. 打开今天，在同一侧栏创建/切换两个 Session；检查草稿、学习方式与强度各自保存，默认浅色及人工深色持久化正确。
2. 多选并快速全选/反选；运行中归档及撤销，确认反馈有弹性但操作不等待动画，输出不会复活。
3. 使用隔离旧知识场景学习 RAG；分别看已讲过/验证过/近期答错的教学表达，点击关联返回正确记录。不制造真实用户掌握数据。
4. 归档旧会话后再次关联；随后选择不用于跨会话记忆并重启，确认该来源不再被使用，独立知识卡仍可访问。
5. 连续补充、纠正、拒绝保存；受控制造断线/主备异常/摘要失败，检查已保存消息、真实状态、未完成标记和评分隔离。
6. 最小 760×620、默认与宽窗，浅深主题、中文输入、键盘、长回答上文阅读、Reduce Motion 及快速打断下检查核心路径。

性能沿用本地回显/停止 ≤100 ms、接收事件呈现 ≤200 ms、额外显示缓冲 ≤100 ms。模型识别/首段/总耗时独立记录，不把环点流畅当作模型变快。语音真实收音、自然轮替/打断、音频清理和供应商条件另设后续验收，不用固定波浪循环证明已录音。

### 0.10 完整修复、学习手册与新图标（既有门槛与证据）

基线 e89a85c：108 项 Python 测试通过，但已复现「攻克通过后不保存仍等待」「讲义继续后来源变用户资料」「摘要失败使成功回答失败」。2026-09-05 已完成第一批修复及下列检查；**不是完整 M1 通过**，未验证项不继承旧绿灯。

| 验证组 | 必须验证的正向/拒绝行为 | 初始证据状态 |
|---|---|---|
|连接|分类/脱敏/角色部分可用；10/30/120退避；401/403不循环；未开始续发、失败手动|待受控与真实|
|权限|新旧入口归档/撤销/停止/迟到/提交竞争；恢复不生成，未知提交幂等|待自动与原生|
|学习|五模式/JD；独立作答+追问完成后拒绝保存仍完成；条件/否定/版本拒绝|待自动与真实|
|连续性|计划调整/补学/跳过；生成来源稳定；混合来源/交接；无FSRS副作用|待自动与真实|
|事件|摘要失败不改成功，慢ACK不挡增量，重复/乱序/缺口/重启|待自动与性能|
|数据|增量迁移、清理须ACK/无引用、缺失快照恢复且不执行、状态冲突409|待自动|
|链接|逐跳/实际连接公网检查、DNS变化、超限/超时、无伪证据|待自动|
|投影|统一待处理准确跳转，停止非完成，活动去重，Today三条/展开/续学|待自动与原生|
|UI|隔离RAG空态/讲解/练习/完成；固定清单、步骤定位、阅读/输入焦点|待原生|
|图标|网页实际制作/原生导出/参数复现；两种轮廓、16–128px、浅深透明检查|待制作/Rex选择|

原生验收需中文选词、Return/Shift Return/连续发送、失败保留、草稿隔离、长答案/上文阅读、最小760×620/默认/宽窗、浅深主题、Reduce Motion和键盘。使用隔离数据，真实用户知识库不做破坏性测试。

受控性能：本地回显/停止≤100ms，收到事件呈现≤200ms，显示缓冲≤100ms。真实寒暄/RAG/长资料/攻克分别记录首段生成、首段显示和总耗时；供应商慢调用不以动画掩盖。

文档push是实施前置；完成后逐项填写命令/构建/环境/观察结果，不能只记录测试数量。图标选择前不替换正式资源；#17/M2不放行、不发布。

#### 2026-09-05 实施与验证记录（替代上表初始状态，不扩大通过范围）

- 文档门槛：`ea50bdd` 已独立提交并 push，随后开始代码和 OneWorks 制作。
- 服务：`bash agent-service/run-controlled.sh`，隔离临时 SQLite、空 API key；132 项全部通过（最终 4.498 秒）。新增断言覆盖目标完成后拒绝保存、单节通过不完成全计划、生成来源保持、归档撤销授权、恢复不执行/不覆盖已有状态、缺失检查点拒绝静默重建、摘要失败不污染成功及 ACK 不丢未摘要上下文。旧生命周期 ACK 曾实测返回 200（应拒绝），已改为共同事务校验；已 claim 的本机原子保存仍可确认，但不恢复会话。流式单独断线的退避也增加先失败再修复的测试。
- 连接：受控测试覆盖角色分离、10/30/120 退避、单飞、403 不当作网络且不重探、脱敏允许列表。2026-09-05 11:43 同供应商/原 Luna、Terra、Sol 的真实结构化探测全部 HTTP 403，分别 729/728/726 ms；无回答生成。该延迟只是拒绝耗时，不能报告模型提速。没有换模型、供应商或绕过限制。
- 链接：逐跳私网、混合 DNS 答案、连接固定已核验地址、实际 peer、公网跳转次数、响应大小和时间边界的 7 项受控测试通过；未以模型编造链接代替来源。
- Mac：Debug 无签名 `xcodebuild` 通过。`bash tests/mac/run-contracts.sh` 的输入/草稿/标签/活动、重放/重复/乱序/停止/生命周期/会话隔离测试通过；最终停止本地保存约 0.94 ms。`tests/mac/run-sse.py` 用单次 loopback SSE 分割中文字节，原生解析 3 段间隔到达，最终最大受控接收延迟 25 ms；不是供应商生成延迟，也不是全 UI 渲染性能证明。
- 迁移：`bash tests/mac/run-migration.sh` 从 `e89a85c` 源码生成旧磁盘 fixture，使用新模型读取；会话、草稿、来源、知识和正式复习 ACK/评分保持，新字段默认/空值正确。未写用户数据库。
- 原生交互：Debug `REVIEW_TODAY_M1_UI_FIXTURE=learning` 使用内存数据并禁用服务/同步，界面标注“不代表 Harness 验收通过”。已看到 RAG 空态/讲解/练习/完成四种状态，检查清单展开、点击已讲解步骤定位、整行详情、保持编辑焦点、归档后输入消失/模式禁用/计时冻结、撤销后不续跑、深浅主题、侧栏收起、Shift Return 多行、Return 回显并清空。发现并修正侧栏把停止显示为完成的文案。初次锁屏后由 Rex 手动解锁继续，未绕过保护。
- 品牌：两版 OneWorks 实际矢量、参数与小尺寸/浅深对照在 `brand/oneworks/`。网页下载未落盘，复制 SVG 已核对；保存网页真实矢量几何，PNG 为其本地矢量渲染而非原生 PNG 下载或截图放大。项目“另存为”未取得文件；参数记录不冒充工程文件。Rex 未选择，正式 V8 未变。
- CI：新增 `.github/workflows/m1-controlled.yml`，无密钥服务回归 + macOS 26 原生构建/契约/旧库迁移；托管运行结果另记，不把本地通过写成 GitHub 已通过。

#### 当时未完成或未通过（历史；本轮变化见 §0.12，不可关闭 #17）

1. **实现限制**：Mac 后台完整 checkpoint 落后于消费游标时，只保留历史并拒绝自动恢复；任意崩溃时点的增量快照合成尚未完成。来源已有稳定 ID/类型/版本/快照复用，但显式刷新为新版本、复杂混合材料及跨会话知识/步骤引用仍需进一步分支验证。计划按标题匹配保留已有步骤，语义改名的步骤映射未实现。
2. **真实上游阻塞**：五模式完整教学、资料包互补性/可定位性、JD 选题、独立作答与迁移评价、真实生成中断网/归档/恢复、真实供应商流式和性能不能通过。403 需原供应商访问恢复后复测，不通过静默换供应商消除红灯。
3. **原生待测**：真正中文输入法候选确认（受控 marked-text 测试不等于真 IME）、最小/宽窗口、长答案滚动抢位、Reduce Motion、键盘全路径、实际正式复习/热力图/来源与跨 Session 交接；四种 fixture 的视觉最终仍由 Rex 验收。
4. **图标待选择**：当前为候选探索，仍需轮廓精修；高清 PNG 的网页原生下载/工具工程文件尚未取得，Dock/Finder/正式资源接入不提前进行。

旧 M1 已证明固定文字材料能够形成一张知识卡并完成文字评分，但没有证明 Session、连续学习、服务自动恢复、增量反馈或四模式工作流成立。#17 与父 Issue #1 在本节通过前保持开放，M2 不启动。

M1 Harness V2 的唯一总命题是：

> 用户能否在真实 Mac App 的学习 Session 中提交材料、主题、问题或 JD，立即得到可信反馈，并在服务波动和多轮交互下完成对应学习目标；需要形成记忆时，结果仍能安全接入已有知识卡与复习闭环？

### 0.1 必须实现的产品闭环

2026-09-04 新增阻塞性体验门槛（实施前冻结，非通过记录）：

- 原生输入：Return／Shift Return／⌘ Return、中文选词不误发、清晰光标及焦点、正文与占位对齐、多行最后一行可见、粘贴撤销、失败不丢输入、Session 草稿隔离。
- 阅读：运行详情整行点击和键盘展开，移除对话头像但保留品牌；历史不重播，阅读上文不被抢滚动，减少动态效果下保留实时状态。
- 流式：完整结果前可看到正文；中文跨片段、未闭合 Markdown、重放、乱序、断线／重启续接、停止／纠正后旧片段不得覆盖，最终不重复生成消息。
- 五模式：寒暄和无副作用暂缓不创建目标或调用 Terra；混合意图不走轻量捷径；部分结果不得提前触发授权、理解通过、Task 完成或正式复习评分。
- 服务状态：后台能力探测不冒充当前消息阶段；服务与消息协议可用时不显示全宽检查横幅，部分能力异常只给紧凑提示，全部模型不可用才阻塞。
- 受控性能目标：本地回显和停止反馈 100 ms 内，收到事件后呈现 200 ms 内；分别测量首段生成、首段呈现和总耗时，不用动画掩盖慢模型。
- 真实模型与原生 App：寒暄、RAG、长资料教学、问题攻克分别记录前后耗时；最小／宽窗口、明暗主题、中文输入法、Reduce Motion 必须真实检查。供应商只能整段返回时明确提示，不算流式验收通过。

2026-09-04 代码完成后的工程证据（不等于原生体验验收）：

| 场景 | 旧构建首段／总耗时 | 当前构建首段／总耗时 | 判断 |
|---|---:|---:|---|
| 寒暄 | 无首段／97.3 秒后超时（Luna 后又调用 Terra） | 8.1／8.1 秒（仅 Luna） | 快捷路径生效；单样本不构成稳定 SLA |
| RAG 普通回答 | 24.8／24.8 秒 | 114.4／134.3 秒 | 当前确有 61 个正文增量，但供应商首段显著变慢，速度未通过 |
| 长资料教学 | 21.2／21.2 秒 | 无首段／90.3 秒超时 | 当前样本在 Luna 意图阶段超时，未通过 |
| 问题攻克 | 63.1／63.1 秒 | 29.4／62.4 秒 | 总耗时相近，首次在完成前显示 31 个正文增量 |

以上为同日单次、隔离临时数据库的顺序请求，供应商波动很大，只证明真实增量或失败路径，不证明模型统一提速。请求不会提交知识或修改正式复习数据。受控工程检查为：107 项 Python 测试通过，包含补充输入主动关闭旧连接并续接同一 Run、暂缓对话不调用 Terra、结构化角色不误报流式失败；原生输入测试通过 Return／Shift Return／⌘ Return、空白／长按、marked text、失败保留和 Session 草稿；SwiftData 重放测试通过重放、乱序、缺口、停止、旧 revision、重启与隔离，停止落盘约 0.9 ms；能力状态投影测试确认检查中不阻塞、部分受限紧凑提示、全部不可用才阻塞；本地真实 SSE 三帧接收延迟最大 30 ms。Debug 与 Release 构建通过。

Mac 已解锁。真实失败 Run `efb27df2-da15-43ae-ac0b-03dd15d0bba0` 修复后在 6 ms 内完成兼容轻量回复，无 Terra 调用、无 Task、无重复消息；最新签名 Debug App 在临时本地服务下已看到该 Run 为“已完成 · 0.0 秒”，并确认服务就绪时不再显示全宽能力探测横幅。修订后新的 Luna 真实识别请求在 60 秒超时，未取得新的 `defer` 模型样本。停止临时服务验证 App 自托管时，系统再次弹出桌面文件夹访问确认；未代替用户授权，因此中文输入法、实际焦点／光标、滚动锚定、整行点击、窗口、主题和 Reduce Motion 仍需权限通过后的原生复验。#17 与 M2 继续暂停。

2026-09-04 解锁后的真实截图发现：“我思考一下”已被 Luna 正确判为无任务的 `self_report` 并生成轻量回复，但旧守卫只放行问候／感谢／能力询问，随后多调用 Terra；Terra 返回 `response.failed`，Run 以 `RT.MODEL.INCOMPLETE` 失败。该证据只证明意图识别正确、执行分流缺失；修复后必须以“仅一次 Luna 调用、无 Task、无状态变化、正常完成 Run”复测，且能力探测不得继续显示成消息处理状态。

历史 §0.7 的固定 72 pt 输入区验收只代表旧构建，当前以 DESIGN.md 的 64–160 pt 自适应输入规范为准。#17 与 M2 仍不放行。

1. **Session**：新建、继续、归档、恢复、上下文隔离和结构化新 Session 交接。
2. **即时反馈**：输入先本地持久化并立即显示；服务启动、运行、等待用户、失败和恢复均有真实状态。
3. **工作过程**：主 Task 卡片显示可理解阶段；运行详情显示节点、路由依据摘要、耗时、重试、错误和恢复，不展示隐藏思维链。
4. **五个模式**：Auto 自动调度，知识整理、资料学习、主题探索和问题攻克分别遵守当前入口、输出与完成条件。
5. **记忆衔接**：资料学习和问题攻克只有经用户确认才生成记忆；教学检查和模拟回答不更新 FSRS。
6. **兼容**：本文件后续固定知识卡和文字评分契约继续通过，v1 数据不被破坏。

### 0.2 Harness 固定验收场景

#### 场景 A：主题探索与即时反馈

输入：

```text
我想了解 RAG
```

预期：

- 用户消息在等待网络前已经显示且重启可读；
- Auto 识别学习愿望并调用主题探索，无需用户选择分类；
- Agent 每次只询问一个影响学习目标的关键问题；
- 用户可以提供材料、链接、让 Agent 查找，或明确要求生成讲义直接教学；
- Agent 查找时返回 2–4 个带用途、日期、范围和定位的来源；
- 外部资料包确认后，或明确选择生成讲义后，同目标转入资料学习，来源类型明确，不重复消息或资料。

#### 场景 B：问题攻克

先选择“问题攻克”，或在 Auto 中明确提出面试准备目标，再输入：

```text
请解释 RAG 是什么，以及为什么要使用它。
```

预期：

- 首轮先给一版可用答案，并标记关键假设、置信度或证据状态；
- 同轮展示相关知识依赖、可能缺口、针对性学习顺序和一个校准问题；
- 用户独立作答后，Agent 从正确性、完整性、表达和迁移能力给出反馈并按缺口追问；
- 未经过独立作答不能显示任务完成；
- 通过后询问是否形成记忆，未确认时不得写入 Knowledge 或 FSRS。

#### 场景 C：JD 拆解

输入一段至少包含三项岗位要求的文字 JD。

预期：

- 先返回岗位目标、能力地图、风险点和按优先级排序的问题清单；
- 不一次性生成整份答案；
- 用户选择一题后进入同一 Task 的问题攻克子流程；
- 选择前不得为所有题生成记忆。

#### 场景 D：服务故障与恢复

1. 在服务未运行时提交一条消息；
2. 确认消息已独立保存，界面显示“正在启动学习服务”；不得为了保存消息强行建 Task；
3. App 自动启动服务并续接同一消息/Run，已有学习目标续接同一 Task；
4. 运行中终止受托管服务，确认显示恢复状态并有限重试；
5. 恢复后按 Event seq 继续，不能重复教练消息、用户消息、来源或知识；
6. 配置或解释器缺失时显示具体诊断、重试和取消，不无限等待。

#### 场景 E：Session 隔离与交接

1. 在 Session A 学习 RAG 并形成至少一个已确认决定；
2. 输入明确无关主题，确认只出现“建议新建 Session”，不会自动移动；
3. 选择新建 Session B，确认只传递结构化交接包；
4. Session B 不读取 A 的全部聊天，只使用最新输入、推断目标、相关决定、引用和未解决问题；
5. 归档并恢复 A，完整可见对话、Task 和关联仍存在。

### 0.3 自动化证据

必须覆盖：

- AgentSession、AgentMessage、LearningTask、TaskEvent、SessionSummary 的持久化与关系；
- Turn、Task snapshot、Event polling、Action 和 ACK 的幂等；
- Event seq 递增、重复读取、断点恢复和事件窗口缺口；
- 五模式意图、Auto 同目标自动转接、明确模式选择作用于当前目标，以及跨模式进度保留；
- 服务启动失败、异常退出、模型不可用、超时、重试、取消与 terminal failure；
- JD 拆解、先答再校准、用户独立作答、追问和记忆确认；
- 风险查证、证据冲突、来源定位和局部暂停；
- 本文件第 2–5 节的原知识卡与文字评分回归。

### 0.4 真实 Mac App 证据

必须在运行的原生 App 中保存操作路径、关键 ID、必要截图或录屏、观察结果与未验证项：

1. 走通场景 A–E；
2. 确认 Today 没有输入框，所有输入从“学习”工作区进入；
3. 展开运行详情并核对真实 Event，而不是预设动画或本地假状态；
4. 重启 App 后恢复 Session、消息、Task、Event、资料与知识关联；
5. 使用键盘和指针完成核心路径，检查焦点与提交；
6. 检查默认、最小和扩大窗口，中文长内容、中英混排、浅色、深色及 Reduce Motion；
7. 运行现有固定知识卡和正确／错误文字回答路径，确认兼容。

构建成功、静态截图和服务级测试不能替代本节。最终视觉与真实体验仍由 Rex 验收。

### 0.5 旧 V2 工程自检记录（历史证据，当前修订尚待新增验收）

已完成但不等于 Rex 验收：

- 40 项 Python 契约与兼容回归测试通过，覆盖 V2 API 幂等、事件顺序／ACK、SQLite 重启恢复、四模式受控输出、JD、问题先答再校准、独立回答门槛、来源不足失败关闭、取消后的迟到结果抑制、新主题建议和交接；
- macOS Debug 原生构建与运行；Release 无签名构建在最终提交前复跑；
- 原生 App 输入“我想了解 RAG”后立即显示本地消息与接收状态，进入目标收窄；运行详情显示真实 `mode_router`、`topic_goal` 等事件；
- 任务执行中强制终止受托管服务后，App 自动重启服务并继续同一 Task；App 重启后 Session、Message、Task 和 Event 仍在，退出时自己启动的服务同时终止；
- 真实问题输入能立即进入问题攻克并显示“正在准备基础答案和针对性学习路径”；45 秒后模型超时收口为 `retryable_failed`，界面显示重试／取消；
- 启动能力检查确认当前 Luna、Terra、Sol 虽在模型清单中，但真实短生成探测均为 `APITimeoutError`；学习工作区明确显示三个角色不可用，未静默换模型；
- Today 已无输入框，只显示学习／复习数据和继续入口。

仍未通过：场景 A 的真实可靠资料包及转资料学习、场景 B 的实际答案到独立作答、场景 C 的真实 JD 输出、四模式记忆 ACK、场景 E 的原生交接操作、完整尺寸／主题／Reduce Motion，以及旧知识卡评分的本轮 UI 回归。主要外部阻塞是当前模型服务生成超时；自动化替身只能证明流程控制，不能证明真实答案质量。

结论：Harness V2 已从“尚未实现”进入“工程实现完成、部分原生自检通过、等待模型服务恢复与 Rex 验收”。#17、父 Issue #1 与 M2 状态不变。

### 0.6 2026-09-03 学习工作区适配修复（历史证据，布局已由 §0.9 替代）

Rex 反馈左右裁切后，先补充 DESIGN 4.1.1，再修改原生呈现层。没有修改模型、任务执行、持久化或复习评分逻辑。本节更新 0.5 中的窗口／主题验证状态，不代表 Harness 完整验收。

实现：主窗口最小内容 980 × 680 pt、默认 1280 × 820 pt；学习可用宽度不足 900 pt 时收起 240 pt 会话列表，通过弹层访问。正文与输入共用最大 820 pt 阅读边界，标题最多两行，任务摘要与恢复操作分层，输入文字区保持 72 pt 并独立滚动。

原生环境：macOS 26.7（25G220），Debug App，保留现有本地 Session 数据。通过拖拽缩至最小窗口、恢复到 1280 pt 宽及系统“缩放窗口”检查实际表现；截图为工具输出尺寸，放大窗口的图片经过等比缩放，不用于反推 pt。

| 实际检查 | 观察结果与证据 |
|---|---|
| 最小窗口、浅色、展开运行详情 | 左侧导航完整，详情靠左换行；滚动正文不遮挡输入框和发送。[截图](evidence/2026-09-03-learning-layout/minimum-light-details.jpeg) |
| 最小窗口、深色、等待选择 | 长来源选项、模式选择和输入均在视口内。[截图](evidence/2026-09-03-learning-layout/minimum-dark-actions.jpeg) |
| 1280 pt 常用宽度 | 会话列表并排显示，正文与输入左右边界一致。[截图](evidence/2026-09-03-learning-layout/standard-dark.jpeg) |
| 系统扩大窗口、失败态 | 阅读宽度不无限伸展，重试／取消完整可见。[截图](evidence/2026-09-03-learning-layout/expanded-dark.jpeg) |
| 长中文、英文与连续 URL | 已有消息在正文内换行，不再撑大主窗口。[截图](evidence/2026-09-03-learning-layout/minimum-long-content.jpeg) |
| 多行草稿与焦点 | 放大再缩小跨越分栏阈值后文字及编辑焦点保留；输入框可独立滚动至最后一行，发送始终可见。[截图](evidence/2026-09-03-learning-layout/compact-light-draft.jpeg) |

另外实际操作了：窄窗打开会话列表、选择后关闭、Escape 关闭并恢复输入焦点；主侧栏隐藏／恢复；空归档列表与空对话；明暗主题切换。测试草稿没有发送，未新增或删除学习数据。长来源截图来自历史测试输出，只证明布局，不证明链接或答案可靠。

技术检查：Debug 与 Release 无签名构建通过；40 项 Python Harness／知识卡／评分回归通过；`git diff --check` 通过。没有新增依赖或修改数据库。

未覆盖：VoiceOver、完整键盘导航、系统较大文字、Reduce Motion／Reduce Transparency 的设置切换，以及四模式真实模型闭环；本次分栏重排没有新增动画。现有模型不可用状态仍显示，不能因布局修复而解除 #17／M2 门槛。最终舒适度与真实学习体验仍待 Rex 验收。

### 0.7 五模式意图修订验收场景

| 场景 | 必须观察到的行为 |
|---|---|
| 五模式问候/感谢/能力询问 | 自然回应，无强制学习 Task |
| Auto“RAG 是什么” / 选择问题攻克后同句 | 前者先直接答，后者先答再校准与训练 |
| Auto 裸资料 / 知识整理资料 | 先整理草稿，不假定理解、不自动入库或排期 |
| “不要保存/不要切换/不要新建”“可以，但第二点不对” | 无错误授权；纠正使旧确认版本失效 |
| 资料学习“先别考我，继续” | 继续讲解，未验证状态保留，不改 FSRS |
| 主题探索“你直接教我” | 可生成讲义、注明来源；高风险仍查证，不能伪造外部来源 |
| 攻克中的提示/不知道/跳过/自述理解 | 不算通过；独立作答与追问才形成掌握证据 |
| 当前目标主动选模式 / 切回 Auto | 从下一步真实生效，资料进度不丢；Auto 内部转接不改选择框 |
| 运行中补充、纠正、排队、停止、恢复 | 消息不丢，停止不取消目标，不自动消费队列，迟到输出与入库失效 |
| 断线、进程中断、重复消息/操作/ACK | 本地可续接，无重复回答或知识；确认对象与版本仍匹配 |
| 跨 Session 与引用指令 | 无越界对象引用；网页/引用中的指令不成为操作授权 |
| 模型检查与错误 | 真实结构化探测、空输出/拒绝/超时/结构错误可区分，首次失败 attempt=1 |

验证顺序：受控模型单元/接口/并发回放 → Swift Debug/Release 构建 → 真实角色模型语义验证 → 原生 App 操作。结果分别标 existing/proposed/unverified；替身、构建和截图不代表真实闭环或 Rex 验收。历史 MODEL_FAILED 原始异常未保存，不能以新架构推定旧异常原因。

### 0.8 本轮实施证据与接力入口（2026-09-03）

结论：五模式修订代码已接入，受控测试通过；真实模型和原生 App 仅部分检查。最新 Debug 启动受 macOS 桌面文件夹授权阻塞，完整 M1 验收仍未通过，#17/M2 继续暂停。

| 证据层 | 已执行及结果 | 不代表什么 |
|---|---|---|
| 无密钥受控测试 | 107 项通过。`test_conversation_v2.py`、`test_conversation_http.py`、`test_response_stream.py` 覆盖五模式普通对话、暂缓、否定/条件/引用、草稿版本、模式切换及附带追问、JD/选源、理解分级、独立作答+迁移、提交领取、补充/停止/队列竞争、旧连接取消、流式结构与公开投影、幂等、回答落盘后中断不重复、摘要/ACK 裁剪、旧动作守卫；旧知识卡与评分回归保留 | 不证明所有自然语言样本、供应商时延和真实答案质量 |
| 构建 | macOS Debug 本机临时签名与 Release 无签名构建通过 | Release 未内置 Python，不是安装包或分发验收 |
| 真实模型（隔离的临时 checkpoint） | Auto 你好约 39.7 秒、RAG 问答约 17.2 秒，均无 Task；问题攻克成功首答/路径/校准约 60.5 秒；知识整理约 77 秒交付不入库；资料学习约 25.2 秒生成标明来源讲义、理解 unknown | 为单次结果，不是延迟 SLA；曾发生 CONNECTION/TIMEOUT，四工作流完整闭环仍待验证 |
| 原生 App | 验收会话 `你好，原生交互验收`：即时回显、运行事件、停止/恢复、失败后重试得到 RAG 回答；重启仍保留消息。最新构建已检查五项模式菜单、Today 最新运行状态、离线消息保存与有限启动失败提示 | 没有提交正式知识或修改复习分数；不能证明最新构建全程通过或 M1 已被 Rex 接受 |
| 最新启动阻塞 | Python 初始化读取文件时阻塞；系统日志出现 `Failed to match existing code requirement` 和 `kTCCServiceSystemPolicyDesktopFolder` 等待授权；四次启动后停止，消息仍在本机。[原生截图](evidence/2026-09-03-intent-harness/local-input-startup-blocked.jpeg) | 与意图识别、模型连接是不同层；截图记录诊断措辞补充前的检查，不能作为恢复成功证据 |

原生验收会话 ID：`23370d37-008b-4f50-983e-c10faf92aa54`。已检查的停止/恢复 Run：`d356a769-c04f-4031-806d-ec23e6d1efdc`；连接失败后重试得到回答的 Run：`1c90ff35-f0d2-4650-ad8c-9bb8ad8f9d5b`。后续新增的“你好”会话只有一条本地待发测试消息，不应删除用户已有会话来重测。

恢复及 Rex 验收顺序：

1. 在系统提示中允许当前 Review_Today 访问项目所在桌面文件夹；若没有提示，检查系统设置 → 隐私与安全性 → 文件与文件夹中的该 App。不要为此删除数据库、重置全部系统权限或修改模型凭证。临时签名重编译后可能需要再次授权。
2. 打开 Xcode 当前项目的 Debug App，学习页点击服务“重试”；已有本地消息应自动送达，不必再次输入。若报模型 CONNECTION/TIMEOUT，用该条 Run 的“重试”，不要重新提交同一问题。
3. 新建 Auto 会话先“你好”，收到回应后问“RAG 是什么”；应直接回答，不把问候视为旧目标，不强制任务卡。另建问题攻克会话输入同句，应先答再校准。
4. 按 §0.7 逐项做否定、检查跳过、手动模式切换、连续补充/排队/停止、确认新会话。正式入库前须核对整理版本和理解状态。
5. 补齐真实 2–4 来源确认→教学、JD 逐题、独立作答+追问→入库、原知识库/正式复习 UI、归档/交接、长会话、无障碍与系统动态偏好；记录实际通过项后才讨论 #17/M2 放行。

自动回归：在 `agent-service` 执行 `.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -q`。无需密钥、不写用户知识库。模型检查用的是 [官方结构化输出机制](https://developers.openai.com/api/docs/guides/structured-outputs)，但当前配置提供方的可用性仍以真实请求为准。

### 0.9 单侧栏、Session 导航与活动反馈验收（2026-09-04）

本节替代 §0.6 中第二 Session 栏和窄窗弹层的目标。AirJelly 截图只作为结构参考，不是 Review Today 的交互验收证据。

当前工程证据：文档检查点 `bb88512` 已在代码修改前独立推送；根侧栏 Session 导航、共享 selectedSessionID、开放式消息画布、直接归档／只读恢复、自动与人工标签、AgentRun／ReviewAttempt 活动事实、26 周热力图和最近学习页面内展开均已接入。108 项 Python 回归、两个原生独立契约测试及 macOS Debug 无签名构建通过；Debug 固定数据已实际查看深色学习结构和 Today 热力图。屏幕随后进入锁定，未继续操作系统主题、Reduce Motion、中文输入法、侧栏拖拽、归档中的真实请求或日期明细跳转，因此这些不能标为通过。

#### 自动化

- 根侧栏显示／隐藏状态可恢复；学习 Session 区搜索、进行中／已归档切换、独立滚动与准确选择不会改变草稿或 Run；
- Today、侧栏和 LearningWorkspace 共享同一 selectedSessionID，点击任一 Session 都打开对应标题、历史、标签和草稿；
- 自动主题标签、人工覆盖、恢复自动、更多数量和知识标签只读均有确定性测试；模型标签不能触发目标切换、归档或入库；
- 运行中归档先持久化 stop 和本地失效，再改变 Session 状态；迟到 delta、completed 与活动字段均被拒绝，撤销／恢复后历史仍完整；
- AgentRun 仅允许已完成 `knowledge_answer`／`lesson_step` 计入活动；ReviewAttempt 仅允许非 preview、ACK 成功且有效等级计入正式复习；对象 ID 去重；
- 26 周边界、系统日历／时区归日、0／1／2／3–4／5+ 强度、活跃天数和连续天数有固定时钟测试；不明确的旧数据不回填；
- Today 最近学习默认三条，展开后固定高度滚动全部进行中 Session，收起恢复三条；
- 五模式、输入法、流式、确认／否定、理解检查、知识提交与 FSRS 回归全部保留。

#### 原生 Mac App

1. 在最小、默认和扩大窗口显示、隐藏并拖宽侧栏；主导航与设置固定，会话列表独立滚动，窄窗不出现第二 Session 弹层。
2. 从侧栏和 Today 分别打开两个 Session；核对选中态、标题、主题标签、模式、历史和独立草稿。
3. 修改 Session 主题标签，再恢复自动；知识关联标签可看但不可编辑。选中、悬停、运行中、失败和归档不能只靠颜色辨认。
4. 在回答生成时归档；确认可见部分冻结、输入和历史保留、迟到结果不再追加；使用“撤销”或“恢复”继续。
5. 检查轻量顶部栏、820 pt 最大阅读列、右对齐用户消息、无外层大回答卡和无重复头像；输入框锚定底部且不遮挡内容。
6. 触发服务／模型失败；主视图只显示可理解原因、保存状态和恢复动作，底层路径、命令与堆栈仅在展开技术详情出现。
7. 产生一次普通知识回答、一次资料学习步骤和一次正式复习；确认热力图当天计三次。问候、preview、失败和未完成生成不改变热力图。
8. 悬停活动格查看日期与类型；点击进入当日明细，再进入对应 Session 或知识。检查最近学习三条收起和页面内展开滚动。
9. 在浅色、深色、Reduce Motion、键盘路径、中文输入法、长中英内容和阅读上文时的新流式内容下重复核心操作。

受控性能目标保持：本地回显与停止反馈 ≤100 ms，已接收事件至显示 ≤200 ms，流式显示缓冲 ≤100 ms。自动测试、构建和截图不能替代上述原生操作；完成后最高可先标为 `code complete` 或已执行场景的 `experience verified`，最终由 Rex 标记 `user accepted`。#17 与 M2 在此之前继续暂停。

## 1. 兼容子基线：纯文字知识卡与评分

以下旧固定样本继续验证已有知识卡与评分能力：

> 用户提供一段稳定知识内容后，系统能否形成一张忠于原文、可以被提问和判断的知识卡，并正确区分用户的正确与明显错误文字回答？

M1 证明一条真实纵向链路可用，不证明模型已经能稳定处理所有知识类型和边界回答。

## 2. 固定样本

### 2.1 用户输入

```text
光合作用是植物利用光能，把二氧化碳和水转化为有机物，并释放氧气的过程。
```

选择这个样本的原因：

- 只包含一个稳定概念，正常情况下应生成一张知识卡；
- 原文同时包含能量来源、输入、产物和伴随释放物，适合检查知识卡是否遗漏关键点；
- 不涉及时效、精确数字、医学、法律或金融，不应触发联网核验；
- 正确与错误回答边界清晰，适合验证最小评分闭环。

### 2.2 正确回答

```text
植物借助光能，用二氧化碳和水合成有机物，同时释放氧气。
```

预期：未使用提示时返回 `good`。回答完整覆盖原文的四个语义要点，没有引入冲突信息。

### 2.3 明显错误回答

```text
光合作用就是植物从土壤里直接吸收有机物。
```

预期：返回 `again`。回答把“利用光能合成有机物”错误说成“直接吸收有机物”，且遗漏二氧化碳、水和氧气。

## 3. 知识卡最小契约

服务必须生成且 Mac 必须保存至少以下内容：

| 字段 | M1 要求 |
|---|---|
| 知识数量 | 固定样本生成且只生成 1 张知识卡 |
| `title` | 能独立识别为“光合作用”，不是说明句、半截标题或空分类 |
| `explanation` | 用简体中文覆盖光能、二氧化碳与水、有机物、氧气；允许不同分点和措辞 |
| `evidence_excerpt` | 必须是用户原文的连续子串，不得翻译或补写模型常识 |
| `knowledge_type` | `concept` |
| `theme` | 明确指向“光合作用”，不得使用“知识”“技术”“其他”等空主题 |
| 语言 | 内容、默认问题和预期回答均使用简体中文语境 |
| 主问题 | 存在 `variant_index = 0` 的开放式主动回忆问题，不是是非题 |
| `scoring_spec` | 语义上覆盖光能、二氧化碳与水、有机物、氧气，并保留原文证据 |

问题不要求逐字固定，但必须能通过上述评分关键点判断用户是否理解光合作用过程。

### 3.1 M1 capture 稳定字段

M1.4 Mac 客户端可以直接依赖以下服务字段：

- 任务层：`task_id`、`status`、`user_status`、`error_code`、`receipt`、`result`；
- 回执层：`understood_as`、`theme`、`knowledge_count`、`attribution`；
- 知识层：`id`、`learning_goal`、`knowledge_type`、`theme`、三类语言字段、`evidence_excerpt`、`evidence_locator`、`title`、`explanation`、`scoring_spec`、`questions`；
- 评分规格：`learning_goal`、`must_cover`、`acceptable_paraphrases`、`common_misconceptions`、`evidence`、`order_rules`；
- 问题层：`variant_index`、`prompt_text`。

服务端必须在进入 `committing` 前完成以下硬校验：

- 每个知识 `id` 是有效且批次内唯一的 UUID；
- 每张知识卡恰有一个 `variant_index = 0` 的主问题，问题序号不得重复；
- `evidence_excerpt` 与 `scoring_spec.evidence` 都是当前来源原文中的非空连续子串；
- 结构或来源忠实性不合格时不返回可提交结果，也不能进入 `completed`。

### 3.2 任务幂等与重试语义

- 同一 `task_id` 处于 `processing`、`committing` 或 `completed` 时，重复提交只返回同一任务，不生成第二份服务结果，也不重复入队；
- 同一 `task_id` 处于 `retryable_failed` 时，重复提交允许将原任务重新置为 `processing` 并入队，但仍复用同一任务记录；
- 用户主动 `reprocess` 同样复用原任务记录，不创建第二份知识结果；
- 没有 Mac ACK 时任务必须保持 `committing`；仅当 ACK 中的 knowledge IDs 与服务结果完整匹配时才能进入 `completed`。

### 3.3 Mac 本地提交语义

#12 的 Mac 端必须先建立可持久读取的 `Source` 与 `CaptureTask`，再异步提交服务：

- 本地任务使用稳定的 `task_id` 与 `source_id`，服务不可用时保留原始文字和队列记录；
- `processing`、`committing` 和 `completed` 按任务 ID 轮询，不能把提交响应的 `processing` 当作完成；
- 服务返回 `committing` 后，Mac 先按服务知识 ID 幂等写入 `Knowledge`、`Question`、评分规格和 `FsrsState`，本地保存成功后才发送知识 ID ACK；
- 本地写入或 ACK 失败时保留任务和原始输入，不显示为完成；下一次轮询可继续提交，不插入重复知识；
- `retryable_failed` 是可恢复状态，待处理入口和首页计数必须可见，并提供重新整理操作。

### 3.4 M1 review 评分契约

评分请求稳定包含 `attempt_id`、`prompt_text`、`scoring_spec`、`answer_text`、`hint_used` 与 `primary_language`。其中 `answer_text` 是用户原始自然语言回答：服务可以检查它是否为空，但不得先改写、提取关键点或增加独立预结构化模型调用；Mac 负责将原始回答保存在 `ReviewAttempt.answerText`。

服务端必须保证：

- 空白回答在调用模型前以 HTTP `422` 拒绝，不生成等级；
- 评分模型同时获得问题、学习目标、必答点、可接受同义表达、常见误解、原文证据、顺序规则、提示状态和原始回答；
- `agent_grade` 只能是 `again`、`hard` 或 `good`，模型输出 `easy` 或其他结构错误时必须失败，不能转换成有效等级；
- 使用提示后，即使模型返回 `good`，服务也必须程序化降为 `hard`；
- `brief_feedback` 必须非空、简短并使用用户主语言；
- 同一 `attempt_id` 首次评分成功后，重复评分返回同一缓存结果，不再次调用模型；评分失败不缓存伪结果，允许用同一 ID 重试；
- 只有已经产生有效评分结果的 `attempt_id` 可以 ACK；错误 ID 必须拒绝，重复 ACK 保持幂等；
- 模型、网络或结构化评分失败返回 `RT.REVIEW.GRADE_FAILED`，不能产生 ACK、有效等级或“已掌握”状态。

评分响应稳定包含 `attempt_id`、`agent_grade`、`brief_feedback` 与 `hint_used`。服务内评分结果与 ACK 仍是 M1 的内存状态，服务重启恢复不在本里程碑验证范围内。

### 3.5 M1 文字答题客户端契约

- 每道当前题先创建一个 `ReviewAttempt`，原始 `answerText` 在请求评分前写入 SwiftData；空白回答不得创建或提交有效评分。
- 答题页在 `grading` 期间锁住提交入口；评分失败保留当前题、原始回答和同一个 `attemptId`，允许修改原回答后重试，不推进下一题，也不写入 `agentGrade` 或 `effectiveGrade`。
- 评分成功只展示 `again`、`hard`、`good`；客户端不得把服务契约之外的等级当作有效结果。
- 采用判断或用户改判后，正式复习才更新对应 `FsrsState`；`preview` 只记录预览尝试和结果，不更新 FSRS、不进入今天正式复习结果。
- 同一题的重复点击不能在等待期间创建第二个 `ReviewAttempt`；评分失败重试复用原尝试记录。正式复习 ACK 失败后的保持、补偿和恢复已由 #15 收口。

### 3.6 M1 评分失败安全与重试契约

正式复习的完成顺序固定为：

1. 用户选择 `again`、`hard` 或 `good`，本地保存 `pendingGrade` 和 `ack_pending` 状态；
2. 服务 ACK 成功；
3. 本地更新 `FsrsState`、`effectiveGrade`、`acked` 和完成状态，并成功保存；
4. 保存成功后才进入下一题或总结页。

失败状态必须满足：

- 评分、网络或结构错误：`agentGrade` 与 `effectiveGrade` 为空或保持未完成，原始 `answerText` 和同一个 `attemptId` 可继续重试；
- ACK 失败：当前题停在可恢复状态，`pendingGrade` 保留，`effectiveGrade` 不写入，FSRS 不变化，不进入下一题；
- 本地保存失败：不显示已掌握或已计入，保留当前题和可重试的本地状态；
- 服务端错误码或本地归因码写入 `ReviewAttempt.reviewErrorCode`，用户看到的是“还没有计入复习”，不能误解为回答错误；
- ACK 重试使用同一个 `attemptId`，服务端重复 ACK 保持幂等，不重复产生业务结果。


## 4. 允许变化与失败边界

以下差异属于生成文风，不应单独判为失败：

- 标题使用“光合作用”或“植物的光合作用”；
- 解释采用 `1. 2. 3.`、短横线或其他清晰分点；
- 使用“制造／合成／转化为有机物”等不改变含义的同义表达；
- 问题使用“请解释……”“这个过程如何发生？”等不同问法；
- `must_cover` 将两个相关关键点合并在同一条中，只要整体语义覆盖完整。

以下情况必须判为失败：

- 生成 0 张或多张重复／拆碎知识卡；
- 遗漏光能、输入、产物或释放氧气中的任一核心语义；
- 证据不是原文子串；
- 用模型常识补充原文没有的事实并当作来源内容；
- 问题无法依据评分规格判断；
- 正确回答未得到 `good`，或明显错误回答未得到 `again`；
- 模型、结构、本地写入或 ACK 失败后仍显示完成或已掌握。

部分正确、隐含同义、长回答跑题、遗漏限定和常见误解属于 M2 质量评测，不作为 M1 通过门槛。

## 5. 三层验收证据

### 5.1 服务级证据

证明 Agent 契约本身可运行：

- 通过真实 FastAPI／TestClient 入口提交机器可读 fixture；
- 提交响应允许先返回 `processing`，测试需按任务 ID 轮询 `GET /v1/capture/tasks/{task_id}`；
- capture 在 ACK 前最终进入 `committing`，正确 ACK 后为 `completed`；
- 返回一张符合第 3 节的知识卡；
- 正确回答为 `good`，明显错误回答为 `again`；
- 结果记录模型与命令，但不得输出 API Key、Authorization 头或完整凭证。

无模型契约回归命令（仓库根目录执行）：

```bash
cd agent-service
PYTHONPATH=. .venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v
```

服务级成功不能替代真实 App 验收。

显式真实模型冒烟（会产生模型调用费用）使用 `agent-service/tests/m1_real_smoke.py`。先在一个终端启动本机服务：

```bash
cd agent-service
PYTHONPATH=. .venv/bin/python -m agent_service.main
```

再在另一个终端执行：

```bash
agent-service/.venv/bin/python agent-service/tests/m1_real_smoke.py
```

脚本会使用临时 `task_id`、`source_id` 和 `attempt_id`，完成 capture 轮询与 ACK、知识卡结构/来源/主问题校验、正确与明显错误回答评分、重复评分和重复 ACK 检查。默认 `unittest discover` 不会加载该脚本，也不会调用真实模型；脚本输出只保留脱敏后的状态、ID、等级和反馈存在性，不输出 API Key 或 Authorization。

### 5.2 真实 Mac App 证据

证明产品纵向链路可运行：

1. 从真实文字入口粘贴第 2.1 节输入；
2. 重新读取 SwiftData，确认 Source、Knowledge、Question 与评分规格已经保存；
3. 打开真实知识卡，核对第 3 节字段；
4. 从当前知识卡进入“试一题”，不手工拼接 ID；
5. 分别完成正确与错误回答；
6. 确认 preview 不更新 FSRS 或正式复习指标；
7. 模拟至少一种失败，确认没有错误成功或重复写入。

应保存：操作路径、关键数据 ID、运行命令与结果、必要截图、失败现象和未覆盖项。

#12 本地恢复检查还应覆盖：

1. 关闭本机 Agent 服务后输入固定样本，确认原文仍留在本地 `Source` 和 `CaptureTask`，首页任务链路显示等待服务恢复；
2. 启动服务后等待任务依次经过 `processing`、`committing` 与本地写入，确认同一 `task_id` 只产生一张知识卡，问题、评分规格和 `FsrsState` 均已保存；
3. 在 `committing` 或已完成但本地未落库的补偿路径重复 tick 或重启 App，确认先完成本地幂等写入，再发送知识 ID ACK；
4. ACK 失败时确认任务保持 `committing`，原始输入保留，下一次确认不会产生重复知识；
5. 重新打开 App，确认已完成任务和原始 `Source` 仍可从 SwiftData 读取。

服务级契约和客户端构建命令仍以第 5 节为准；本地保存失败与 ACK 失败需要同时保留任务原文和可归因错误码。
### 5.3 Rex 体验验收

证明最小体验可以理解和使用：

- 输入入口清楚；
- 知识卡内容能对回原文；
- 问题自然且可以作答；
- 判断与反馈容易理解；
- 用户知道如何重试或继续；
- 页面没有明显裁切、重叠或关键操作不可用。

真实界面最终体验由 Rex 判断，自动化和构建成功不能替代。

### 5.4 当前兼容子基线记录

已完成的机器与服务证据：

- 固定样本服务入口生成 1 张知识卡，capture ACK 后为 `completed`；
- 真实模型 smoke 返回正确回答 `good`、明显错误回答 `again`，重复评分与重复 ACK 保持幂等；
- 无密钥快速测试 20 项通过，覆盖结构/来源校验、capture 幂等、评分失败、空回答、未知 ACK 和重复 ACK；
- macOS Debug/Release 构建通过，真实容器启动正常，没有恢复出空白复习窗口；
- `review` fixture 已检查问题输入页，`retry` fixture 已检查持久化 ACK 失败恢复页：保留 Agent 判断和重试入口，不显示总结成功状态，也不更新 FSRS。

仍待 Rex 从真实 App 入口完成并确认：

1. 启动本机 Agent 服务，从今天页输入第 2.1 节固定文本，确认本地 `Source`/`CaptureTask` 和服务任务状态；
2. 打开真实知识卡并核对标题、详解、来源、主问题和评分关键点；
3. 从卡片进入“试一题”，分别提交第 2.2 节正确回答和第 2.3 节错误回答，核对反馈；
4. 正式复习采用判断后，重新读取 SwiftData，确认只有 ACK 成功的尝试有 `effectiveGrade`/`acked`，FSRS 和今天结果同步；
5. 停止服务或制造评分失败，确认原始回答保留且没有错误成功；使用 `REVIEW_TODAY_M1_UI_FIXTURE=retry` 对照 ACK 失败恢复状态；
6. 由 Rex 确认目标视口下页面无裁切、重叠和关键操作不可用。

因此当前结论是：旧纯文字知识卡、评分、客户端构建和主要失败状态已有证据；Harness V2 已完成工程实施并取得第 0.5 节部分原生证据，但场景 A–E 尚未获得 Rex 完整验收。在第 0 节通过前，不关闭 #17 或父 Issue #1，也不切换到 M2。


## 6. 父 Issue #1 映射

| 父 Issue 完成标准 | 主要证据 |
|---|---|
| 真实 App 完成文字端到端链路 | 第 5.2、5.3 节 |
| 知识卡字段忠于原文 | 第 3、4、5.2 节 |
| 正确与错误回答判断符合预期 | 第 2、5.1、5.2 节 |
| 失败不会写入错误成功状态 | 第 4、5.1、5.2 节 |
| 用户可以重试或继续 | 第 5.2、5.3 节 |
| 测试范围和未覆盖项有记录 | 第 5 节 |
| Rex 完成真实界面验收 | 第 5.3 节 |

## 7. Harness V2 仍明确不验证

- 部分正确和复杂语义边界；
- 语音输入、Realtime、VAD、打断和转写纠错；
- 登录墙、付费墙、浏览器会话网页与多模态文件；
- 语音、Realtime、通知、菜单栏输入和正式五分钟语音复习；
- Release 安装包、签名、自启动代理和公共部署；
- Apple Foundation Models／PCC 模型对比；
- 吉祥物、嘴型和 Spine 动画；
- iOS、同步、账户、部署和商业化。

## 8. 后续执行者的最小上下文

处理 Harness V2 子任务时，默认按顺序读取：

1. `docs/agent-harness-v2.md`；
2. 本文；
3. 自己负责范围对应的 `product-requirements.md`、`architecture.md` 或 `DESIGN.md`；
4. `agent-service/tests/fixtures/m1_acceptance.json` 与上游完成证据。

不要把本次聊天上下文作为唯一事实来源。发现主规格无法覆盖的真实阻塞时，先回写文档和决策记录，再继续代码实施。


## 2026-09-06 条件式教学与归档删除修复

已实施 Harness §25 和 DESIGN 本轮契约。云端 CI 保持停用，未关闭 #17 / #1 或发布。

已确认的根因和修复：

- 旧主题探索在未生成搜索词时直接走“两条来源”门槛；现在以主题、用途和当前步骤分别规划查询，自动搜索、阅读、核对后教学。单条可靠来源可以足够，相关追问复用结果。查询准备失败时复用安全的意图查询；没有可用公开查询时不谎报已搜索；准备失败且无法补建安全查询时，明确说明网页核验未完成。
- 澄清用途后曾再次追问目标；现在保留澄清上下文，用途充分即推进。意图类别、工作流和直接教学标志分开校验，失败做一次针对性修复，诊断不含错误原文。
- 真实模型曾把“直接教我”误作练习答案；完整教学控制语句在已知目标下直接续教，普通判题须引用本轮原话。仅讲过和要求继续不能升级为掌握。
- 每个模型/搜索步骤最多两次尝试、共用有界预算；成功节点缓存复用。已经输出正文后的格式失败不拼接另一代答案，保留输入和重试入口。供应商仍可能格式失败或耗时较长，本轮未承诺彻底消除模型随机错误。

验证记录：

- 最终独立验证：`/tmp/review-today-harness-final/` 固定本轮源文件，排除同时进行的正文排版任务改动后，210 项服务回归、完整 Mac 契约、迁移检查、Debug / Release 构建全部通过。日志分别为 `service-final.log`、`contracts.log`、`migration.log`、`debug.log`、`release.log`；受控 SSE 接收最大 26 ms。保留既有语音 API 弃用警告，没有新增云端工作流。
- 服务受控回归覆盖原始三轮、单来源、空词补建、无结果/冲突/超时、格式修复成功与耗尽、停止/重试/重启、请求幂等及过期拒绝。Mac 契约覆盖空库、只有会话、只有卡片、不相关候选、归档可用、排除与版本失效；preview 和无效分数不进入正式复习证据。
- 删除契约覆盖预览不写入、保存失败回滚、范围变化重新确认、未知归属/共享保护、批量删除、卡片/练习/孤立复习窗口清理与无正文标记。服务拒绝 active 会话删除，重复请求幂等。
- 隔离原生 App 完整走过 RAG 三轮教学；后续实际模型复测最终三轮均 completed，第二轮网页核验 verified，第三轮复用资料 not_called，约 57.5 秒 / 6.2 秒。已读内容包含 RAG 论文和 Microsoft 文档页面；not_called 表示这轮复用，不等于没有检索能力。真实样本是单次证据，不代表内容准确率或普遍时延。
- 原生删除：Escape 取消后内容保留；默认不勾选卡片删除；勾选后独有卡片及预览记录被清理，共享卡片保留。暂停隔离服务时本地立即删除并显示后台清理待完成，恢复服务后队列清空。另行批量删除 2 个归档会话；重启后会话/消息/Run/事件/Task 均为 0，共享卡片和其练习各 1。保留卡片显示“原会话已删除”。旧快照恢复与迟到消息均返回 409 / RT.SESSION.DELETED。
- 原生证据目录 `/tmp/review-today-conditional-native/`：`deletion-offline-proof.json`、`deletion-complete-proof.json`、`before-delete-snapshot.json`。真实模型最终结果在临时目录 `review-today-live-conditional-0jqmoxu9/results.json`，日志 `/tmp/review-today-live-conditional-verified.log`；不将含正文的临时库提交到 Git。

实际旧数据接回已完成：

- 先备份两个库并在副本演练；关闭日常 App 后事务导入。正式备份位于 `~/Library/Application Support/Review Today/Backups/20260906-harness-library/`，包含 `legacy.store`、`current.store` 与逐字段核对报告。
- 导入 8 张知识卡、11 个问题、8 条原排期、1 个必要来源、1 个已结束的预览窗口及 3 条 preview 练习；原 ID、正文、关联、评分和排期保持。无可靠完成时间的历史记录保持未知，没有升级为正式复习。4 个没有练习记录的未完成旧复习窗口不接回。
- 重复正式导入后数据完全相同。原库保留；当前库已有 11 个会话、42 条消息、16 个 Run、4 个任务和设置/草稿逐字段保持。导入不触发旧任务。
- 额外在副本注入保存前失败，确认零内容落盘；同 ID 冲突在写入前拒绝。证明见 `/tmp/review-today-library-failure/verification.json`。导入工具和使用边界见 `tools/README.md`。

本轮剩余体验验收：Rex 用自己的学习目标检查澄清是否合适、讲解是否有用，以及删除确认是否易懂。VoiceOver 全路径、所有窗口尺寸/主题组合与大规模长历史数据的性能未在本轮完整覆盖；既有完整 M1 验收仍保留。

## 日常提示收敛（2026-09-07）

已移除侧栏后台清理提示和标签编辑承诺，缩短引用知识说明、发送/回复/中断提示及流式能力提示。完成运行只保留默认折叠的运行详情，事件精确时间、毫秒耗时和首段指标仅在开发模式显示。异常恢复、理解验证状态、删除后果与后台清理机制不变；产品规则已同步 DESIGN.md。

验证：Apple Development 签名 Debug 构建及严格签名校验通过（`/tmp/review-today-copy-cleanup-build.log`）；LearningInputContractTests、SessionDeletionContractTests、AgentServiceMonitorContractTests 全部通过（`/tmp/review-today-copy-contracts.log`）。覆盖输入/草稿/保存失败重试、删除事务回滚/范围复核/共享卡保护/离线清理队列与防复活、启动超时/断连恢复/能力检查状态。编译仍有既有 NSSpeechSynthesizer 弃用警告。

已更新并打开 `output/default-brand-build/Build/Products/Debug/Review_Today.app`。通过原生 AX 与截图确认待处理页＋归档空态不再显示后台清理提示；实际打开 Agent → 添加材料 → 引用已有知识卡，确认显示「选择知识卡，插入草稿」，随后关闭弹窗，未发送测试消息或改动知识内容。当前没有可用历史会话供检查标签弹窗/运行详情；本轮未重新触发真实模型的生成、中断、断线提示，也未运行 VoiceOver、深色及缩窗专项验收。下次日常对话可核对「正在回复」、完成后的折叠详情与开发模式指标；最终体验验收仍由 Rex 确认。

## 鼠标焦点残留修复（2026-09-07）

共用按钮按输入来源显示焦点：鼠标点击（含空白处）隐藏键盘描边，Tab/方向键/Escape 导航保留焦点显示，不移动 first responder、不拦截事件、不改变真实选中状态。上下文用量提示采用同一规则并清理弹窗开关、窗口失活时的悬停状态。DESIGN.md 已同步。

签名 Debug 构建、严格签名验证通过（`/tmp/review-today-focus-fix-build.log`）。InteractionFocusTests 与 LearningInputContractTests 通过（`/tmp/review-today-focus-contracts.log`），包括鼠标/Tab/方向键/Escape/普通输入的来源切换、浅深色单行焦点与禁用渲染、输入/IME/撤销/草稿/保存失败与重试。首次构建补充 Combine 导入后通过；既有渲染测试补充明确键盘输入前提后通过，无跳过断言。

更新并打开日常开发 App，原生鼠标复测：归档开/关后点击空白无黑框，侧栏导航无残留，已暂停筛选保留选中底色无黑框，知识详情关闭后原卡片无黑框，上下文详情外部点击关闭后无小提示残留。原生 Tab 从归档移到多选按钮，焦点框正确显示；再用鼠标操作即消退。未发送模型消息、未修改知识内容。深色覆盖为渲染检查，未进行 VoiceOver、完整所有控件/窗口尺寸及菜单键盘路径专项验收；最终体验待 Rex 确认。
