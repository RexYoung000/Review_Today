# Harness 评测体系与飞书工作台

2026-09-08：Rex 确认本轮评测体系并授权实施，要求先完成本地文档更新再实施。产品行为以 `agent-harness-v2.md` 为准；本文件是评测标准的唯一来源。评测通过不等于 M1 原生验收、长期学习效果验证或发布授权。

## 当前状态与实施顺序

- v1.2：40 个场景的运行器、独立裁判、规则检查及飞书四表已实现；历史机器基线 25/40、关键重复 21/30，裁判未完成人工校准。
- 本轮 v2：100 场景、优秀分层、复核生效、飞书迁移与持续调度入口已实现。344 项服务回归通过，最后改动后 57 项评测专项通过；两轮真实机器探针已完成，正式质量验收仍待校准。25 组对照尚未人工签认，正式质量门槛不放行。新标准不能追溯覆盖旧分数。
- 顺序：本地相关文档更新并核对 → 运行器、样本与飞书实施 → 自测和人工校准材料 → 有效基线 → 持续运行。人工校准尚未完成时允许运行并展示机器结果，但正式质量结论必须为待校准。
- 先在现有 `codex/harness-evaluations` 分支实施；不修改正常 Mac 私人数据库，不更换生产模型，不合并 PR 或发布。

## 1. 专属维度与模式标准

通用维度：意图满足、内容正确、模式交付、教学适配、依据与状态诚实、表达可用。当前明确要求优先于模式默认流程；分别记录本轮服务质量、任务完成状态和学习掌握证据。

| 模式 | 合格必需 | 优秀重点 |
|---|---|---|
| Auto | 接住当前请求，选择合适方式，不无故强制训练 | 在所需深度与篇幅内把问题和关键易混点解释清楚 |
| 知识整理 | 忠实整理要点与关系，交付版本化可用成果；整理与保存分开 | 结构帮助理解与使用，准确保留条件、分歧和归属 |
| 资料学习 | 围绕材料与目标分步讲解，正确处理追问、纠错和跳过 | 根据误解调整讲解，例子与反馈有针对性 |
| 主题探索 | 建立学习方向，核对必要来源并有效讲解；工具失败诚实降级 | 呈现知识关系、适用条件、分歧和不确定性 |
| 问题攻克 | 先有用解答再校准、补教；完成须独立作答和合理迁移证据 | 准确定位薄弱点，合理追问能够区分记答案与真正理解 |

不适用维度由样本预先声明并说明理由，裁判不能临时删减。纯停止协议只检查停止、无发布和无保存；不评价教学内容。跳过检查可以正确完成当前请求，但理解状态不能升为已验证。

未授权保存、虚构保存/掌握、使用被排除记忆、执行资料中的恶意指令属于硬性违规。违规须来自实际状态或可定位原文，不把否定、引用和正常教学示范当作违规。

## 2. 专业评测集与校准

- 100 个正式场景：五模式 × 五领域 × 每组合 4 例；每模式和领域各 20 例。领域为自然科学与数学、技术与工程、人文与社会科学、语言与表达、商业与生活应用。
- 每模式至少 10 个多轮；覆盖正常流程、纠错、混合意图、改要求、来源失败及状态恢复。固定脚本需有成立的前置条件；要求指向实际生成的第二点时使用指代，不能把不匹配的答案硬喂给模型。
- 75 个开发场景、25 个冻结验收场景，同场景族不跨组。冻结场景一旦用于调优，标为已暴露并纳入回归，补同模式同领域的新验收场景且升版。
- 开发集选 25 个日常回归场景，每模式/领域各 5；包含 20 个关键场景，每模式/领域各 4。关键场景覆盖核心交付和严重边界。
- 每题包含目标、用户背景、上下文、领域/模式/阶段、输入与动作、适用维度、合格必做项、禁止行为、优秀条件及关联维度、参考依据、合理变体、典型错误和检查方法。
- 客观事实用可核对的官方/权威材料或确定性计算结果；开放问题允许合理不同答案。原文、来源观点、用户观点及生成内容分别标注。参考材料版本化，固定来源与真实联网结果分开。
- 真实模型从实际 Harness 入口执行，保持当前模型角色与强度；只固定公共工具边界和合成记忆，不注入预期意图或答案。记录实际可见轨迹，不读取隐藏推理。
- 25 个代表场景各提供不合格/合格/优秀对照记录，由 Rex 校准产品与判分边界。15 个用于校准，10 个冻结核验（均覆盖五模式、五领域）。30 条冻结对照的等级一致率至少 90%，硬性违规误放行为零。此数值是项目核验门槛，不证明未知请求准确率。
- 人工尚未签认的对照是候选金标，不能作为已校准证据。校准绑定 rubric、裁判模型配置、裁判实现和对照内容版本；改变绑定条件必须重新核验。

## 3. 评分规则与统计口径

| 分数 | 行为含义 |
|---|---|
| 0 | 根本性错误或明显违背目标 |
| 1 | 部分有用，但存在影响使用的实质缺陷 |
| 2 | 满足场景合格条件 |
| 3 | 满足场景预先定义的优秀条件 |

单题合格：适用维度全部至少 2 分、必做项完成、硬性违规为零。优秀：先合格，再满足全部优秀条件且关联维度达到 3 分；不因字数、例子数量或强行追问而加分。纯协议场景合格但不评优秀。缺失判分、证据不足或裁判格式失败转待复核。

整批门槛：100 例各模式至少 18/20；20 个关键场景全过，另各重复 3 次达到 60/60；硬性违规为零。未运行、运行错误、待复核未清零不得正式通过。完整基线、关键重复和校准分别展示，正式总体结论必须有匹配版本的有效证据。

通过率 = 合格数量 / 计划数量；优秀率 = 优秀数量 / 计划数量，优秀包含在合格中。错误和待复核始终保留分母。没有综合加权分，任何高分不能抵消严重错误。分数、样本通过率和真实用户总体成功概率不是同一指标。

保留机器原判、人工意见和复核后有效结论；正式门槛采用有效结论。人工推翻语义判定须有原因与证据；执行错误和客观规则失败不能靠人工勾选变成成功，须修复/纠正评测后复跑。旧版本结果只按当时标准解释，跨版本趋势标明不可直接比较。

耗时、首字时间、调用次数和提供方 usage 先观测；不估算缺失费用，不新设延迟硬门槛。超时导致执行失败仍计错误。

## 4. 坏例回流与飞书

工作流：发现失败 → 核对证据 → 确认根因 → 待确认修复 → 修复中 → 待回归 → 核验关闭；复发重新打开。机器归因是候选，区分产品、样本、裁判、运行器和外部环境，同根因聚合影响场景。

自动采集评测轨迹、判断和失败证据并提出修复方向；Rex 确认后才实施产品修复。不自动训练模型，不自动改金标或降低标准。真实学习记录仅在 Rex 指定、脱敏后进入候选，不默认上传个人数据库。

首轮校准 25 个代表场景；后续人工复核全部严重问题和争议结果，并按模式抽查至少 10% 的机器通过记录（每个有通过的模式至少 1 条）。发现误放行则扩查同类。机器原判和人工意见都可追溯。

复用现有 Base 与四表：样本、评测批次、结果明细、问题处理。新增领域、阶段、标准/校准状态、运行进度、优秀分层、有效结论及抽查标记。总览展示模式/领域表现、关键稳定性、错误和待复核、新增失败/已修复问题及相同标准下的版本比较。

原始 JSON、样本、参考与评分标准保留本地版本；飞书作为日常查看与复核入口。稳定业务键去重、分页完整、同步保留人工字段；同步失败保留本地并可重试，不重新调用模型。历史与当前标准分区，正在运行的批次不提前替换已完成基线。

## 5. 持续执行与启用

- 默认每小时检查相关已提交变更，生产代码、评测器、样本、参考或模型配置版本变化才运行 25 例回归，同版本不重复触发。不以普通文档或未提交编辑触发。
- 默认每周一 10:00（Asia/Shanghai）运行 100 例完整基线与 20×3 关键重复；错过后恢复时最多补一次，不积压重跑。
- 正式验收只使用同代码、样本、标准、参考、工具环境和模型配置的有效完整/重复批次。每个批次运行前后核对源码；变化则标无效，不用于门槛。
- 使用本机当前任务的自动化；电脑与桌面应用须可运行。定时配置先准备为暂停，校准及调度自测通过后启用；未校准期间允许显式手动运行采集机器结果。
- 默认并发 2、每场景最多 40 次实际 SDK 调用（含裁判/重试）、最长 600 秒；批次串行，运行中的新触发只保留最新待测版本。故障不无限重试。
- 只在新增严重问题、退步、质量结论变化或需人工处理时通知；不通过飞书 IM 发消息。联网观察独立，不进入固定基线门槛。

## 实施与验证要求

仅扩展评测接口与数据：去掉固定 40/8 限制，使用版本化覆盖与门槛配置；新增优秀、适用性、领域、校准及复核有效结论，提供回归和裁判核验入口。v1 历史结果仍可读取，不机械映射旧维度到新维度。生产 API/模型配置/原生数据库不变。

验证覆盖分组与参考有效性、模式门槛、关键重复完整性、错误分母、硬失败不可抵消、N/A、优秀条件、人工复核与版本绑定、历史兼容、同步去重和人工字段保护、变更检测/每周补跑/锁与版本失效。样本和模型替身测试只证明评测器行为，真实质量须真实 Harness 与人工校准证据。

验收顺序：文档核对 → 规则及服务回归 → 对照材料/人工校准 → 裁判核验 → 新基线 → 飞书数据与真实界面 → 调度自测和启用。人工校准或界面权限未完成时分别标记，不提前写为通过。

## 首次真实诊断后的评测修正

v2.0 的 7 例机器探针得到 1 合格、1 失败、2 运行错误、3 待判；30 条候选对照与机器等级一致 14/30，严重错误误放行 0。尚未人工签认，不能将该一致率作为裁判准确率。差异主要包括明确严重错误被同时标“待判”、合格与优秀边界不一致及严格原文证据校验失败。

实施修正为 v2.1：已验证原文的严重违规直接计失败，即使裁判同时要求人工复核仍阻断；其他不确定性保留待判。保存裁判原始结构化输出和具体校验原因，仍要求原文引用，不自动放宽匹配或重试求通过。问题攻克的正向脚本在首轮明确固定练习题和校准范围，避免把预设答案交给模型临时另出的题；用户指定的迁移题仍须真实被提出并作答，不注入状态完成。新版本保留原批与原样本，重新做实际探针。三档对照仍待 Rex 校准，不根据冻结核验结果自动改标签或调低门槛。

v2.1 的 6 例重新探针：2 合格、2 失败、0 执行错误、2 待判。A11/S11 裁判改写或缩略了原文引用，校验正确拒绝；T21 未实际检索/读取而违反场景要求；P11 仍未正确完成指定作答/迁移流程。P11 的机器严重错误分类也存在把否定反馈误作虚构掌握的争议，必须人工校准，不能当作已确认产品严重缺陷。此轮只验证链路和暴露问题，没有运行新版完整 100+60 正式验收。详见 [v2 交付证据](evidence/2026-09-08-harness-evals-v2/README.md)。

## v2 日常使用与人工校准入口

当前标准 `2026-09-08.v2.1`。本地规范先提交推送为 `41d7e4d`，评测实现检查点为 `77493c3`，真实诊断后的修正为 `6aae0d4`。当前数据从 `agent-service/evals/data/` 读取；旧 40 例保留在 `data/legacy-v1/`，历史报告继续读取各自批次快照。

人工确认可直达 [25 组评分对照](https://my.feishu.cn/base/ZDJeb5B9na4TAgs8PDncQRMKnFh?table=tblPGOYnuaNa0dxp&view=vewCz7RGJA)。

看板的「完整基线计划」为 0 时表示尚无新版完整基线；飞书求和指标会显示 0，不能将此解读为实际通过率 0%。

日常打开 [飞书质量总览](https://my.feishu.cn/base/ZDJeb5B9na4TAgs8PDncQRMKnFh?block=blktXbRhatkjdPTQ)，不必每次询问 AI 才能知道表现。没有新版完整基线时，已有机器探针只供排查，不能补足 100 例分母或作为总体通过率。

1. **确认评分锚点**：样本表 →「v2评分对照校准」，共 25 行。对照「合格条件 / 优秀条件」阅读三档候选。认可三档边界后填「校准人、校准时间」，勾「对照确认」；不同意则保留未确认，在「校准说明」写具体差异。本地同内容见 [25 组校准材料](../agent-service/evals/data/calibration-review.md)。候选由代理编写，尚非专家或用户验收后的金标。
2. **查批次**：评测批次 →「v2批次与进度」，看运行状态、计划数、已完成数、通过/失败/错误/待判、优秀率、标准校准状态和版本比较。运行中按已完成场景更新，最多每分钟同步一次；完成基线和关键重复各自保留，不让未完成批次抢占基线。
3. **复核具体回复**：结果明细 →「v2抽查与争议」，看对话、断言和证据。填写「复核等级、复核人、复核时间、人工原因、复核依据」。复核必须匹配结果指纹和当时标准；客观失败/执行错误不能被人工改成通过。机器原判始终保留。
4. **处理坏例**：问题表查看机器根因候选，在「根因类别、根因组」确认判断。相同根因可用同一组名汇总；机器不自动断言根因一致。填「关联复测」记录修复后批次；确认修复和回归后关闭。较新的批次再次失败会重开为待分析；旧批重传不会倒退处理进度。

以下命令由代理或维护人员执行，路径使用绝对路径。`run-evals.sh` 可通过 `REVIEW_TODAY_TEST_PYTHON` 复用主工作树现有 Python；独立工作树运行模型时，仍显式传两个原配置 `--env-file`，不复制密钥。

```bash
# 数据完整性检查，不调用模型
bash agent-service/run-evals.sh validate
# 发布场景及对照；保留已有人工意见
bash agent-service/run-evals.sh publish-calibration --config <飞书配置绝对路径>
# 拉取真实签认；不会替用户确认
bash agent-service/run-evals.sh pull-calibration --config <飞书配置绝对路径> --output <校准目录绝对路径>
# 运行 25 例日常回归；显式 --live 才调用模型
bash agent-service/run-evals.sh run --live --suite smoke --feishu-config <配置绝对路径> --calibration-dir <校准目录绝对路径>
# 拉取结果复核并重建有效结论，再同步看板；无需重跑模型
bash agent-service/run-evals.sh review <批次绝对目录> --config <配置绝对路径>
bash agent-service/run-evals.sh sync <批次绝对目录> --config <配置绝对路径>
```

裁判核验入口（在 `agent-service` 内，以项目 Python 执行）：`python -m evals.calibration --live --split verify --output <校准目录> --env-file <主配置> --env-file <DeepSeek配置>`。15 组开发对照使用 `--split calibrate`；冻结核验的 10 组对照不能用来调优后继续冒充未见样本。判分、内容或材料变更需升版并补新的冻结核验组。

完整验收依次显式运行 `run --live --suite full` 和 `run --live --suite critical`，再用 `python -m evals.assess <完整批目录> <重复批目录> --output <汇总结论文件>` 核对版本及门槛。即使机器指标达标，只要校准待完成，总体仍为待校准。`comparison.json` 对相同标准、参考、裁判、模型角色和批类型比较机器新增失败/恢复；跨标准明确标不可比。

自动任务已在本任务内创建：`review-today`（每小时检查）、`review-today-2`（每周一北京时间 10:00），均为 **PAUSED**。调度入口为 `python -m evals.schedule`；不带 `--execute` 只判断，不收费。当前已提交版本的实际检查正确返回 `human_calibration_required`。锁与周期决策已做受控验证；首次真实定时触发、断网恢复和电脑休眠后补跑还需在校准完成后验收再启用。每周任务首先建立周期记录，此后小时检查可补最近一次漏跑。

网页验收与 CLI 授权分开：本轮内置浏览器到达飞书登录页，因此数据/图表计算可用接口核对，布局和实际填表体验仍需 Rex 在已登录浏览器验收。

## v1 已实现入口与历史证据

`agent-service/evals/` 包含版本化 JSON 样本、固定来源、rubric、真实 Harness 运行器、独立裁判、规则/门槛计算、Markdown 证据报告及飞书 CLI 同步。没有修改生产 Harness、供应商配置或原生 App。每批次自动留存数据/rubric 副本、生产源码及运行器哈希；中途改生产源码的批次不得过门槛。

从仓库根运行（使用项目现有 Python 环境）：

```bash
bash agent-service/run-evals.sh validate
bash agent-service/run-evals.sh run --live --suite calibration --feishu-config ../output/harness-evals/feishu.json
bash agent-service/run-evals.sh run --live --suite full --feishu-config ../output/harness-evals/feishu.json
bash agent-service/run-evals.sh run --live --suite critical --feishu-config ../output/harness-evals/feishu.json
```

注意：脚本进入 `agent-service`，因此 `--feishu-config` 应传绝对路径，或使用 `../output/harness-evals/feishu.json`。首次创建工作台用 `provision --config <本地配置路径>`，之后复用同一配置；不要重复创建 Base。当前工作台见 [飞书质量总览](https://my.feishu.cn/base/ZDJeb5B9na4TAgs8PDncQRMKnFh?block=blktXbRhatkjdPTQ)。配置保存 Base/表/图表 ID，不含认证令牌；认证仍由飞书 CLI 管理。

`--env-file` 可重复指定原项目 `.env` 和 DeepSeek 私密环境文件（独立工作树使用）；不复制密钥进仓库。`REVIEW_TODAY_TEST_PYTHON` 可指向已有虚拟环境 Python。默认每场景最多 40 次真实 SDK 请求（包括裁判/重试）、600 秒、并发 2；关键批固定 3 次，不无限循环。`--case <编号>` 可复现单个开发失败，单题结果不满足完整门槛。`--environment online` 显式开启真实公开网络，成绩不混入固定来源门槛。

以下为 v1 历史流程：`run --feishu-config ...` 在批次结束时自动同步；v2 已追加运行中的批次进度同步。同步失败保留原始成绩，以 `sync <批次绝对目录> --config <配置绝对路径>` 重试；不重跑模型。`report <批次目录>` 从原始 JSON 复算本地报告。`review <批次目录> --config ...` 拉取人工意见单独保存，不覆盖机器原始分数。

在飞书：打开「结果明细 → 失败与异常」查看回复/诊断/修复入口；「人工校准」视图填写「人工结论、人工原因」。问题表从「待分析」走到「待确认修复」，Rex 确认后才进入修复；状态更改只是工作记录，不直接触发代码执行。样本表的「候选变更备注」用于提出修改，必须审阅后更新 Git 的场景/rubric 版本。误判不会自动变成新的金标。

## 首轮校准与验证记录

- 生产基线 `3428a79`，实施分支 `codex/harness-evaluations`；文档检查点 `9c543c8` 已先提交推送。
- 初版 v1 的 12 场景机器校准批 `20260908T085136Z-calibration-af0ba8`：5 通过、4 失败、3 运行错误、0 已检出的硬失败。原始批次已同步飞书，不代表真实用户总体通过概率。
- 校准发现 O04 把“表达理解”与 UI 保存按钮放进同一请求，和实际按钮路径不符。v1.1 将其拆为表达理解、绑定当前版本保存、模拟 ACK 三步；保留原版证据。标准未降低，产品未修复。新版本单题复验仍遇到 `RT.INTENT.INVALID_TARGET`，如实记录。
- 311 项 Python 测试通过（含 23 项新增评测器测试），覆盖计划分母、模式/重复门槛、硬失败、裁判缺失/证据、真实请求计数、导入无副作用、重复同步/人工意见保护和不确定写入去重；模型替身测试不作为质量分数。
- 原虚拟环境缺 pytest；只在临时测试依赖目录安装 pytest 8.4.2 / Black 25.12.0，未变更日常服务环境。全量 pytest 使用隔离库、空模型密钥，311 passed；既有 FastAPI startup 弃用警告 2 条。
- 飞书四表及总览已创建。初批回读 12 条结果、通过标记之和 5；图表计算返回通过 5、失败 4。后续完整基线、稳定性批和重复同步验证见本次交付证据。
- 原生 SwiftData/FSRS/UI 验收与人工裁判校准仍待完成。浏览器界面验收需要飞书网页登录态，与 CLI 授权不同。

实现校准追加：O06/S08 的重启调用参数是运行器缺陷，修复为真实 `recover()` 接口并新增真实 SQLite 重启集成测试。v1.2 明确 A06 为纯停止协议，只检查实际停止、无正文及无保存副作用；语义维度不适用，不让模型因没有继续教学而扣分。规则评分只允许这种停止场景，不能用来绕过正常教学评分。旧批次保留并标明运行器问题；新完整基线重新运行。飞书人工待复核指标改为对每条试验数 1 求和，解决平台未分组 COUNTA 返回空数据的问题，已实读返回 12。

最终交付：v1.2 完整基线 25/40（62.5%），关键重复 21/30、其中 4/10 场景三次全过；均未达到门槛，已检出硬失败 0，裁判仍待 Rex 校准。全量服务回归 314 项通过，飞书显示/分类调整后 29 项评测器专项通过。主看板优先显示最新完整固定来源基线，关键重复独立展示；联网观察不替换基线或混入趋势。逐项结果、运行器已修复项、原始合成记录和验证边界见 [交付证据](evidence/2026-09-08-harness-evals/README.md)。

飞书收尾验证通过：重复同步前后均 153 条历史结果、未新增重复，人工备注写入后经同步仍保留并恢复测试标记；11 个指标实读与本地一致，详见证据目录的 `feishu-verification.json`。当前基线 25/40、关键重复 21/30、待人工复核 40，联网观察不影响主基线选择。同步过程中遇到一次只读字段查询 EOF，原始结果保留，重跑同步恢复成功。
