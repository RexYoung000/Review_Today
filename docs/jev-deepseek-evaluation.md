# Jev 与 DeepSeek 独立判断评测

## 题目独立复核（2026-09-21，已实现／隔离验证）

在隔离教学题目节点增加必经的独立 LLM 复核，Jev 返回通过也不跳过；遇 Jev 故障／不确定时由同一次复核承担回退，不重复调用 LLM。两方结果存在否定或 LLM 不确定时，定向修正一次并重新检查；复核未完成不能依靠 Jev 单方发布。完整边界见 [Harness 复核契约](agent-harness-v2.md#教学题目增加独立-llm-复核2026-09-21已实现隔离验证)。

优先复用上一批原生 RAG 额外必答点、光合作用额外原料点以及正确题目对照，运行新实验文件，不覆盖旧记录。分别核对实际拦截／修正／发布、两方语义错误和服务故障，记录新增 LLM 请求、用量、耗时与估算费用。此批不改变卡片检查的放行规则，也不扩展日常自动启用。

719 tests / 327 subtests 通过。初轮 v3 自动结果 1/3，含一条 LLM 语义漏检与一条修正后格式失败；v4 收紧必要性规则并简化返回结构后，开发回归 3/3，两条错例保持题目不变、去除多余必答点并重新检查。v4 共 12 次 HTTP（5 次 Jev、5 次复核、2 次修正），流程中位 8.526 秒，已知费用 0.001943–0.003576 USD，1 次用量缺失；不能把开发错例复验当独立准确率。原生两轮共 12 次 HTTP，教学题复核本身 1.008 秒；只答题目所问两个要点已通过。全部原始结果、人工复核与开销拆分见 [本批报告](evidence/2026-09-21-jev-question-review/README.md)。

## 卡片与出题质量扩展（2026-09-21，已实现／隔离验证）

本轮只实施两项：替代知识整理中有界的语义检查，以及在展示教学检查题前核对题目／标准／真实参考的一致性。遵循 [Harness 质量契约](agent-harness-v2.md#jev-卡片与出题质量检查2026-09-21已实现隔离验证)，使用 `jev-quality-2` 规则与既有隔离开关，不扩展正式复习评分、排期或自动合并知识。

第一批 `jev-quality-1` 的整题检查漏掉了“题目只问光能作用，却要求列举两种原料”的额外必答点。v2 增加逐个必答点的必要性检查；该错例加入开发回归，不作为独立验证集。保留 v1 原始输出和自动通过结果，人工语义复核单独记录。

复用光合作用和 RAG 合成材料，补充本次契约所需的好稿、扩大原文结论、关键限定遗漏、本批重复拆卡、标准超出题意、错误标准、同义表达、材料内指令样例。预期来自原文和产品规则，不以基线模型作为答案。保留原始请求、响应、实际模型、采用／回退、修正前后、费用缺失及人工复核项；新结果文件不得覆盖历史。节点检查和实际生成／保存前检查分别报告，未运行的项目不算通过。

Jev 有效判断应跳过重复 LLM 检查；不确定与故障保留原因并回退。修正后的稿件再次判断，不以一次格式正确等同内容正确。出题节点尚无独立检查调用可替代，因此预期增加检查时间，必须实测，不用局部模型价格推算整轮节省。

713 tests / 327 subtests 通过；v1、v2 和修正夹具的真实自动结果分别为 19/19、16/23、1/2，自动通过不等于人工内容复核通过。v2 固定 Jev 节点 5/10 有效，另 5 次超时；LLM 对照也有误判。最终原生 Auto 教学与答错两轮实际采用题目检查，11 次 HTTP（Jev 6、LLM 5），14.492／15.953 秒，已知估算费用共 0.003491–0.006307 USD，用量完整。v2 原生仍错误放过额外必答点，因此卡片与出题两项均为“需要继续校准”，仅保持隔离测试；没有准确率提高、提速或总体降本结论。工程验证、原始失败和人工复核分开保存在 [本批报告](evidence/2026-09-21-jev-quality/README.md)。

## App 内交互测试（2026-09-21，已实现／原生真实发送已验证）

除离线/脚本对照外，增加真实模型隔离 App 的 Jev 启动入口，供 Rex 直接输入和体验。复用已实现节点、现有凭证及 DeepSeek 配置，不要求先完成更多案例或概率校准。测试与日常 App 使用不同数据、端口和应用标识；界面及健康状态能确认 Jev 配置，单次真实请求还须核对节点调用和采用记录，不能仅凭启用开关宣称生效。

已完成服务与 Mac 契约、签名构建和原生窗口检查；首次桌面访问授权阻塞的记录保留在 [启动批次](evidence/2026-09-21-jev-app-test/README.md)。随后本机授权已恢复，Rex 实测暴露问候固定覆盖和反馈失当，本轮已修复并通过原生三轮发送：3 次 Jev、3 次入口 LLM，首轮实际保留 Jev 局部结果，反馈由 LLM 处理，原历史不变。最终 12 轮合成对照与 691 tests / 320 subtests、初轮话术失败和调用费用见 [回复反馈修复证据](evidence/2026-09-21-reply-feedback/README.md)。这证明接入与本次修复真实生效，不代表日常自动接管准确性已校准。

## 入口逐项合成优化（已实现／隔离验证）

上一批入口 1/31 是完整采用率，不是准确率：15 次连接/超时、5 次混合或保留职责、3 次局部不确定、7 次完整替换。原规则允许首次问题标成延续，且单项 `unsure` 会导致整组回退，需先修正分工与合成。

本次按 [Harness 入口契约](agent-harness-v2.md#入口逐项合成修订) 实施 `jev-entry-2`，保留一次 LLM 入口调用和日常默认关闭。使用现有 31 场景与新建临时数据库重新比较原 DeepSeek 和 Jev 分支，保存新的输入输出与代码/规则摘要。按字段报告程序确定、Jev 保留、LLM 补判/修正；按轮报告完整采用、部分采用、保留职责和调用失败。完整 LLM 替换时，即使标签相同也不算 Jev 实际采用。

只补新增契约检查：首轮关系、手动模式、单字段不确定、局部冲突、未补齐字段、社交与工具冲突、控制/边界/跨会话优先、缓存规则失效及旧记录兼容。保留真实语义错误和故障，不用概率阈值提高采用率，不以新旧两批服务可用性差异宣称准确率提升。

681 tests / 310 subtests 通过。62 轮真实对照中，Jev 分支整轮自动检查 31/31：11 轮保留明确局部判断、3 轮采用局部 LLM 更新、15 轮完整交回 LLM、2 轮 Jev 超时；基线 30/31，一次讲解格式失败。Jev 分支仍有 62 次 DeepSeek 调用，另增加 31 次 Jev，整轮中位耗时 6.321 秒对 2.389 秒。已知费用分别 0.017539–0.032877 / 0.012101–0.024202 USD，实验一次用量缺失。合成机制已生效，但没有证明准确率提高、提速或降本，入口仍仅隔离使用。逐项采用、3 个实际补判、原始错例和后续话术限制见 [本批报告](evidence/2026-09-20-jev-entry-fields/README.md)。

## Harness 职责分工接入（2026-09-20，已实现／隔离验证）

在已有独立评测后，新增默认关闭的真实 Harness 判断能力，使用独立实例和合成数据；日常模型配置、正式复习接口和排期不变。本批不再以扩大样例库或先确定置信度阈值为前置条件，复用已有回归材料，只补接入契约与失败路径需要的案例。

采用范围：入口主意图/Auto 模式/当前会话关系/联网需求；有效学习记录与实际搜索结果筛选；明确结论与正文的支持判断；按预先固定题目标准检查要点与核心误解。自然语言控制、产品边界和跨会话续学保留 LLM。入口仍允许一次 LLM 检查；独立筛选成功时不重复调用原模型。完整契约见 [Harness 主规格](agent-harness-v2.md#jev-局部决策接入2026-09-20已实现隔离验证)。

评分标准在出题时冻结；程序以必需要点全部满足且无核心误解计算通过，提示/独立作答/迁移条件另由原程序检查。缺标准旧题保持原路径。Jev 默认五秒、零自动重试；不确定和失败回原节点；取消/过期不得回退继续；认证失败停用实例。所有调用受既有整轮预算约束。

新实验同时记录节点原始判断、最终采用结果、真实分流、HTTP 次数、已知/缺失用量及费用、中位数与范围。采用完整真实 Jev 调用，不复用旧缓存冒称实时耗时；预期只取现有产品契约。关闭能力必须保持原行为。局部语义错误和整轮故障如实交付，日常启用继续等待独立校准。

已完成 116 次隔离对照、224 次 HTTP 请求（Jev 57）。实验分支整轮自动检查 31/31，基线 30/31；入口仅 1 次直接采用，其余为保留职责、冲突、不确定或调用失败，整轮中位耗时 4.764 秒对 2.488 秒。记录和网页候选各 6/6 实际采用且无真实误选；答案评价最终 12/12，含 2 次超时兜底；证据两次由 Jev 判断、一次保留复杂核验。已知费用 0.041310–0.080162 USD，另 15 次用量缺失，不能当总账单或降本结论。

真实出题复核发现两份模型摘录不能逐字匹配实际讲解，原先使标准未绑定；已修复为程序绑定出题时实际参考原文，保留模型生成的要点规则，并单列生成式作答流程验证。优先候选筛选小范围试点，学习评价与证据继续校准，入口暂不用于日常自动接管。全部原始结果、重复条目复核、话术问题和复现入口见 [本批报告](evidence/2026-09-20-jev-harness/README.md)。

## 首轮独立评测契约（2026-09-20，历史批次）

状态：独立评测器已实现，55 个样例、每方 299 项判断的首轮真实对照已完成；7 次无效响应如实保留，不能称为全部通过。代码基线 `58e6541`，文档检查点 `0b8a4f7`。只建设评测工具和证据，不改变 App、服务业务 API、供应商配置、学习状态或复习排期，不放行 M1/M2。

### 样例与判断标准

- 对话：复用案例库当前全部 31 个固定合成场景及完整本轮前文/任务状态。逐项按既有契约标注对话类型、知识请求、暂缓/续学/下一主题、编程与资源边界、会话修复、网页外发范围。标注由代理依据现有产品契约整理，尚未经过 Rex 的逐条验收；混合资源请求比较规范化之前的语义边界，不把最终投影 `none` 当作模型应忽略代办部分。
- 候选：学习记录与网页来源各 6 组。覆盖直接相关、弱相关、全部无关、重复、注入式干扰、概念歧义（学习记录）和官方域名限制（网页）。仅允许输入中提供的候选 ID；本机确定性逻辑执行去重、域名过滤及最多 2 条记忆/3 条来源的限制，不把这些程序能力归功于模型。
- 评价：光合作用、RAG 各 6 条答案，涵盖正确、同义、遗漏、错误、正误混合、自述理解。逐个检查必须覆盖的语义点，并独立检查核心误解。派生的“具备完整作答证据”仅用于离线诊断，不是实际掌握判定、Again/Hard/Good 或 FSRS 写入。
- 两方使用同一份 state 和 questions。期望结果与出处只进入报告，不发送给任何模型；DeepSeek 回答不作为标准答案。各问题采用有穷 Choice，含 unsure；概率保留原值，DeepSeek 自报概率不视为已校准概率。

### 模型、执行与结果

- Jev 固定 `jev-1.13.0`，仅调用 `https://api.typesafe.ai/v1/systemone`；不走 OpenAI 兼容客户端或未经核实的网关。
- DeepSeek 使用现有节点角色：对话/记忆使用 router，需核验的网页来源使用 risk，答题评价使用 coach；保持现有模型 ID，智能模式不启用思考。只读取现有配置，不改写 `.env`。
- 统一记录样例/问题/代码摘要、完整实际材料与输出、请求模型/实际返回模型、耗时、输入/输出/缓存 token、收费请求数、错误与人工复核项。结果文件仅新建，不覆盖、拼补或掩盖历史失败。
- 默认离线校验，真实调用必须显式 `--live`。不导入或启动 App/Harness/日常数据库，不进行网页搜索、抓取或知识写入。服务错误最多额外一次；认证、权限、无效结构不重试，不切供应商。取消执行保留已完成记录和缺失项。
- 未知标签、缺失或多余问题、无效概率、非法候选、模型版本漂移、超时均为失败/待复核，不能折成负向语义答案。报告区分“执行完整”和“语义全部命中”。缺结果、空计划、重复结果、截断或记录/汇总不一致不能通过报告校验。
- 准确率与每项错误对照人工预期；候选额外计误选、漏选与重复；评价额外计带误解的答案被误判完整。`unsure` 与调用失败计需要回退的样例；不以任意 confidence 阈值宣称自动处理率。
- 每样例每模型一次有效判断，不通过重复抽样选最好答案。模型轮换先后顺序；首次调用单列，报告中位数/范围。费用按可核实的当日官方价格估算；缺失用量或单价保留未知，不当作零费用。

### 验证与采用边界

先完成夹具/报告校验与故障注入测试，再运行正式两方对照。纯局部分类不会产生完整 IntentDecision 或实际回复，不重用端到端 PASS 名义。本轮另用现有 DeepSeek 流程执行全部 31 场景的完整 Harness 回放，使用既有隔离执行器，阻断网页与知识写入；作为独立行为层报告，不与局部判断混算。本批不接入 Jev 到 Harness，因此不宣称 Jev 整轮行为通过、App 提速或产品验收通过。

最终按类别给出“适合小范围试点 / 需要继续校准 / 暂不采用”，保留所有错误和人工复核。不根据这组开发样例制定自动接管阈值；阈值必须在选定试点后使用独立样例验证。

## 首轮结论

推荐顺序为学习记录筛选 → 答案要点/误解检查 → 网页候选筛选；完整对话路由暂不接管。

| 接入点 | 结论 | 收益与限制 |
| --- | --- | --- |
| 学习记录候选筛选 | **适合小范围试点，优先 1** | Jev 6/6 组整例命中、14/14 个标签正确，局部调用中位数 369 ms；两方最终清单均无漏选或误选，尚无筛选质量提升证据。先做旁路建议，既有记忆授权和归属检查继续由程序执行。 |
| 答案要点与核心误解检查 | **适合小范围试点，优先 2** | Jev 12/12 条、54/54 项符合标注；未把遗漏、误解或“懂了”当完整答案。可提供诊断证据，不自动修改掌握或排期；仅两个主题的短答案，长答案与隐含误解尚未验证。 |
| 网页候选筛选 | **需要继续校准，优先 3** | Jev 最终清单 6/6 正确、相关标签 12/14；差异均为 weak/irrelevant。官方域名过滤和去重是程序保护，不能归为模型能力。需补更多“相关但不支持结论”的真实形态后再选试点。 |
| 对话局部信号 | **需要继续校准** | Jev 判断命中 206/217，但整例仅 23/31；会话修复、承接代办、社交词附带转场仍会误判。可用于观察，不能仅凭一个标签提前终止教学流程。 |
| 完整路由、教学生成与掌握状态写入 | **暂不采用** | 本次没有实现 Jev 的完整路由/生成替代，也没有与真实学习表现建立联系；局部速度与正确率不能证明整轮体验改善。 |

自动接管阈值未设置。后续选定试点后，须用独立样例校准；本批错误保留，不通过改标注或重抽样提高分数。

## 对话准确率校准（2026-09-20，已完成实验／本轮不接管）

本批只扩展独立评测器。固定 `jev-1.13.0`，比较 v0 原始上下文与七个问题、v1 只整理上下文而问题不变、v2 使用 v1 上下文并拆成原子问题。保留原始 31 场景及七项预期不变；v2 的原始问题正确率与程序组合后的七项契约正确率分别统计，不将程序纠正归功于模型。

上下文整理只使用现有原话和实际状态：最近用户请求/助手答复、带位置引用的前文、当前任务与步骤。保留完整相关文本和原始副本，不利用预期答案生成摘要；其他会话目标不参与当前意图分类，跨会话候选选择仍未接入。

先冻结三版规则，再新增 24 个代理编写的保留验证场景，涵盖省略指代、纠错、混合请求、长前文和产品边界；不是外部人工盲测。开发集为原有 31 场景，同一来源的改写不拆到验证集。先运行开发集并记录推荐版本，再运行保留验证集；验证结果不用于本轮继续调提示词或挑重跑结果。

新增离线组合检查：实质请求与闲聊冲突、进度与当前任务不符、缺失/不确定字段均交回复核。仅报告回退需求与漏检错误，不设自动接管阈值、不执行学习状态或任务写入、不替代完整 Harness 行为验收。原始输入输出、规则与样例摘要、全部失败、耗时和用量分别落入新证据文件；不覆盖首轮记录。沿用已授权的官方 HTTP 评测适配器，不修改 MCP 或供应商配置。

开发集 93 次调用、保留验证 72 次调用均已完成且有效。开发集 v0/v1 整例均为 24/31，v2 经组合后为 26/31；先冻结 v2 为候选再验证，验证集 v0/v1/v2 分别为 18/24、20/24、17/24。v2 的开发收益未在新场景延续，验证中的 7 个错例均未被本轮一致性检查拦截，因此本轮不采用。v1 的局部改善作为后续方向，不在看完结果后改选并宣称通过。

38 项专项测试及全量 604 tests / 245 subtests 通过，原始记录、计量、验证前选择与逐项缺陷见 [校准证据](evidence/2026-09-20-jev-calibration/README.md)。本批只完成输入/问题拆分对照和离线组合检查，没有 Jev 完整 Harness 接入或整轮通过结论。

## 三类边界单项优化（2026-09-20，已完成／不接管）

Rex 已授权开始下一轮。保留日常 App 与供应商配置，沿用独立 HTTP 评测适配器；旧 55 场景全部转为回归开发集，不再称为保留验证。原七项预期不改写，标签差异、潜在路由风险和实际整轮行为分开报告。

本轮固定五组：v0 原版、v1 上下文整理，以及基于 v1 的 scope（只明确当前指令/引用/历史的作用范围）、resource（只收紧资源代办定义）、progress（只明确知识请求与学习推进及承接对象）。全部保留七项输出、不组合原子判断、不按关键词覆盖答案。每个变体修改的字段预先固定，完整输入、概率、耗时、用量和异常落入新文件。

先冻结规则和新增的 48 个保留验证场景，再在 55 场景上运行五组配对对照。新场景来自代理按不同情境构造的合成对照组，不是外部人工盲测；同一情境的正反例全部留在验证侧。旧预期与新预期均依据已有契约，不能根据模型结果改答案。

每项修改仅在目标字段错误减少、整例命中不下降、总标签错误不增加、没有新增潜在高影响路由风险时，才允许成为候选。潜在风险只表示错误标签可能影响分流，不冒称 Harness 已执行。若多项合格，再在回归集单独验证组合版，保留组合带来的新错例；组合不合格就按潜在风险、整例命中、标签错误依次选择单项版。候选和选择依据在运行验证集前写入不可覆盖的选择文件。没有合格候选也记录结论，不强行接入。

验证只比较预先选择的候选与本轮 v0/v1；调用失败、缺失结果和 unsure 独立报告为需回退，不作为否定答案或成功。看过验证结果后不继续调提示词、改标注或择优重跑。未拦截错误、潜在风险、整例与逐项正确率、请求数、耗时和费用同时报告，不在本批拟定自动接管阈值。

只有新验证集同时改善整例与潜在风险、无新增高影响错误，才进入隔离 Harness 的有限整轮实验；七项判断不能直接补成完整 IntentDecision。若未达到条件，本批以评测结论和可重跑工具交付，完整路由继续由既有 DeepSeek 处理。

### 资源字段的隔离整轮实验

三项改动中只有 resource 满足回归门槛，验证前选择提交为 `38750e2`。新增 48 场景中，resource 的资源字段为 48/48（v1 为 45/48），整例 38/48（v1 为 35/48），潜在风险 8 例（v1 为 10 例），无新增整例错误或潜在风险，达到有限整轮实验条件。v0 整例同为 38/48，不能宣称候选已经胜过原版完整判断。

有限整轮实验复用现有全部 31 个固定 Harness 场景及断言，分别运行当前 DeepSeek 和“只替换资源字段”的实验分支。DeepSeek 仍生成完整 IntentDecision、知识片段和回复；只在本轮第一次意图识别后，注入同场景已记录的真实 Jev resource 标签，保留 DeepSeek 原始输出与替换前后对象。不将 Jev 七项标签伪装成完整意图，也不修改生产解析器。复核项/失败沿用 DeepSeek 原判断，并记录回退原因。

Jev 结果取自已冻结回归证据，避免重抽样改变输入；这属于已知场景的干预回放，不是新的独立语义验证。每个分支使用新的临时数据库，轮换先后，网页、知识写入继续阻断并记录；学习状态变更只发生在合成临时库。记录真实整轮状态、回复、断言、语义调用数和耗时；缓存的 Jev 判断不算本轮新网络请求，因此该耗时不能用于宣称 App 提速。沿用原有模型配置、不校准生产自动接管阈值。

本批已经完成 419 次有效 Jev 调用及 62 轮整轮回放。整轮旧断言两分支各 30/31：DeepSeek 一轮超时，实验分支一轮将资源能力咨询标成实际代办。另发现 A005 编程能力问句被实验分支错答为下载资源拒绝，旧断言未捕获；依照已有边界预期补查实际分流后，实验分支为 29/31，两次差异只有 A005 是明确可见的回复退步。超时未重跑、不计为模型语义修正。故资源定义改写保留作校准候选，正式路由不接管，App/模型配置不变。分层准确率、潜在风险、实际回复、费用与局限见 [三类边界证据](evidence/2026-09-20-jev-boundaries/README.md)。

## 执行与证据

- [首轮复核与采用依据](evidence/2026-09-20-jev-comparison/README.md)：错误影响、回退比例、调用/用量/费用、分层验证与局限。
- [真实原始记录](evidence/2026-09-20-jev-comparison/run-01.jsonl)：完整共同输入、固定期望、请求/响应、概率、模型、时间、用量、错误、代码与样例摘要。
- [机器重算摘要](evidence/2026-09-20-jev-comparison/run-01-summary.md)：所有逐项差异；该表耗时只统计有效调用，复核文档另列包含失败的全部调用耗时。
- [现有 DeepSeek 整轮回放](evidence/2026-09-20-jev-comparison/deepseek-harness-01.jsonl)：与局部比较分开记录。上一轮 15 次探索性 API 调用不混入本批正式样本。

入口位于 `agent-service/tests/run_judgment_comparison.py`。在 `agent-service` 目录运行：

```bash
# 默认离线；不读取供应商配置或日常数据库，不产生费用。
.venv/bin/python tests/run_judgment_comparison.py

# 显式真实调用：终端隐藏输入 Jev Key；也可通过 TYPESAFE_API_KEY 提供。
# 输出必须使用新路径；不会覆盖历史结果。
.venv/bin/python tests/run_judgment_comparison.py --live --jev-key-stdin \
  --output ../docs/evidence/jev-comparison-next.jsonl

# 离线核对并生成新摘要，路径不能已存在。
.venv/bin/python tests/run_judgment_comparison.py \
  --report ../docs/evidence/2026-09-20-jev-comparison/run-01.jsonl \
  --markdown /tmp/jev-comparison-review.md
```

`--cases` 可选择 dialogue/memory/source/grading 或固定 ID；`--providers` 可缩小供应商范围，子集不能冒称全量对照。报告验证返回 1 可表示记录有效、但存在调用失败或缺失；需读取汇总，而不是把文件存在当通过。本轮 110/110 个记录齐全，其中 DeepSeek 7 次无效响应，因此 `execution_complete=false`。语义差异另由 `all_assertions_match` 与逐项结果报告。

Jev MCP 在本会话未加载；此评测器使用已验证的官方 HTTP 接口，独立于 Codex MCP 配置。不会把 HTTP 调用写成 MCP 验证。密钥不写入代码、夹具、报告或仓库。

## 官方依据

- [TypeSafe 模型与价格](https://docs.typesafe.ai/models)：Jev 1.13、输入 $0.042 / 百万 token、输出免费；中文效果需自测。
- [TypeSafe API](https://docs.typesafe.ai/api)、[置信度](https://docs.typesafe.ai/confidence)、[已知局限](https://docs.typesafe.ai/model-jaggedness/jev-1.13)。
- [DeepSeek 模型价格](https://api-docs.deepseek.com/quick_start/pricing/)；执行前核对可获得的价格，不将历史价格伪装成当日账单。
