# 本轮原生交互检查

以下实际操作使用独立 QA 应用和测试数据库，不发送模型请求。日期按钮、工具栏和草稿实现已与构建对应；随后活动观察器增量不改变这些界面控件，最终版本还需做切页与服务烟雾检查。

- 压力数据 Today：完整 26 周网格正常绘制，全部历史统计显示 360 天，未来日期不可用。
- 点击 2026-09-13 日期：显示 14 次有效活动；点击「隔离会话 61」后，窗口标题和历史消息均属于该会话。
- 会话 1 输入「隔离验收 A：保留中文草稿」，切到会话 2 无串稿；输入独立 B 草稿后返回会话 1，A 草稿完整恢复。
- 学习方式菜单：方向键与 Return 从 Auto 选择「知识整理」，菜单关闭，按钮标签同步；思考强度菜单 Escape 关闭，保留原选择。
- 深色界面：选中会话、输入焦点、菜单与日期网格可区分。
- 拖窄窗口：侧栏自动收为图标栏，输入工具栏保留一组，正文与日期网格通过原有滚动容器承载；没有重叠控件。

未以这些功能检查声称自由指针、完整 VoiceOver 或端到端帧时间已通过。
