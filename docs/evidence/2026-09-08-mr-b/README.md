# 当前：A 循环整理 → B 收尾 → 盖章（2026-09-09，视觉验收通过／工程自测完成）

Rex 于 2026-09-10 明确确认当前 A → B → 同纸面 2D 盖章已完成视觉验收。此前的「待视觉确认」状态由本条覆盖；下方历史记录保留。

逐行消除现在表达等待过程：A 持续清除、上移、补入横线；点击「完成并保存（模拟）」后，B 继承当前步态，停止新增行，清空剩余内容并站稳，再接同一纸面的盖章。成功文案立即更新，演出不决定保存状态。此次不连接真实生成、保存、服务或日常数据。

- A 保持两三条可见横线，不表示条数、百分比或预计剩余时间。B 接住已经出现的补行，避免成功信号到达时横线或身体跳变。
- 失败／取消冻结当时横线，身体短暂收势，不盖章。每次运行只接受首个结果；重新开始清空结果与定位历史。暂停时可以接受信号，恢复后继续；时间条只回看已播放部分。
- 减少动态：A 静态整理，成功直接保留 R，失败／取消静态停下；关闭减少动态不重播已结束的盖章。普通后台暂停并在返回后续播，不追赶时间。

## 最终原生证据

| 文件 | 实际内容 |
| --- | --- |
| [A → B → 盖章原速录像](process-flow/flow-success-native.mp4) | 最小窗口 760×652，15.840 秒／388 帧。手动触发保存，B 清除剩余行，同纸盖章，最终演出时间 14.80 秒 |
| [A → 失败收势原速录像](process-flow/flow-failed-native.mp4) | 标准窗口 1280×820，9.468 秒／227 帧。手动触发失败，停止在部分清除的横线，不出现 R |
| [取消](process-flow/cancel-dark-minimum.png) | 最小窗口深色，停在当前横线并收势，不盖章 |
| [减少动态 A](process-flow/processing-dark-minimum-reduced.png) / [成功结果](process-flow/saved-dark-minimum-reduced.png) | 同一最小窗口深色，静态三行直接切至最终 R，播放及逐帧控件禁用 |

两段最终录像均由最终构建捕获自身窗口，目标 30 fps，保留实际时间戳，无音频、无变速；实际采样率不保证 30 fps。完整解码采用时间基准透传，无错误；默认 null 输出曾因时间基准重采样报重复 DTS，原始输出与透传复核结果保留在 [recordings.json](process-flow/recordings.json)，没有修改源录像。已查看原生窗口及录像中的 A、B、盖章、失败和结尾画面，正常录制关闭制作网格。

## 检查与限制

- [54 项动作回归](process-flow/animation-regression.txt)通过：新增 6 项覆盖 A 持续 121 秒、循环接点像素连续、抬步／擦除／承重／补行时接受成功、只收完已进入的行、失败／取消、减少动态与重启、同纸接段、接触后才出现 R；验证实际官方 Spine 顶点、附件透明度、网格无翻折及浅深／语言图形一致。保留原有动作回归。
- [原生契约](process-flow/native-contracts.txt)通过：暂停中 A → B、重复信号不覆盖结果、正反定位与完成回调去重、失败／取消不成功、重启恢复 A、减少动态、后台暂停／恢复，以及现有生命周期。新增精确结束时刻断言，修复浮点误差导致已结束画面仍显示「盖章确认」的问题。模型定位只允许已播放范围。
- **补充验证通过（2026-09-10）**：解锁后，完整原生契约及新增「A 录制时转后台继续、后台接受失败并收势」检查通过。记录见 `native-contracts.txt`。两段原速录像与最终构建使用相同的动画资源；验收确认后仅将试演说明改为「动作视觉已验收」。
- [构建与资源检查](process-flow/build-checks.json)通过。最终原生构建、严格签名、日常动画源一致和 diff 检查均通过；没有改正式 App 的动画资源。
- 原生实际查看标准／最小窗口、浅深、手动成功／失败／取消、重启、减少动态与恢复。一次标准窗口录制未完成收势、持续到 45 秒上限，窗口处于后台；回到前台的普通试演完成收势，相同构建重新录制失败流程正常。该次录制原因未完整隔离，不称为已修复录制异常，不作为通过证据；文件保留在忽略目录 `output/mr-b-preview/superseded-flow-captures/native-interaction-1788967741.mp4`。自动后台契约通过不能证明该偶发采集条件已解决。

运行 `bash tests/mac/run-mr-b-contact-preview.sh`，打开 `output/mr-b-preview/MrBContactPreview.app`，选择「A → B → 盖章」。让 A 循环几轮，再点击「完成并保存（模拟）」；分别在抬步、消除和补行时触发，重点看是否保持同一身体、纸张和动作节奏。失败／取消可通过「重新开始」分别查看。

制作方式：`settlement-flow.mjs` 负责事件排程，复用真实 Spine 网格、附件与官方渲染器，不增加身体材质、打光或文字贴图。`export-contact-studies.mjs` 输出到 `output/mr-b-preview/process-flow/`；固定短样仍有 Spine JSON／atlas，动态流程只提供明确标为离线的关键帧及模拟信号记录，不能作为独立固定 Spine 动画或原生验收录像。Spine 编辑器人工验收、完整辅助技术流程、正式业务接入和真实保存信号未验证。当前连贯演出的视觉验收已通过，正式完整／精简结算及业务接入仍待后续制作和验证。

---

# 上一版：同一纸面的消除与盖章（2026-09-09，随后增加 A／B 状态流程）

Rex 指出两段的纸张与角色站位不同，直接衔接会跳变。本轮以逐行消除为基准统一纸张、Mr. B、阴影和取景，并在隔离试演增加「消除 → 盖章连播」验收入口；仍不进入完整结算或真实业务接入。

- 两段共用 760×400 舞台中的同一纸张：中心 (380,248)、294×194；相同圆角、材质、描边与前后层。移除盖章原有的小卡和额外卡片阴影，不以淡入、缩放或换纸遮掩接点。
- Mr. B 盖章站位由 (422,175) 对齐至消除结束的 (380,154)，大小、眼神与身体阴影也承接结束状态。盖章不增加身体形变或新打光。
- 印章从右侧身后取出，完全离开身体轮廓后换至前层；落点改在纸面右下 (438,312)，避开面部，保持刚性 2D 尺寸和竖直下压。印记与轻气绑定该落点，压下覆盖 R，抬起后显露，收回后 R 留在原纸面。
- 两个独立短样仍为 4.6／4.8 秒；验收连播 9.4 秒，共用一套附件与一个时钟，4.6 秒接段时不销毁或重建画布。跨接点定位、逐帧、重播、后台暂停和减少动态沿用原控件；减少动态直接显示同一纸面上的最终 R。

## 本轮原生证据

| 文件 | 实际内容 |
| --- | --- |
| [消除 → 盖章原速连播](shared-stage/continuity-native.mp4) | 1280×820，10.445 秒／307 帧；9.4 秒演出加起播与结尾停留，同一纸面从三行消除接至盖章 |
| [对齐后的独立盖章](shared-stage/stamp-native.mp4) | 1280×820，5.855 秒／172 帧；同一站位、右下落点、接触轻气和 R 结果 |
| [接段前 4.5667 秒](shared-stage/seam-before-dark-minimum.png) / [盖章首帧 4.6000 秒](shared-stage/seam-after-dark-minimum.png) | 最小窗口深色，前后逐帧；纸张、Mr. B、眼神和阴影相同 |
| [连播减少动态结果](shared-stage/continuity-dark-minimum-static.png) | 最小窗口深色，直接显示最终 R，播放及帧控件禁用 |

两段录像由原生 App 捕获自身窗口，目标 30 fps，保留实际呈现时间戳，无音频、无变速，完整解码通过；元数据见 `shared-stage/recordings.json`。截图由 CUA 读取实际试演窗口保存。正常录屏及截图关闭制作网格。

## 验证与边界

- `shared-stage/animation-regression.txt`：**48 项通过**。新增同一纸面贯穿两段、接段附件状态一致、跨接点正反定位、R 位于纸内／被压章覆盖、悬停不遮眼；扩展浅深及两种画布适配尺寸的像素对比，消除末帧、盖章首帧和连播接点前后无像素变化。减少动态连播结果与独立盖章末帧一致。既有网格、踏步接触、刚性道具、语言像素与官方 Spine 回放回归继续通过。
- 导出检查覆盖长时间轴接点与反向定位，并修正 Spine Float32 时间精度导致阶梯帧在 6.8 秒晚读一帧的问题；检查使用官方运行时，未将离线图等同原生证据。
- `shared-stage/native-contracts.txt`：原生桥接、可见像素、暂停、定位、减少动态、一次性完成、后台及视图销毁生命周期通过，新增 9.4 秒连播与 4.6 秒接点两侧的正反定位。首跑在既有 `Mid-book stop did not settle` 检查失败，原因未确定；保留 `native-contracts-first.txt`，相同代码及测试重跑通过，没有放宽等待或删除断言，不声称已解决该偶发检查失败。
- macOS 26.7（25G220）原生实看：标准浅色原速连播与独立盖章；最小窗口深色，4.7 秒后退至 4.5667、再前进至 4.6000／4.6333 秒；切换减少动态显示最终 R。录屏实际采集帧确认接段前后未换纸、角色站位不跳，印章从右侧取出并落于同一纸面。
- 两张原生接点截图的舞台区域像素完全相同：`shared-stage/native-seam-comparison.json`，区域为 x=300、y=220、300×270，排除时间条与控件。静态一致证明接点状态，不单独证明动作观感。
- `shared-stage/build-checks.json`：原生构建、严格签名、日常动画源一致和 diff 检查通过。未修改日常 App、模型、服务或数据。

运行 `bash tests/mac/run-mr-b-contact-preview.sh` 后打开 `output/mr-b-preview/MrBContactPreview.app`，点击「消除 → 盖章连播」及「重播」。窗口已恢复标准浅色、中文、网格关闭，并停在接段位置。优先检查 4.6 秒交接以及其后取出、悬停、下压、抬起的整体连续感。

`node tools/mascot-motion/src/export-contact-studies.mjs` 输出本版到 `output/mr-b-preview/shared-stage/`，含两个独立短样及验收连播的 Spine 4.2 JSON、atlas、贴图、离线关键帧。Spine 编辑器人工播放、完整辅助技术流程、正式完整／精简结算和真实保存链路未实施或验证；最终视觉与透视表达仍待 Rex 验收。

---

# 上一版：承重与停顿细化（2026-09-09，后续反馈要求统一两段空间）

沿用 Rex 已基本认可的逐行消除和 2D 盖章，细化脚底承重／回弹、阴影、工作视线，以及盖章前后停顿。保持原有形象、两个隔离短样范围与 4.6／4.8 秒演出时长；本轮原生自测和录屏已完成，不关闭最终视觉验收。

- 踏步先压稳再擦除，完成一条后短暂承重，脚底卸力后小幅回弹，剩余行同步上移。整体重心下沉约 3 pt、回弹约 1.4 pt，底缘轻微展开；抬起时阴影变轻、落下时更集中，目光跟随当前处理位置。
- 盖章保持刚性道具与原样身体。对准后停 0.2 秒，抬高后停约 0.22 秒，再加速落到卡片并压实 0.24 秒。抬起后留约 0.38 秒看结果再收回；轻气更贴近接触瞬间，目光小幅跟随道具与印记。

## 本轮原速原生录像

| 文件 | 实际内容 |
| --- | --- |
| [逐行消除细化版](timing-polish/walk-native.mp4) | 1280×820，5.668 秒／162 帧；4.6 秒演出加起播和结尾停留，三次落步、压稳消除、卸力回弹与上移 |
| [2D 盖章细化版](timing-polish/stamp-native.mp4) | 1280×820，5.868 秒／171 帧；4.8 秒演出加起播和结尾停留，对准、悬停、压实、抬起看结果后收回 |
| [最小窗口深色静态](timing-polish/stamp-dark-minimum-static.png) | 减少动态直接展示卡面 R，播放及帧控件禁用 |
| [最小窗口深色踏步](timing-polish/walk-dark-minimum-contact.png) | 暂停在 2.233 秒，检查第二条线、底缘接触和剩余行；网格关闭 |

录像由独立 App 捕获自身窗口，30 fps 目标、保留实际呈现时间戳，无变速／音频，正常录屏关闭调试显示。两段完整解码通过，尺寸／时长／帧数及解码输出见 `timing-polish/recordings.json`。本轮证据与 `line-beats-2d/` 基准版分开保存。

## 本轮实际验证

- `timing-polish/animation-regression.txt`：**46 项通过**，包含既有回归与新增承重／卸力／回弹／阴影联动、四段停顿及眼睛范围检查。局部网格无翻折、先接触再消除、无身体字印、刚性道具与前后遮挡、浅深主题下语言像素一致、30 fps 官方 Spine 导出回放继续通过。
- `timing-polish/native-contracts.txt`：macOS 26.7（25G220）解锁后，原生 Spine 桥接、实际可见像素、时钟、暂停、定位、减少动态、一次性完成、录制时后台继续及结束后恢复暂停、视图销毁停播检查通过；复用既有中断、资格与事件去重回归。
- 初次锁屏运行曾在 `Clock not advancing` 失败，保留 `native-contracts-locked.txt`。解锁后以相同代码和测试重跑通过；没有修改检查、强制可见或取消正常后台暂停来绕过。
- 原生实看标准／最小窗口、浅深主题、两短样切换、时间条定位和前后逐帧。踏步从 2.3 秒后退三帧到 2.2 秒，再前进到 2.233 秒；盖章定位到 2.4 秒可见压实状态。减少动态时盖章直接保留 R，切换踏步直接展示清除后的纸面，关闭后恢复正常演出。
- 两段正常速度录像及其实际采集帧已查看：踏步先压稳、清除完成后停顿，卸力时剩余行上移；印章抬高后加速下压，压实阶段覆盖印记，抬起后显露 R，收回前留出观察时间。上述为代理自查，不代替 Rex 对步态和取放观感的判断。
- `timing-polish/build-checks.json`：原生 App 重新构建、严格签名、日常动画资源一致及 diff 检查通过。离线关键帧已查看并与原生录像分别标记。日常动画及正式 App 资源未改动。

运行 `bash tests/mac/run-mr-b-contact-preview.sh`，打开 `output/mr-b-preview/MrBContactPreview.app`，选择两个短样并点击「重播」。窗口已恢复标准浅色、中文、关闭网格。离线导出入口为 `node tools/mascot-motion/src/export-contact-studies.mjs`，本版输出到 `output/mr-b-preview/timing-polish/`，含 Spine 4.2 JSON、atlas、贴图及离线关键帧。

本轮仍停在两个短样。完整／精简结算、正式业务接入、Spine 编辑器人工验收及完整辅助技术流程未实施或验证；English 原生开关沿用基准版检查，本轮语言不影响演出由像素回归覆盖。测试通过仅证明所覆盖的行为，最终视觉体验待 Rex 确认。

---

# 基准版：逐行消除与 2D 盖章（2026-09-09，动作方向基本认可）

按 Rex 最新反馈完成两个隔离短样及本轮自测。三条抽象横线随三次踏步依次清除并上移；盖章采用书本式 2D 取放、抬起、按下与轻气。保留既有 Mr. B 轮廓和浅深材质，取消文字贴图、立体透视及身体包裹章柄。**Rex 反馈「差不多这个意思」，仅动作方向基本认可；未关闭最终视觉验收，未进入完整／精简结算或正式业务接入。**

## 原速原生录像

| 文件 | 实际内容 |
| --- | --- |
| [逐行消除](line-beats-2d/walk-native.mp4) | 1280×820，5.618 秒／166 帧；演出 4.6 秒，含起播和结尾停留。三次底缘踏步，每条清除后剩余横线上移 |
| [2D 盖章](line-beats-2d/stamp-native.mp4) | 1280×820，5.852 秒／172 帧；演出 4.8 秒。印章从身后取出、移至卡片上方、抬高按下，轻气后收回，留下单色 R |
| [最小窗口深色静态](line-beats-2d/stamp-dark-minimum-static.png) | 减少动态直接显示 R 结果，播放及帧控件禁用 |
| [最小窗口踏步逐帧](line-beats-2d/walk-dark-minimum-english-mesh.png) | 深色、English、2.04 秒；制作网格与接触点开启，第二条线正在消除 |

录像由独立 App 捕获自身窗口，30 fps 目标，保留实际呈现时间戳，无变速／音频；完整解码通过。元数据见 `line-beats-2d/recordings.json`。调试网格在正常录屏中关闭。

## 实际验证

- `animation-regression.txt`：**44 项通过**，包含 36 项既有动作回归和 8 项本轮检查。新检查覆盖三次落步才消除、上一行消除后才上移、擦除边界位于脚底接触区域、上半身稳定、网格不翻折、印章尺寸不变／身体不伸出、完全移出角色轮廓才换前后层、按下后才留 R 与轻气，以及 30 fps 导出在官方 Spine runtime 的正反向定位。
- 中英文在两个短样、浅深主题及多个时点的演出像素相同，动画中没有文字或身体字印贴图；轮廓和身体材质与现有日常来源一致。English 是试演参数，原生调试标题仍使用中文，不声称已完成全应用本地化。
- `contracts.txt`：原生运行时通过，含两个短样的实际可见像素、暂停、定位、减少动态、一次性完成、录制时后台继续与结束后恢复暂停、视图销毁停播；既有中断、资格及事件去重继续通过。
- 原生实看标准／最小窗口、浅深、暂停与时间条、前后逐帧、重播、切换短样、网格开关、English 和减少动态。实际从 1.84 秒连续推进六帧到 2.04 秒，确认第二条线和脚底的局部动作同步；English 切换不改变该帧。原速录像及其逐帧画面确认三条线先后消除、印章取放无换层跳变、抬起后卡面留下 R。
- `build-checks.json`：独立 App 严格签名检查、日常动画资源与源文件一致检查、diff 空白检查通过。日常动画与正式 App 资源未修改。

## 制作方式与验收入口

运行 `bash tests/mac/run-mr-b-contact-preview.sh` 后打开 `output/mr-b-preview/MrBContactPreview.app`，选择「逐行消除短样」或「2D 盖章短样」，点击重播。窗口已恢复标准浅色、中文、关闭网格。重点看每条线消除是否跟随踏步，以及印章取出、停稳、抬起、按下、轻气、收回是否自然。

`settlement-scene.mjs` 使用固定 2D 坐标与明确前后层；直接读取原有角色轮廓，四圈身体网格仅在踏步时局部变化，盖章身体保持原样。印章为一个刚性平面附件，卡片从开始已放好；所有位置、遮挡、透明度共用一个场景时钟。复用官方 Spine Canvas runtime 和日常网格绘制适配，不再使用透视投影、逐像素深度或新身体打光。

运行 `node tools/mascot-motion/src/export-contact-studies.mjs` 可在 `output/mr-b-preview/line-beats-2d/{walk_study,stamp_study}` 生成 Spine 4.2 JSON、atlas、贴图和明确标记为离线的关键帧。JSON 含 30 fps deform、alpha、drawOrder，包含阴影与轻气附件；本轮原生和离线使用同一渲染器。编辑器导入未人工验证，导出在官方 runtime 复现的检查不能替代该项。

本次是演出短样，不是通用物理模拟。真实保存／复习链路、完整／精简结算、正式业务接入和完整辅助技术流程不在本轮验证范围；工程通过不代表 Rex 认可动作观感。

---

# 历史：行走消字与取卡盖章短样（2026-09-08，后续反馈未通过）

**旧结算及首版滚印短样均未通过 Rex 视觉验收。** Rex 最新反馈取消滚动、取消身体字印，改为既有 Mr. B 用底缘波浪走过文字，经过一段消失一段。新增轮廓和高光偏离既有角色，需同时收回。此前滚印数据和录屏不代表新版通过。

当前在两个隔离原生短样内修订，完整／精简结算及真实业务接入继续等待 Rex 确认。运行 `bash tests/mac/run-mr-b-contact-preview.sh`，打开 `output/mr-b-preview/MrBContactPreview.app`；独立 bundle ID／偏好域，不访问日常数据和模型。

- **行走消字**：6.4 秒，复用既有轮廓与材质，底缘局部波浪抬起／承重；固定取景，角色不翻滚，文字仅在实际经过后消失，身上和经过后的平面不留字印。
- **取卡盖章**：7.2 秒，保留托出一张卡、放下、包住章柄、到位后垂直下压、抬起留下单色 R 的动作；角色外观同步统一。
- 原生控件支持重播、暂停、前后 1/30 秒、时间条定位、网格／接触显示。减少动态直接展示静态结果；普通播放隐藏或后台暂停、恢复不追赶。
- 主动录制期间，仅被捕获的这一个原生窗口维持场景时钟；录制结束恢复普通后台暂停。采集自身窗口，按实际时间戳，无音频、无变速。

## 本次原生证据

| 文件 | 内容 |
| --- | --- |
| [walk-native.mp4](contact-studies/walk-native.mp4) | 最终行走消字，1280×820，7.453 秒；215 帧，含起播与结尾停留，无身上字印 |
| [stamp-native.mp4](contact-studies/stamp-native.mp4) | 统一角色材质后的取卡盖章，1280×820，8.270 秒；242 帧 |
| [stamp-dark-minimum-static.png](contact-studies/stamp-dark-minimum-static.png) | 最小窗口、深色、减少动态：直接显示已盖 R 的卡片，帧控件禁用 |
| [walk-dark-minimum-english.png](contact-studies/walk-dark-minimum-english.png) | 最小窗口、深色、英文演出文字和底缘波浪，暂停在 1.28 秒 |

`recordings.json` 保存实际时长、尺寸、帧率及保留原始时间基准的完整解码结果；两段均解码通过。旧滚印和中途草稿录像移至忽略的 `output/mr-b-preview/superseded-contact-captures`，不混入本次验收。

原生实看：标准／最小窗口（760×620 内容区）、浅深主题、英文演出文字、正常速度、重播、暂停、时间定位、前后逐帧、网格／接触开关、场景切换中断、减少动态直接静态。逐帧确认 3.20 → 3.233 秒，底缘局部变化而面部不转动；文字在覆盖区清除，离开后不再露出。盖章录像确认托出卡片、章体到位再下压；减少动态直接显示卡面 R。恢复后台不追赶由原生检查覆盖。窗口已恢复标准浅色中文，点击“重播”即可看新版。

## 制作与验证边界

`settlement-character.mjs` 直接读取现有角色轮廓，播放器直接复用现有 `material.mjs` 的浅深身体贴图，不增加身体高光。专用身体、眼部、卡片与章体使用 Spine 网格形变；源文字由宿主生成演出贴图，不含身上字印。卡片与章体保留轻量几何接触约束，并非通用软体物理模拟。

执行 `node tools/mascot-motion/src/export-contact-studies.mjs` 可生成两套 Spine 4.2 JSON、atlas、贴图及标记为离线的关键帧，目录为 `output/mr-b-preview/contact-studies/{walk_study,stamp_study}`。JSON 包含 30 fps deform、alpha/rgb 和 drawOrder 时间轴；原生播放器经官方 Spine runtime 计算网格顶点，另用深度缓冲解决表面遮挡。接触阴影、逐像素深度和道具明暗插值属于宿主渲染扩展，不声称单独导入 Spine 编辑器即可复现全部效果，编辑器未人工验收。

## 验证与验收入口

- `contact-studies/animation-regression.txt`：**44 项通过**，含 36 项既有回归与 8 项新增检查。新增检查覆盖局部波浪／上半身稳定、消字之前确实覆盖整行高度、无身上字印、既有轮廓与浅深材质像素一致、表面与取景稳定、托卡／章柄接触、章面下压后才留下 R，以及导出时间轴在官方运行时复现。
- `contact-studies/contracts.txt`：原生运行时检查通过，覆盖实际可见像素、暂停、时间定位、减少动态、一次性完成、录制期间允许后台继续及结束后恢复暂停、销毁视图停播；复用已有中断与资格检查。
- 录像按实际呈现时间戳保存，30 fps 目标，不加速。系统录制接口曾卡在直接写验收目录的文件扩展调用；已改为后台启动录制，先写试演临时文件，成功结束后移动至验收目录，不改变任何系统权限。
- 本阶段只验证两个短样；真实保存／复习业务链路、完整／精简结算、完整辅助技术流程和 Spine 编辑器播放未验证。原速录屏供 Rex 判断步态、外观和承托观感，工程通过不等于视觉接受。


---

# Mr. B：第一阶段原生试演

角色信息和交互规则以根目录 `DESIGN.md` 的“Mr. B 角色与表现规范”为唯一来源。此次交付为真实 Spine 的隔离原生试演，**尚未接入日常 App 的入库、复习或运行事件，等待 Rex 视觉确认后再进入第二阶段**。

## 打开与操作

```sh
bash tests/mac/run-mr-b-preview.sh
```

打开 `output/mr-b-preview/MrBPreview.app`，左侧依次选择等待、知识入库、复习结算和待机动作。试演不读取日常数据库，不调用模型，首次展示标记仅存于独立的 `Rex.Review-Today.MrBPreview` 偏好域。

- 等待：阶段选择、长文案、首段正文、停止、失败、重新等待；正文即时出现，角色短暂收势后退出；进展可展开，文案每六秒原位渐换。
- 入库：模拟保存成功，首次完整、之后精简；可重置首次、重播、改变数量、模拟保存失败、重复事件、历史恢复和资源失败。浮层关闭和查看结果不等待动画。
- 复习：检查、收齐、盖章、得意；全部完成显示本轮完成，暂停、时间到、试一题、保存失败不播放盖章。表格为明确的模拟结果。
- 待机：已有四类动作低频混入两个新片段，或单独重播接到任务、歪身琢磨、左右权衡、重新集中、偷瞄装认真、藏书露角。
- Cmd+Option+1 / 2：1280×820 / 760×620；D：浅深；M：减少动态；S：本窗口截图；R：开始／停止本窗口原速录屏；3：首段正文；4：重新等待；5：完整入库；6：复习结算；7：自动录制六段原速连播。待机页也可直接点击“六段动作连播”，随时停止。

## 实际制作与验证边界

新动作由 `mr-b-rig.mjs` 生成真实 Spine 时间轴，复用已有网格、眼睛、本子、材质和渲染器；三行文字转移与成卡共用动画时钟。浮层及所有按钮是 SwiftUI 原生界面。文字、卡片轮廓与盖章道具由同一 Canvas 场景绘制，角色和阅读本子由 Spine 渲染；不把这些场景绘制声称为新增 Spine 美术附件。

因测得原构图滚动时上下超出边界，舞台绘制高度从 220 扩到 268 的逻辑空间，上移留白改为 24；没有缩小角色或动态缩放摄像机。最终浮层宽 560 pt，高度由原生排版自然确定（约 450 pt）。

自动化命令：

```sh
bash tests/mac/run-mr-b-contracts.sh
npm test --prefix tools/mascot-motion
node tools/mascot-motion/src/build-native.mjs --check
codesign --verify --deep --strict output/mr-b-preview/MrBPreview.app
```

新增检查覆盖新时间轴保留旧资产、眼白内视线、回归中立与收起道具、低频调度和不连续重复、完整与精简的逐行转移、滚动的完整取景，以及原生 WKWebView 加载、隐藏暂停、减少动态、任意中断、单次结束回调、主题切换不重播。选择契约覆盖成功／失败／重试／重复／历史／后台／模态冲突和正式复习结束原因；回复状态测试检查正文立即出现、旧退场不结束新等待、减少动态直接退出。

本阶段未验证真实模型输出、服务入库与复习保存链路，也未修改服务、业务模型、排期或日常 UI。原生试演通过不等于 Rex 已验收动作风格。完整新动作数据导出在 `output/mr-b-preview/mr-b.spine.json`；已通过官方 Spine runtime 解析，不声称在 Spine 编辑器中人工验收。

## 原生证据

录屏使用 ScreenCaptureKit 的 current-process window，在 macOS 26.7 上采集自身窗口；实际呈现时间戳、30 fps 目标、无音频，未加速。截图同样从自身窗口合成结果采集，包含 WKWebView。

- `actions-v2.mp4`：Rex 要求加大差异后的六段动作原速连播，作为当前强度的验收依据。
- `settlements.mp4`：完整入库、原生浮层与复习盖章；`reduced-motion.mp4`：最小深色窗口的静态结果。后续仅修正英文示例标题的提前截断，结算节奏与构图保持。
- `waiting-layout.mp4`：等待布局、正文出现与退场（拍摄于动作强度修订前；动作强度以 v2 连播为准）。
- `waiting-light.png`、`waiting-dark-minimum.png`、`review-complete.png`：原生比例、长英文与复习总结。
- `contracts.txt`、`animation-regression.txt`：当前原生契约与 36 项动作回归全部通过。日常内嵌动画资源的来源一致性检查和隔离 App 严格签名检查通过。

Rex 首轮指出多个动作差异不明显，因此 v2 保持固定取景，改为大幅侧倒／两侧摆动／压扁弹起／蹲低探出和突然藏书。逐帧测试覆盖放大的身体与道具在等待区和待机画布内不裁切，瞳孔不离开眼白。v2 待 Rex 重新确认，未提前接入真实业务。

记录采用原始呈现时间戳；全片使用保留原时间基准的解码检查（`-fps_mode passthrough -enc_time_base demux`），避免将可变帧率误当固定帧率。未变速。
