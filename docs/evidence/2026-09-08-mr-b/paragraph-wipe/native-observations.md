# 段落涂抹验证记录（2026-09-11）

## 已覆盖的自动检查

66 项动作回归通过。四项新增测试覆盖：段落宽度／左对齐／段末留白；距离与接触时长比例、所有长短行的消除边界处于身体接触范围内；段末卸力停顿、上移和循环前后行身份稳定；在 16 行的多个动作阶段接收完成信号后不重置已显示内容并接到同纸面盖章。既有固定周期与 34 pt 等行距断言按新排程改为动作阶段与实际段距检查；没有删除接触、先清除后上移、网格不翻折和盖章时序约束。

原生契约最终通过，新增实际绘制的长／短行和段间距检查。首次在旧有「减少动态」检查失败，第二次在 A／B 接点检查失败。前者使用固定 100 ms 等待，后者只检查 A 状态而可能读到前一个场景的绘制帧；改为有时限地等待目标状态／时间帧绘制后复跑通过，未改变动画以适配断言。两次失败日志保留；首个失败的全部环境影响未独立隔离，不能声称只是机器负载。

## 表现参数与边界

三个段落模板共八种稳定行长，原始舞台宽度分别为 140／112／64、124／76、136／100／52。段内间距 28、段间 40；段末卸力后停 0.22 秒。接触擦除时长按行长从约 0.119 到 0.32 秒变化，局部波浪接触前沿同步移动；准备与恢复也随行长小幅调整。完整行周期约 1.039 到 1.2 秒（包括段末停顿）。这些数值为当前实现，节奏仍待 Rex 原速视觉确认。

独立三行短样保留 4.6 秒总长；A 循环、B 收尾、完整／精简入库复用同一排程。盖章路径、纸面、角色材质、真实业务均未改变。导出的 JSON／atlas／关键帧位于忽略目录 output/mr-b-preview/settlement-suite，可通过现有导出命令重建；A／B 是宿主事件排程，不宣称为固定时长 Spine 文件。

## 原生实看

- 标准窗口 1280×820：原生实看 A 循环，长短行统一左对齐；段末较大的间距跟随上移，角色底缘局部起伏，无新增四肢或整体翻转。自动演示约 10 秒后进入 B，最终在同纸面留下 R。原速录像 18.70 秒，547 帧，实际平均约 29.26 fps，无音频、无加速，已完整解码并检查段落／盖章／最终关键帧。
- 最小窗口 760×652（内容 760×620）深色：纸面、角色、行长与控制均可见。回看至 2.586 秒看到短行、段间留白与下一段，打开网格检查局部波浪和接触点；前后逐帧可返回同一时刻，关闭调试后截图保存为 minimum-dark-paragraph.png。
- 减少动态：重新开始保留长中短静态横线，点击模拟保存直接出现 R；关闭减少动态后可重新开始正常循环。
- 正常播放约 6.45 秒时触发失败，时钟收势至约 6.63 秒停止，保留已擦除部分与剩余长短行，没有印章或 R。后台暂停与取消另由原生契约覆盖。
- 试演窗口恢复标准尺寸、浅色、无网格，停在 A 的准备段供 Rex 继续查看。

首次手动录制等待过长，超过原 45 秒录制上限才播完全部收尾；该文件仅保留在忽略目录 output/mr-b-preview/paragraph-wipe/manual-recording，不作为完整验收录像。新自动入口与手动入口共用播放器，只按已播放时间触发模拟保存；最终录像完整结束。

本轮没有进行 Spine 编辑器人工导入、完整辅助技术检查或正式服务／保存联调。真实用户对段落间停顿和快慢差异的感受仍待 Rex 验收。
