# 单题回应原生观察（2026-09-10）

本轮仅在 MrBContactPreview 中使用模拟答题状态，没有运行正式模型或写入日常复习记录。原生窗口通过 CUA 实看，录像来自试演应用自身的 ScreenCaptureKit 采集。

- 标准窗口 1280×820：默认 A／B／C 无语义标签并排播放。A 明显压低再挺高，B 左侧探近并点头，C 转向右侧后停留；固定取景，没有靠移动镜头放大差异。
- 最小窗口 760×652（内容 760×620）：三条短样与控制可见；深色／减少动态下保留三个不同的代表姿态，见 minimum-dark-reduced.png。三条短样结束后归同一中性体态。
- 最终构建再次验证暂停定位至 0.995 秒、后移一帧至 1.028 秒、前移一帧回到原位，局部网格与接触点可见；关闭调试后正常播到 3.2 秒。结束后退到 3.167 秒再播放也能完整停止。
- 反馈页使用 190×184 pt 画布；最小窗口浅色原速认可片段已录制。角色位于解释左边，判断文案与采用／改判操作在动画期间已出现。长内容和试演控制通过纵向滚动访问。
- 需要再看样例：深色减少动态下，角色向右侧解析倾身；改判为回答顺利后同时保留原判断与选定值，采用后可进入下一题。下一题清空答复并恢复输入焦点。
- 模拟判断失败：输入 a 后提交，失败说明明确回答保留，可以重试，没有答错惩罚动作。失败与过期结果、重复提交、重试及改判不重播另有状态契约覆盖。
- English + 减少动态 + 费力样例：解释明确费力回忆不等于回答错误；最小窗口文本换行可读。模拟动画失败后显示占位说明，采用判断仍成功、Next question 可用，见 minimum-english-fallback.png。English 仅覆盖产品反馈样例文案，试演控件和降级提示未全面本地化。
- 原生契约验证三个动作后台停止计时、返回后收势、减少动态静态结果、主题和偏好切换不重复结束事件。真实 Spine 导出回放正反定位、网格不翻折和面部在体内由 JS 检查覆盖。

## 连续重置检查中的修正

角色正收势时再次重置，旧逻辑可能重新使用原始反应姿态；改为从当前收势帧续接。新增原生像素比较检查。首次和诊断检查要求 PNG 字节完全相同，失败；解码后只发现 3,584,000 个通道值中的 21 个不同，最大差为 2/255，合计差 27，属于再次插值后的边缘取整。将检查改为原始通道差不超过 2/255，且变化比例小于 0.01%，最终通过；没有放宽动作幅度或删去检查。失败日志保留。

## 证据边界

两个录像保持实际时间戳，目标 30 fps，实际平均帧率见 recordings.json，不宣称恒定帧率。已完整解码并查看关键状态。无标签对照先录制，之后只补试答／录制控件与连续重置衔接，三条动作和尺度未变；最终版本另行构建及原生自测。

动作轮廓已可区分，不代表用户已能准确读出情绪；是否像 Mr. B、是否过度拉伸、鼓励是否温柔、引导是否清楚仍待 Rex 原速视觉确认。Spine 编辑器人工导入、完整辅助技术流程、正式业务接入和真实判分未验证。
