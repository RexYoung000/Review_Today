# 指定材料、JD 分析与半截回复恢复

2026-09-26。工程已实现，当前 Xcode 开发版已更新；Rex 的实际使用体验验收保持开放。

## 修复范围

- 具体 JD 走岗位分析，传入原文和用途；方法教学单独识别。没有履历不认定个人缺口。
- 指定材料最多读四个去重 URL；脚本、登录页、空壳不当作正文。先做正文检查，再判断内容是否足以支撑请求。既有可用备用源只处理同一个 URL，不追加无授权搜索。
- 缺 JD 时等待正文并保留当前目标；补充后续接。JD 和产品分别记录状态，小程序名称不代表已读到产品内容。
- 普通 URL 概括直接回答并提供实际来源，不自动套知识整理模板或生成课程。
- 部分正文格式失败后，同节点最多恢复一次。恢复片段隐藏，完整校验通过后以同一回答 ID 替换；停止、旧版本和预算不足阻止继续发布。

## 验证

- Python 定向回归：249 passed、42 subtests passed。覆盖路由、网页读取及回退、范围与隐私、调用记账、格式恢复、消息流。随后针对最终的材料续接、URL 简答与输出格式增量验证：140 passed、16 subtests passed；并补测预算耗尽、重复编号和旧误路由失败任务重试：重算入口判断，保留原任务 ID，转换为具体 JD 分析。
- 原生 `ConversationReplayTests`：恢复中状态、同 ID 整段替换、停止后迟到完成、旧 revision、重放幂等、归档守卫和真正 SSE 传输通过。受控停止本地落盘约 0.9–1.2 ms，SSE 受控接收最大延迟 25 ms；不当作真实模型延迟。
- Debug `xcodebuild` 成功。只更新原有 DerivedData 开发 App，退出旧进程后重新打开；`/healthz` 返回 ok。今天页仍显示 8 条知识，原有聊天与失败记录保留。没有生成新的预览 App。
- 两条旧测试仍要求“理解后才能保存”，与现有保存/复习分离契约冲突；另有测试依赖本机默认供应商、匿名网页开关。已在 HEAD 基线重现旧失败，随后只修正这些测试的契约和显式配置，未改变保存规则或供应商配置。

运行命令：

```sh
cd agent-service
.venv/bin/python -m pytest -q tests/test_material_routing.py tests/test_response_stream.py tests/test_structured_recovery.py tests/test_conversation_v2.py tests/test_web_tools.py tests/test_web_failover.py tests/test_scope_privacy.py tests/test_selective_web.py tests/test_source_links.py tests/test_contextual_scope_reply.py tests/test_run_controls_scope_usage.py
.venv/bin/python -m tests.material_routing_real_smoke --live --output <新的结果文件.json>
# 在仓库根目录
python3 tests/mac/run-contracts.py ConversationReplayTests
```

## 真实模型与实际调用

使用独立临时数据库、合成岗位/产品内容和当前模型配置。JD 及产品网页由合成读源控制；最后一个 Python 文档链接使用真实网页服务读取。格式恢复用“真实模型输出后注入一次格式错误”验证，不伪称供应商在该案例自然出错。

| 场景 | 整轮耗时 | 模型请求数 | 实际输入 / 输出 token |
|---|---:|---:|---:|
| 问候 | 2.304 s | 1 | 9,727 / 346 |
| 面试背景 | 2.255 s | 1 | 9,898 / 360 |
| JD 空壳页面，等待正文 | 5.418 s | 3 | 13,713 / 785 |
| 补充 JD，续接同一任务 | 12.623 s | 3 | 16,073 / 1,451 |
| JD＋产品，注入一次格式失败后恢复 | 11.697 s | 4 | 16,663 / 1,642 |
| 实读 Python 公开文档，简短回答 | 9.455 s | 4 | 17,817 / 817 |

供应商用量返回的模型：deepseek-flash。节点请求仍沿用项目的 router/coach 配置；实际用量、缓存 token 和请求模型均在 JSON 中。费用没有取得本次账单或核定费率，未虚构估算；这些结果也不是提速或降本证明。

原始记录：

- [第一轮](live-replay-1.json)：保留失败。公开文档已读到，但模型把不相邻段落拼成“原文摘录”，两次校验未通过；没有继续生成答案。已将证据要求收紧为单个短的连续摘录并定向修复。
- [第二轮](live-replay-2.json)：六轮均完成；缺材料、续接、双材料、整段恢复、真实 URL 读取均通过。但最后的普通概括仍套用了整理模板，正文中对 `pop` 返回值的概括也不够严谨；如实保留，不算作语义质量全通过。
- [公开 URL 增量复测](live-public-3.json)：改为普通回答并提供实际引用，没有任务。返回值中已明确 `pop` 返回移除元素，但补充句仍绕，后续可继续校准简洁表达。

原始输入输出只含合成数据和公开文档，没有用户实际招聘链接、日常聊天或凭证。

## 验收入口与边界

在当前开发版左侧打开「你好」会话，点击原失败步骤的「重试」，或新建会话发送公开 URL。预期是读取实际正文；若招聘页只给脚本/要求登录，承接面试目标请求 JD 文字。随后贴出正文，应得到具体岗位分析而非“如何拆 JD”的课程。

本轮没有重新发送用户的实际招聘链接，没有操作微信小程序、没有新增 OCR，也没有修改正式复习结果。原生窗口已重开并检查基础状态；真实屏幕上短暂的恢复提示、完整 VoiceOver 阅读及最终文案体验尚待进一步实测。本轮只做指定修复的定向回归，没有重启 Harness 整体质量评测或宣称 Jev 自动放行准确性。

## 重试无反应补修（2026-09-26）

用户实测发现重试未更新界面。检查实际记录确认第一次重试已被服务执行，完成为等待材料；原生忽略控制响应中的运行状态，失败会话也已停止订阅事件，因而没有接续结果。连续点击产生多个独立操作，其中失效重试被服务器拒绝后仍无限重发，阻挡后来排队的取消。用户已点击取消，所以后续回复被本地停止守卫拦住；不能为恢复显示而撤销取消。

修复：持久化重试立即显示等待确认，同一运行的待发送恢复操作去重；接收并验证控制收据，更新运行状态、重新订阅事件，订阅版本防止旧连接的结束覆盖新刷新请求。失效重试核对服务当前状态后结束投递，记录拒绝原因；传输失败保留相同操作 ID。停止／取消保持优先显示和迟到结果守卫。取消目标不再显示旧的材料补充提示。

原生 `ConversationControlTests`、`ConversationReplayTests`、`SessionOrganizationContractTests`、`LocalDataResetContractTests` 通过；覆盖 20 次连续点击、超时重发同一 ID、服务接受与快速完成、订阅交错、旧重复请求让出队列给取消、错误／过期收据、事件幂等、归档／停止守卫和数据清理边界。真实 SSE 解析复测通过；Debug 构建通过。

更新并重新打开同一个 Xcode 开发 App 后，本机原有 8 项积压操作全部得到处理（拒绝保留原因，不冒充执行成功），待发送操作为 0；用户原先排队的取消已送达，界面显示“目标已取消”，8 条知识和 8 条原消息保留。没有清除原失败记录、没有重新请求实际招聘页面、没有新增模型测试聊天。完整鼠标快速连点手感及新一轮真实模型回复仍由后续体验确认；本轮自动化连点为原生控制入口的受控验证。

## 网页增强读取实测（2026-09-26，仅验证，浏览器接入未实施）

Rex 确认先验证现有工具的增强能力。本次单独调用现有账户的 Exa Contents 新鲜抓取和 Tavily advanced，各读取同一条公开 BOSS 岗位链接及 Python 官方文档一次，共 4 次请求；没有请求模型、重试、改变 App 默认配置或写入用户会话。BOSS 原链接及其跟踪参数不进入提交的证据，原始请求／响应仅保存在本机临时实验目录，文件权限 0600；其中不保存认证头。

| 材料 | Exa Contents，强制新鲜抓取 | Tavily advanced |
|---|---|---|
| 用户提供的 BOSS 岗位 | 2.632 秒，返回 3310 字符脚本，正文检查拒绝 | 13.495 秒，0 条结果，`Failed to fetch url` |
| Python 官方数据结构文档 | 1.203 秒，实际正文 20000 字符，达到请求长度上限 | 1.243 秒，实际正文 15774 字符，也包含导航内容 |

结果见 [脱敏实测记录](url-reader-probe-1.json)。公开文档对照证明本次两个账户的直接读取接口可用；不能据此承诺任意网址成功率。BOSS 的 Exa 响应虽为 `success / crawled`，实际仍没有职责或任职要求，不能作为读取成功的证据。前一轮普通浏览器检查中同一链接在页面加载后展示了实际岗位正文；这只是浏览器可读的证据，App 尚无该兜底链路。

Exa 两次响应的费用估算合计 0.002 美元，未核对账单。Tavily 两次 `usage.credits` 都返回 0；官方按成功 URL 分组计费，不能解释为免费。单次耗时不是稳定延迟或整轮性能结果。此次 Tavily 岗位请求用时超过 App 当前每服务 12 秒预算，因此增强接入还须一起处理整轮等待预算、状态及取消，不能仅将 basic 改成 advanced。

结论：增强模式没有解决当前招聘链接，不能把它直接设为默认并声称修复。下一阶段需要独立实施通用浏览器正文读取，保留公网边界、有限等待、取消／版本守卫、实际正文检查和真实失败说明；尚未验证登录页面、验证码、图片／视频内容或跨网站覆盖。

接口依据：[Exa Contents](https://exa.ai/docs/contents/quickstart)、[Tavily 提取模式](https://docs.tavily.com/documentation/best-practices/best-practices-extract)、[Tavily 用量返回](https://docs.tavily.com/documentation/api-reference/endpoint/extract)。

## 浏览器兜底接入与实测（2026-09-26）

已接入 Exa → Tavily → 本机临时浏览器，Debug App 启用，裸服务默认关闭。已安装固定版本 Playwright 1.63.0，复用本机 Chrome；没有增加 App 副本。搜索、DeepSeek 配置、Jev 职责和正式复习数据保持现状。普通提取成功时不启动浏览器；最后一层获得正文后回到同一材料与回答流程。

浏览器使用临时会话及公网代理，凭证不进入子进程。所有资源、跳转和连接都经过公网 IP 校验及固定连接；系统 Fake-IP 的公开 DNS 查询只传域名。最长 25 秒，无自动重试；整个读取链 60 秒，材料读取与随后查证共享每轮 60 秒／10 次网页预算，并遵守整轮 180 秒截止。取消时终止工作进程并清理临时目录，迟到结果不能写回。来源保留实际最终 URL、截断与读取方式；站点新增的敏感参数不导出，官方域名限制在跳转后再次检查。

隔离数据库使用公开链接与合成请求，结果见 [browser-fallback-1.json](browser-fallback-1.json)。原始输入、输出、模型提示和快照另存于本机临时实验目录，目录权限 0700、原始 JSON 权限 0600；不提交用户 BOSS 完整链接及跟踪参数，不记录认证头。

| 场景 | 实际结果 | 整轮耗时 | 本轮实际模型请求 |
|---|---|---:|---:|
| BOSS 原链接，真实完整读取链 | Exa 返回不可用内容，Tavily 失败；浏览器出现安全验证页。停在待补材料，未生成虚构岗位分析 | 19.234 秒 | 2 |
| Python 官方文档，真实快速读取 | Exa 2.579 秒取得 19999 字符；模型证据摘录过长，首次和一次格式恢复均失败，保留可重试状态 | 8.659 秒 | 3 |
| 补充字符上限说明后，重试同一 Python 轮次 | 复用已读正文，无新增提取请求；完成用途解释和可点击官方来源 | 5.420 秒 | 新增 3 |
| 模拟两家提取失败，真实浏览器＋真实模型 | 浏览器 5.369 秒取得 19999 字符正文，完成回答及来源引用 | 11.710 秒 | 4 |

最后一项有明确故障注入：两个远端错误是合成的，没有真实 Exa/Tavily 请求；浏览器网页加载和 4 次模型请求是真实的。动态 JavaScript 与页面导航另由真实 Chrome 加载受控 HTML 验证。不能把此项说成该网页在 Exa/Tavily 上真实失败，也不能以此宣称任意网址成功率。

以上样本共 12 次真实模型请求、3 次真实提取服务请求、2 次真实浏览器读取；模型用量累计输入 53119、输出 2732 token，命中缓存输入 37888 token。未匹配账单与当次价格，不给出伪精确费用；浏览器自身未调用第三方付费提取 API。这些数值只覆盖表中样本，不含前面的诊断探测或 App 启动能力检查。读取耗时与整轮耗时分别保留，不把单个样本当作稳定延迟或降本结论。

真实回放发现的摘录长度问题已修复提示与诊断：英文也按字符（含空格／标点）限制；格式恢复带可信数字上限，并要求最短连续原文片段。仍保留 240 字符及逐字出处校验，不静默截断或把模型失败当成材料通过。第一次失败留在报告中。

定向验证通过：

- 网页链、材料路由、隐私与条件核验回归：176 项、60 个子案例；4 个浏览器运行测试当次默认跳过，后续已显式运行通过。
- 恢复提示、兼容适配器、增量回复与材料路由：受控 `openai_compatible` 环境 54 项、11 个子案例通过。本机默认 DeepSeek 环境不适用于其中 OpenAI 兼容端点的模拟配置，首次未隔离 provider 的 7 项失败已用正确环境重跑。
- 真实 Chrome 受控动态页面、导航后来源、访问页、空壳超时、私网导航拦截通过；服务缺失／非法输出、取消进程与临时目录清理、共享预算、过期结果、官方域名跳转拒绝由定向测试覆盖。
- 合并每轮网页预算后的最终定向回归（含真实 Chrome）：139 项、32 个子案例通过；材料读取与随后核验不能各获得一份新预算。
- 原生 `AgentServiceMonitorContractTests`、`ConversationReplayTests` 与真实 SSE 解析通过；Debug 构建通过。重新打开同一个 Xcode App 后，健康检查列出 Exa、Tavily、Browser，既有三个 DeepSeek 角色 ready；界面仍显示原有 8 条知识与会话。

**仍未通过：BOSS 在隔离浏览器中取得 JD 正文并进入岗位分析。** 本次站点实际显示 IP 安全验证，已停止而未执行验证或复用用户登录态。早先普通浏览器会话曾显示正文，不代表全新会话始终可读。登录／验证码、图片文字、子框架正文和任意网站兼容不在已验证能力中；完整鼠标交互的新消息测试及最终回复语气仍待 Rex 体验。原聊天不会被实验结果覆盖。

入口：使用当前 Xcode 开发 App，在 Agent 粘贴公开 URL 并说明读取目的。普通读取失败时，运行详情出现“正在加载网页内容”；成功后继续回答并引用来源，失败则说明本次正文缺口。原先已完成为待补材料的聊天可再次发送链接发起新读取；旧的“链接打不开”历史回复保留原样。

运行依赖和限制见 [网页工具说明](../../../agent-service/providers/web/README.md)。实现依据：[Playwright 浏览器](https://playwright.dev/python/docs/browsers)、[网络与代理](https://playwright.dev/python/docs/network)。
