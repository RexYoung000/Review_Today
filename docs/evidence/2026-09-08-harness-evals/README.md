# 2026-09-08 Harness 评测交付证据

评测系统已实现；当前模型与 Harness 的质量门槛未通过。没有对生产 Harness、提示词、供应商配置或原生 App 做产品修复。裁判尚待 Rex 校准，机器得分不可当作最终验收。

| 批次 | 通过 | 失败 | 运行错误 | 待复核 | 门槛 |
|---|---:|---:|---:|---:|---|
| v1 校准 12 例 | 5 | 4 | 3 | 0 | 仅校准，不作基线 |
| v1.2 完整 40 例 | 25 | 6 | 8 | 1 | 未达到 36/40 且各模式 7/8 |
| v1.2 关键 10 例 × 3 | 21 | 4 | 5 | 0 | 未达到 30/30 |
| v1.2 真实联网单例 | 0 | 1 | 0 | 0 | 独立观察，不混入固定来源成绩 |

完整基线样本通过率 62.5%。各模式分别为 Auto 6/8、知识整理 5/8、资料学习 5/8、主题探索 5/8、问题攻克 4/8。关键重复中 A01、A06、O01、S03 三次全过，即 4/10 场景。以上批次没有检出的硬失败；不能由有限样本推断没有其他风险或所有用户请求的通过概率。

代码生成/评分基线为 `ea42f3b`（生产内容继承 `3428a79`）。模型为现有 DeepSeek V4 Flash / Pro，智能强度；裁判单独调用 Pro。完整批次 281 次模型请求、关键重复 183 次，联网单例 6 次；停止协议 A06 不需模型评分。模型请求数包含实际 SDK 重试和裁判，不估算未拿到的价格。每批次具体哈希、时间、各请求 usage 在压缩原始包中。

## 优先检查的问题

1. **多轮任务目标引用**：完整基线 A03、O04、O05、O07 遇到 `RT.INTENT.INVALID_TARGET`。检查意图结果中的任务目标与当前任务/草稿的绑定，不能直接删除现有授权守卫绕过错误。
2. **学习计划引用**：S03、S05 遇到 `RT.PLAN.INVALID_STEP_REFERENCE`。检查生成计划和既有步骤 ID 的契约与恢复。T08 被标为 interrupted，需要核对停止/换主题混合意图。
3. **结构化输出**：P07 运行错误；P03 裁判格式失败导致待复核。裁判错误和被测系统错误分别记录，不能都算内容不正确。
4. **评分需人工校准**：旧版裁判曾把正确的停止不回答判为教学不足，v1.2 改为纯规则协议检查。真实联网试验的三个页面读取均未成功；教练诚实说明没有外部依据，但裁判错误地沿用了“固定夹具可用”的理由。该条机器意见需要复核，不能据此要求产品假装读过资料。

问题表是机器归类的候选问题，同一根因可能影响多个样本。先人工确认/合并根因，再决定产品修复；不因评测失败自动改代码、自动改变金标或降低门槛。留出样本现已被首轮基线使用，后续优化需要维护新的冻结留出组，不能再声称它们从未被观察。

## 运行器自身修复与验证

初版完整批次 `20260908T090507Z-full-e9cba4` 的 O06、S08 含运行器重启调用参数错误，不作为最终质量基线。修正真实 `recover()` 调用，新增真实 SQLite + Harness 重启集成测试，v1.2 两个场景已真实复验通过。v1.0 O04 的表达理解和点击保存拆开为符合实际 UI 的步骤；原始校准结果保留。

全量服务测试 314 passed（包含当时 26 项评测器测试），随后仅改飞书批次显示/分类，29 项评测器专项通过。原生代码没有改动，本次没有重新构建 Mac，也没有把服务模拟 ACK 当作 SwiftData/FSRS 保存证明。

## 飞书验收入口

[质量总览](https://my.feishu.cn/base/ZDJeb5B9na4TAgs8PDncQRMKnFh?block=blktXbRhatkjdPTQ)：主指标优先显示最新的 40 场景固定来源基线；关键重复单独显示；真实联网与固定来源不混合。趋势仅包含固定来源，不同样本/评分版本之间不能直接归因为产品改进。

- 结果明细 → 失败与异常：看实际对话、机器理由和错误代码。
- 结果明细 → 人工校准：填写人工结论和原因，后续同步保留。
- 问题处理：查看问题分类、证据和建议检查入口，确认后修复。
- 样本：按版本筛选当前 v1.2 的 40 例；历史版本保留用于追溯。

CLI 用户身份已验证。重复同步前后均 153 条历史结果，未新增重复且人工备注保留；11 个指标回读一致（主基线 25/40，重复 21/30，人工待复核 40）。验证记录见 `feishu-verification.json`。完整页面视觉验收尚未完成：Codex 浏览器没有飞书网页登录态，外部浏览器正在被用户使用，未继续打断操作。Rex 可用已登录飞书的浏览器打开上方链接验收布局、展开记录和人工字段。

## 原始记录

`index.json` 汇总版本、计划与分数。每个 `.json.gz` 包含完整 manifest、当时的样本/来源/rubric 副本以及逐试验记录；都是合成数据与公开资料，没有正常用户数据库、密钥或隐藏推理。可用 Python 的 gzip/json 模块读取。相邻 `.md` 可直接阅读对话与评分；其中本地输出路径属于本次执行环境，跨机器请以压缩包内容为准。
