# 轻抬入加强档 · 2026-09-13

Rex 确认 8pt 轻抬入已经可见，但反馈表现太轻，授权继续加强。同一效果改为 **24pt 位移、260 ms 收尾**，淡入并行；不增加缩放或额外弹跳。首次／同页不重播、快速切换不排队、减少动态直接归位、输入不等待的规则保持。

本轮仅调整现有位移和时间参数。普通 Debug [构建](debug-build.log) 和 [原生导航回归](contracts.log) 通过，后者包含实际 SwiftUI 导航、显示层身份、点击穿透、快速中断、减少动态与失活等既有契约；完整输入／焦点测试组、Release、压力计时未重跑，不把旧结果当新结果。

[浅色原速](light.mp4) · [深色原速](dark.mp4) · [减少动态原速](reduced.mp4)。使用原生生产页面、独立数据和程序导航，只捕获自身窗口，无声音／模型请求，原始时间戳保留。连续帧检查可见加强后的移动；同一 Today 标题 y73 → 72 → 69 → 67 → 60 → 58 → 54 → 52 → 50 → 49，实际位移 24px，见 [像素轨迹](title-motion.json)。减少动态下淡入层 opacity 恒为 0；三段完整解码无错误，见 [录制检查](checks.json)。程序导航录像不能作为 OS 输入到屏幕延迟或恒定帧率证据。

同签名日常应用已经正常退出、备份、替换并从新访达窗口精确路径启动；实际动态库校验一致，见 [安装记录](installed.json)。原有 3 会话、8 知识可见，5 次主页面导航正确，Agent 输入区接受焦点；本次未输入测试草稿。隔离 QA 已关闭。

试用今天 → Agent → 知识库 → 待处理：上移幅度是上一档三倍，收尾更充分，快速连续操作应直接跟随最后选择。最终力度和节奏仍待 Rex 验收，既有压力性能问题继续开放。
